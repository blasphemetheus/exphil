# Session Handoff — 2026-07-08 (melee-bot)

Compact state for resuming. Read this + MEMORY.md; avoid re-deriving.

## Where we are (one paragraph)

Pipeline is certified (3-tier overfit gates, embed-path parity, backend
parity), four silent-corruption bugs killed this week (scrambled live
embeddings #53, smoothing×pos_weight button poisoning #54, char-ID table,
NaN handling). Best general model: `clean_200_v3_long_*_best_policy.bin`
(mamba, 200 files, 30 epochs, fixed loss — val 4.74, mx 46%). Two new
training features landed for frame-precise play: **--prev-action**
(model sees its own last controller; agent feeds outputs back, regime
stored in policy config) and **--action-delay N** (targets shifted to
controller(t+N) so presses land where aimed). Probe experiments on the
multishine fixture show each helps (prev-action: conf 0.99 live, learned
shine→JC→aerial-shine chains) but the grounded multishine still doesn't
close live.

## The open thread (resume here)

**Delay is 2, not 1.** Frame trace of the delay-1 probe live
(scratchpad/trace_delay_probe.exs) shows the JC press missing the af≤3
reflector window → model drifts into af4-8+ states absent from training
(n=134 stuck = user's "shine wait"). Root: replays record inputs on the
frame they LAND (fixture's 135 X presses all sit at jumpsquat af0, never
in reflector), and live has obs-lag 1 + input-lag 1 = round-trip 2.
**Next experiment (5 min):**
```
mix run scripts/train_multishine_policy.exs --prev-action --action-delay 2 \
  --out checkpoints/multishine_delay2_policy.bin
mix run scripts/play_dolphin_async.exs --policy checkpoints/multishine_delay2_policy.bin \
  --dolphin ~/.local/share/slippi/netplay --iso ~/isos/melee.iso \
  --character fox --stage final_destination --deterministic --on-game-end stop
```
Success = sustained grounded 9-frame loop (count via
scratchpad/trace_delay_probe.exs pattern: jumpsquat→shine transitions).
If delay-2 still misses, trace again — the af histogram tells you which
frame the press lands on; adjust. Consider also that the GRU window build
live pads with duplicated first frames (train windows don't).

## Probe scoreboard (multishine fixture, live in Dolphin)

| Config | Result |
|---|---|
| zeros prev, sampled | 1 cycle/13k frames, junk buttons |
| prev-action, sampled | shines often, shine→JC→aerial chains, taunts (dice) |
| prev-action, deterministic | shines then locks in grounded shine-wait |
| prev-action + delay-1, deterministic | JC misses window, af4+ OOD freeze |
| prev-action + delay-2 | ← NEXT |

## Queued after that

1. **E2 (task #22):** 500 files × 50 epochs, mamba, current loss stack,
   `--prev-action --action-delay 2` if probe validates (else without).
   Overnight run. Compare vs clean_200_v3_long on diagnostics + live.
2. **Task #23 backlog:** polar action space (slippi-ai custom_v1 — best
   steal), tx_like backbone, observation filters, factored char×action
   embed, per-head decode temps.
3. Mewtwo ship run (see MEWTWO_TRAINING_PLAN.md; general corpus has ~0%
   Mewtwo — specialist data is the only signal).

## Decode findings (live play, user-verified)

- temp 1.0 sampling = random-feeling; argmax = modal lock ("personality":
  over-shields to shield-break, does nothing at neutral, but shield-grabs!)
- Sweet spot: `--temperature 0.4 --deterministic-buttons` (new flags)
- Dash-PIVOTING instead of dash-dancing = mistimed flips (motivated
  prev-action; recheck after delay-2 models)

## Key facts that save re-derivation

- val_loss NOT comparable across loss configs; use diagnostics (stick acc,
  rare recall Z/L, diversity) + live play
- Plain NLL collapses rare buttons at 200-replay scale (A/B'd) — keep
  focal+pos_weight until ~100× more data
- Label smoothing floor ≈1.62 for 5 cat heads at ε=0.05 → "flatline" =
  converged; buttons are NEVER smoothed now (GOTCHAS #54)
- 30 epochs ≫ 10 (val 4.74 vs 5.03, still improving) — undertraining was
  chronic
- General corpus mix: fox 25% falcon 23% falco 19% marth 10% (logged
  per-run now)
- slippi-ai lessons doc: docs/research/slippi_ai_lessons.md; clone at
  ~/git/slippi-ai
- No `mix` while training runs (NIF clobber); play sessions coexist fine
  with training (~29GB/32GB used by train, inference fits in remainder)
- Session cmds: tmux sessions `train`/`play`; kill Dolphin with
  `pkill -f "AppRun[.]wrapped"`; replays land in ~/Slippi (finalize on
  game end — quit to menu, or SD out)
