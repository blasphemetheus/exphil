# Session Handoff — 2026-07-08 → 07-12 (melee-bot)

> **SUPERSEDED**: current handoff is `HANDOFF_2026-07-15.md`. This file is
> the blow-by-blow archaeology of the 07-08→07-15 arc.

## 2026-07-14 addendum (read first)

- **NaN SAGA CLOSED (validated 19:03)**: clamped run survived to 556k steps
  (all unclamped deaths: 284-456k), param_max 5.40 past the old "critical
  scale". Bonus: the first clamp attempt (Nx.clip) exposed an upstream Nx
  bug — clip gradients are g² (double-multiply in grad.ex; invisible in
  sum-based tests) — pinned (nx/test/nx/fuzz_clip_grad_test.exs, fails on
  nx main) and FIXED in the local checkout (6/6 pin, 233/233 regression).
  exphil uses the min∘max clamp form regardless. 2e-4 is now a safe LR.
- **NaN SAGA SOLVED (crime-scene autopsy)**: the fatal batch carried button
  logits at −89.91 — f32's exp-overflow cliff is 88.7. XLA's algebraic
  simplifier can rewrite the stable BCE form (or its adjoint) into exp(+x)
  variants during fusion, so stability depended on compilation luck; the
  training pipeline's fused graph made the unstable choice (the same step
  replayed in a freshly-compiled program is CLEAN). Explains all six blind
  refutations. **Fix: `Nx.clip(logits, -60, 60)` in `imitation_loss`**
  (mathematically inert, rewrite-proof). Validation run in flight —
  survival past ~550k steps closes it. Crime-scene infra
  (`interp/crime_scenes/`, `checkpoints/cliffs/`,
  `scripts/replay_crime_scene.exs`) turned each hypothesis into a 2-minute
  replay; that's the tooling lesson of the week.

- **NaN ROOT CAUSE NAMED (grad-localizer, step 344,769)**: NaN gradients
  born in the **buttons-BCE backward under bf16 compute** — loss forward
  still finite, buttons head + trunk grads non-finite, all other heads
  clean. Explains every refutation (mixed precision also ran backward in
  bf16; window/seed/pool don't touch the buttons head; LR = arrival speed
  at the extreme rare-button-logit regime). **Fix: logits cast to f32 at
  the loss boundary** (`imitation/loss.ex` build_loss_fn) — network stays
  bf16. Confirmation run in flight (prior deaths 323-345k steps on this
  recipe; survival past 500k = NaN saga closed).
- **TechRandom dummy is now REAL and MOVES**: `--dummy-cpu-level 0` fix
  verified — first-ever successful techs (mixed classes: 199 in-place,
  201 roll-back + missed), and the dummy now walks toward the bot with
  occasional shield holds between knockdowns (step/3 protocol: runner
  passes the bot's player state). New-era data begins here; do not compare
  scores across the fix.
- **Shield-lock task open** (user-observed shield-held-until-break):
  hysteresis dead-zone hypothesis; diagnosis + shoulder-specific release
  threshold planned (task #6).
- Zombie-wrapper NO-MIX false alarms: guard scripts should use
  `pgrep -x beam.smp` (process name), not command-line text matching —
  harness wrapper shells carry script names in their args.

## 2026-07-13 daytime addendum

- **THE DUMMY WAS NEVER THE DUMMY (drill-invalidating bug, found by interp
  Phase 1)**: every probe/drill recipe passes `--dummy-cpu-level 3`, and
  melee_bridge's menu helper sets port 2 as a **CPU level 3** — the game AI
  owns the port and ignores pipe inputs. The Elixir TechRandom dummy has
  been pressing buttons into a void since the tech-chase drill began:
  ground-truth audit shows **0 successful techs in 91 knockdowns across 12
  probe replays** (lvl-3 CPU Fox never techs), digital R presses recorded
  in-replay, in-window, even held through touchdown — ignored. All combo
  scoreboard numbers are really "conversion vs lvl-3 CPU Fox" (relative
  comparisons still valid — same opponent everywhere). The reaction-drill
  premise was never active. **Fix: `--dummy-cpu-level 0`** in every
  tech_random recipe (dagger_loop.sh + all probe scripts); tech press
  timing hardened (@tech_height 18, 8-frame burst). Verification game in
  flight. Once verified: future pools/probes are a NEW benchmark —
  don't mix scores across the fix.

- **NaN ROOT CAUSE CORNERED (forensics + code audit;
  `logs/diagnose_nan_20260713.log`)**: the drill trains PURE bf16 — params
  and optimizer state at 8 mantissa bits — because `Imitation` defaults
  `precision: :bf16` / `mixed_precision: false` and dagger_drill.exs never
  overrides. **`backbone_defaults(:gru)` already says `precision: :f32`,
  but the drill cherry-picks only shape fields from bb_defaults and drops
  it.** Instrumented run (`--nan-forensics`, vitals every 100 steps) died
  at step 456,055: params flat (5.13→5.15 over the final 3k steps), nu
  frozen at 0.048, loss finite to the last sample, then total poisoning in
  ≤55 steps — a single-step optimizer-side detonation. Forward overflow
  refuted; loss code audited clean (stable BCE/log_softmax, all fancy
  options off in the drill path). Per-epoch shuffling did not prevent it.
  **UPDATE 11:17 — the mixed-precision control DIED TOO** (step 336,695,
  f32 master + f32 optimizer state), refuting precision-of-accumulation as
  the cause. Decisive clue: both forensics runs detonated at nearly the
  SAME WEIGHT SCALE — param_max 5.15 (bf16) vs 5.21 (f32 master) — at
  different step counts. **New leading theory: exploding BPTT gradients
  through the 60-step GRU recurrence.** LR sets the weight growth rate;
  the trigger is a critical spectral radius (~5.2) where 60 backward
  Jacobians overflow in ANY precision (bf16/f32 share exponent range).
  Fits: LR↔steps-to-NaN (2e-4 ~340-460k; 1.5e-4 673k; 1e-4 762k),
  single-step death, grad-clip impotence (clip(inf)=NaN). Discriminating
  test: `--window 16` (flag added; 16 Jacobians = far higher ceiling) —
  bonus: 16 was the intended window all along. Also fixed a real
  MixedPrecision bug en route (blanket bf16 cast clobbered u32 RNG keys →
  GRU compile crash; float-only casts now).
- **ARCHITECTURE CORRECTION**: the drill GRU is **window 60, 2 layers**
  (not "window 16, 1 layer" as earlier addenda say) — bb_defaults supply
  60/2 and the drill's `|| 16` / `|| 1` fallbacks never fire. Training,
  inference, and interp capture are all consistently 60/2.
- **INTERP PHASE 0 COMPLETE** (see `docs/planning/INTERP_ROADMAP.md`, now
  with a concepts glossary + experiment log): `Policy.build_temporal_trunk`
  + `ExPhil.Interp.{Activations,GroundTruth}` + `scripts/interp_capture.exs`.
  Smoke test: {18042, 256} activations + 15 aligned ground-truth labels
  from one probe replay in 6.9 s. Base rates quantify the known pathologies
  (own_airborne 66.8% = jump-spam; own_shielding 6.4% = shield-lock).
  Next: Phase 1 probing harness (edifice LinearProbe over the checkpoint
  zoo; does probe accuracy predict conversion %?).
- NaN-restore logic + per-epoch shuffle seed added to dagger_drill.exs
  earlier today (restore best params on NaN, up to 5x) — likely moot once
  mixed precision lands, but harmless.

## 2026-07-13 overnight addendum

- **Pool-growth night COMPLETE** (`scripts/overnight_poolgrow_20260713.sh`,
  log `logs/overnight_poolgrow_20260713.log`): 3 DAgger rounds, recipe held
  at the validated config (default LR 2e-4, --max-epochs 150), pool grown
  42 → 45 → 48 (28 originals + all 14 clean 2026-07-12 probe replays;
  each round's 3 probes fed the next round):
  - **r1 (42 replays, 811k frames): NaN@ep23, export loss 0.225 →
    3/10, 3/19, 6/14 = 27.9% pooled over 43 knockdowns** — the best
    score yet that clears the ≥30-knockdown protocol bar. New champion:
    `checkpoints/mewtwo_combo_poolgrow_r1_policy.bin`.
  - r2 (45, 869k): NaN@ep36, export 0.305 → 0/0, 0/0, 0/1 = statue
    (1 knockdown in 3 games — too undertrained to even approach).
  - r3 (48, 956k): NaN@ep17, export 0.397 → 0/0, 1/8, 0/1 = 11.1%,
    mostly passive.
- **NaN divergence is now the binding constraint, not data**: every LR
  2e-4 run NaN's (5 of 5: ep99, 42, 23, 36, 17), and the score is a
  lottery on where it lands. Export loss vs pooled score across the
  clean-probe runs: 0.19 → 25%, 0.225 → 28%, 0.305 → 0%, 0.397 → 11%
  (knockdown counts collapse above ~0.3 — policy goes passive).
  Behaviorally-alive band ≈ export loss ≲ 0.23 at this LR.
- **Pool growth verdict: helps when training survives long enough**
  (r1's 27.9% > the 28-pool's 25-26%), but rounds 2-3 were wasted on
  bad NaN rolls.
- **Gradient clipping is NOT the fix — it's already on**: dagger_drill
  passes `max_grad_norm: 0.5` and Imitation.Optimizer composes
  `clip_by_global_norm` into adamw. The NaNs happen DESPITE clipping →
  they're not step-size blowups; at 2e-4 the parameter trajectory
  reaches a region where the forward pass/loss itself overflows
  (clipping a NaN gradient leaves NaN). No 1e-4 run ever NaN'd, so LR
  modulates it.
- **LR 1.5e-4 RESULT (also NaNs, no better than 2e-4)**: r1's exact
  42-pool, only LR changed (`checkpoints/mewtwo_combo_lr15_policy.bin`):
  NaN@ep53 (later than 2e-4's 17-42), export loss 0.106 → 2/11, 2/8,
  4/19 = **21.1% pooled over 38 knockdowns**. LR tuning within
  [1e-4, 2e-4] doesn't beat the 2e-4 lottery — NaN is endemic at
  ≥1.5e-4, and everything exported in the alive band scores ~20-28%.
- **dagger_drill.exs is now NaN-ROBUST** (edited 2026-07-13 ~07:20):
  on a NaN epoch it restores the best params and CONTINUES (up to 5
  restores) instead of halting; shuffle seed now varies per epoch
  (was fixed 42 = identical batch order every epoch — also required
  so a restored retry doesn't deterministically re-diverge). Halt at
  restore #6, target-loss, or plateau, as before. Validation run in
  progress: `scripts/overnight_nanrobust_20260713.sh` — r1's 42-pool,
  default 2e-4, max 150 epochs, 3 probes →
  `checkpoints/mewtwo_combo_nanrobust_policy.bin`,
  log `logs/overnight_nanrobust_20260713.log`. Question: does training
  THROUGH the NaNs reach deeper loss and beat r1's 27.9%?
- Pool bookkeeping: r1-r3 probe replays (Game_20260713T0{15257,15819,
  20600,34021,34742,35544,44658,45439,50255}.slp) are clean real-time
  on-policy data; the r2/r3 ones carry passive behavior.

## 2026-07-12 overnight addendum

- **STATUS (18:30) — all overnight experiments DONE**: the 035205 loop
  completed (both rounds 0.0%), diagnosis + stopping-point A/B + replication
  control all finished. Verdict below; no processes running.
- **REPLICATION CONTROL RESULT (recipe robust → LR is the prime suspect)**:
  the known-good 215741_i1 recipe rebuilt exactly — 28-replay pool WITHOUT
  the salvage replay, default LR 2e-4, `--max-epochs 150`
  (`checkpoints/mewtwo_combo_replicate215741_policy.bin`, log
  `logs/overnight_replicate215741_20260712.log`):
  - Training NaN-diverged at epoch 42; salvage-exported best epoch at
    **loss 0.190** — a totally different trajectory from 215741 (plateau
    stop @250, best ep150 loss 0.039)…
  - …yet probes scored 2/10, 0/0, 1/2 = **25.0% pooled** — matching the
    known-good ~26%. The recipe reproduces the SCORE without reproducing
    the training curve, so export loss (0.039 vs 0.19!) is not the driver.
  - Cross-run pattern is now clean: **both LR 2e-4 runs pool 25–26%**
    (215741_i1, replicate); **all four LR 1e-4 runs pool ≤12%** (tl08
    12.1%, tl15 0%, 035205 i1/i2 0%). LR 1e-4 — not the salvage replay,
    not the stopping point — is the best-supported cause of the 0% rounds.
  - Caveats: 12 pooled knockdowns is below the ≥30 protocol bar, and p2
    produced ZERO knockdowns in a full-length game — knockdown rate itself
    is high-variance, so treat 25% as "consistent with 26%," not equal.
  - 2e-4 NaN'd in 2 of 3 runs (ep99, ep42): it rides the stability edge.
    The salvage exports play fine, but adding gradient clipping to
    dagger_drill.exs would stop gambling on where the NaN lands.
  - **Recommended next step for the combo ladder**: resume `dagger_loop.sh`
    with LR unset (2e-4 default), ≥3 probes per round before trusting any
    delta, and spend rounds on GROWING THE POOL (more on-policy rollouts)
    rather than knob-tuning — a full night of knob experiments moved
    nothing outside noise, while pool growth is what took 9.5% → 36.4%.
- **Stopping-point A/B RESULT (moderate-loss hypothesis REFUTED)**:
  same 29-pool + LR 1e-4 as the failed rounds, only stopping varied
  (new `--target-loss`/`--max-epochs` flags on dagger_drill.exs):
  - tl08 (stopped at loss 0.075, 131 ep): 2/9, 0/10, 2/14 = **12.1%**
  - tl15 (stopped at loss 0.142, 58 ep): 0/4, 0/8, 0/5 = **0%**, and
    knockdown rate collapsed too (undertrained ≠ the old 0.16-loss 36.4%
    policy, which had a much smaller pool)
  - Note: 215741_i1 actually exported at best-epoch loss **0.039** (ep
    150 of 250), and its loss curve descended ~2x faster than every
    LR=1e-4 run — it almost certainly trained at the script-default
    **2e-4** (dagger_loop.sh only passes --lr when LR env is set).
- **Re-probe results (probe env is HEALTHY, scoreboard n is NOISY)**:
  - `215741_i1` (the 23.1% policy) re-probed **30.8%** (4/13) — pooled
    ~26% over 39 knockdowns. Environment reproduces good scores.
  - Tonight's i2 re-probed **16.7%** (1/6) — NOT a total statue; pooled
    1/16 ≈ 6%. Still 4x worse than 215741_i1.
  - **Protocol lesson: one probe game (6-13 knockdowns) cannot rank
    policies** — the same checkpoint scored 0/10 then 1/6. Use ≥3 probes
    (≥30 knockdowns) before believing any round-over-round delta.
- **Why they failed (from the loop log)**:
  - i1: diverged (:nan) at epoch 99, best-epoch export loss=0.12229 →
    probed 0/8 knockdowns converted (28801f clean 60fps game).
  - i2: NO divergence — "Converged: loss=0.00195 after 121 epochs" →
    memorized the 493k-frame pool; probed 0/10 (17924f). Also got ~3x
    fewer knockdowns/frame than the salvage probe (26 kd in 24k frames).
  - Pattern across all combo rounds: every policy that scored well
    exported at MODERATE loss (36.4% ← loss 0.16 @ep36; 23.1% ← ~0.08
    after 250 epochs no-diverge); both 0% policies came from loss 0.122
    (post-NaN salvage) and 0.002 (overfit). Behavioral quality peaks well
    before the loss floor — the drill trainer needs earlier stopping /
    behavioral (probe-based) checkpoint selection, not loss-based.
- Pool bookkeeping: the loop appended tonight's two probe replays
  (`Game_20260712T060102.slp`, `Game_20260712T084659.slp`) to its
  internal pool. They are clean real-time games (valid DAgger on-policy
  data, NOT poison), but they carry 0%-conversion behavior.
- Previous morning status (superseded): salvage probe scored 23.1%
  (6/26 kd) on `~/Slippi/Game_20260712T034257.slp`.
- **The 4 EnetDisconnected probe deaths were OURS, not the parallel
  session's**: the --no-audio "No Audio Output" backend unthrottles Slippi
  Dolphin (~520fps games, policy acts on 1-in-8 frames, game-end hangs the
  frame stream). Fixed in 407701b (Pulse + Volume=0); full writeup GOTCHAS
  #56. The 4 fast-forwarded replays in `~/Slippi/2026-07/` (03:10–03:40)
  are POISON — never add them to a pool. Note some Dolphin configs write
  replays into monthly subfolders (`~/Slippi/2026-07/`) — flat `ls
  ~/Slippi/*.slp` misses them; use recursive find.
- **A PARALLEL CLAUDE SESSION worked on the user's Slippi Launcher stack**
  (~/.config/Slippi Launcher + SlippiOnline; patched AppImage with a
  .vanilla backup). The user's broken online play is ITS context to fix.
  Our stack (~/.local/share/slippi/netplay + temp User dirs) is fully
  isolated; only real shared resources: GC adapter (one Dolphin at a time)
  and both-online port contention.

- **THE YETI COMMITMENT**: by August, someone at Yeti Weekly plays the bot
  via Slippi Direct (Phillip-style; bot stays on the 5090). Full schedule +
  checklist: docs/planning/YETI_DEBUT.md. Netplay plumbing SHIPPED
  (--connect-code; copy_home_directory; online_delay rides --frame-delay).
  Bradley owes: a bot Slippi account. Next model: E3 with netplay delay
  (--online-robust / delay ~18) + curriculum mixing + dropout 0.3.
- **Combo ladder rung 2**: tech-chase expert (reads the dummy's tech choice;
  first REACTION drill) + MewtwoComboExpert (fair when standing, chase when
  down — a chase-only policy is a statue, the recovery-farm lesson).
  Conversion scoreboard (trace_tech_chase.exs): 9.5% baseline → 36.4% after
  one effective round. A 470k-frame round's i1 policy was mid-salvage-probe
  at session end (was 2-stocking the tech dummy when interrupted).
- **Tonight's bug harvest** (each would have hit the Yeti debut):
  - `normalize_value` stringified nil/true/false atoms → Python got
    connect_code="nil" (truthy!) and boolean strings everywhere (the
    "true"/"2" strings eval_model re-parses). Fixed in melee_port.
  - ini case-preservation created DUPLICATE Dolphin.ini keys → crash.
    Reverted: uniform lowercase (Dolphin is case-insensitive; the SIDevice
    worry was really udev — adapter rule still absent from configuration.nix).
  - --no-audio didn't silence (backend name mismatch) → Volume=0 now; loop
    probes pass --no-audio.
  - Loops self-inhibit suspend (hypridle suspends at 40min; a round
    wall-clocked 5h across suspends before the fix).
  - Orphaned Dolphins hijack successor sessions' slippi port (stuck at CSS)
    — loops pre-kill by libmelee temp-dir pattern.
- **backbone_defaults integration**: drills get per-backbone shape (mamba:
  window 60, 2 layers) — cross-architecture drill comparison unblocked but
  not yet run.
- Curriculum bank: drills/ has fox_recovery (19k) + mewtwo_fair (292k)
  .frames artifacts; export at the training delay (mismatch warns).
- NO-MIX RULE IS ABSOLUTE: any live exphil beam (training OR play) —
  a concurrent export bus-errored a play session.

## 2026-07-10 addendum

- **E2 trained & live-diagnosed** (`e2_prevact_delay2_20260710_064208_best`,
  val 2.83, healthiest press-rate profile ever, +0.7% max deviation). Live
  pathologies root-caused via SD post-mortem: neutral run-offs (prev-action
  momentum + zero recovery skill — jump-mash, B never pressed) and 2-3x
  over-shielding; hysteresis 0.45/0.3 amplifies HOLD buttons (shield-lock) —
  it's for taps, not holds. Launch saga fixes: devenv-only mix
  (EDIFICE_LOCAL_NX + cargo), nvrtc symlinks self-heal from the profile,
  systemd-inhibit for overnight runs (hypridle suspends at 40min!).
- **Curriculum mixing SHIPPED**: `scripts/export_drill_frames.exs` →
  `.frames` → `train.exs --mix-frames "drills/*.frames"` (per-segment delay
  shift, cache-key safe). 19k Fox recovery frames banked (drills/ gitignored).
- **FoxRecoveryExpert** (rules-only, :skips onstage — relabels ORDINARY
  replays). Recovery farming = play E2 vs DUMMY=cpu 9 sessions + export;
  a recovery-only drill policy is a statue that never goes offstage.
- **Approach-fair era**: distance-keyed MewtwoFairExpert (label/4 with
  opponent; 15-unit distance buckets in the fine key; range-gated rules) +
  approach-fair fixture + per-fixture expert-port auto-detection (recordings
  land on random ports). Scoreboard: 131 fairs/game, 105 landed, 65.7%
  L-cancel (benchmark 80%). Combo ladder rung 1 done; next: double-fair
  (hitstun-gated), tech-chase vs tech_random dummy, then PPO on combo reward.
- **Backbone finding**: mamba DIVERGES under the GRU drill harness (epoch 4,
  both LRs) — drill conclusions are provisionally GRU-colored; dagger_drill
  needs Config.backbone_defaults integration before cross-arch comparisons.
- **User's plan (agreed)**: scale data over knob-tuning (10x corpus next,
  fewer epochs); Mewtwo combo ladder BEFORE E3; E3 = corpus + drill mixing +
  prev-action-dropout 0.3 once the ladder validates the scaling thesis.

## 2026-07-09 addendum

- **Hysteresis decode is a 4.4x win and now the drill probe default**
  (PRESS/RELEASE env, 0.45/0.3): same policy/opponent went 43 → 189
  fairs/game. Drill policies are systematically under-confident at 0.45-0.5.
- **tech_random dummy live** (`--dummy tech_random`): Elixir-driven via
  port-routed send_controller; techs and missed-tech getups verified in
  replay. Walking-Fox rounds ran (101 fairs, 83% connect on a mover).
- **E2 is smoke-tested and GO**: flags verified end-to-end (config records
  use_prev_action/action_delay; log shows the target shift). Launch command
  in Open Threads below + add `--prev-action-dropout 0.1`. NOT yet launched.
- Incidents fixed at source: dummy menu helper stage-select cursor race
  (dummy now acts only in character select); 29GB ALSA-spam log + orphaned
  bridge pythons (watchdog thread exits on parent death; Dolphin audio
  backend = Pulse). Pre-v3 embedding caches deleted (facing-blind AND
  key-unreachable).
- Next drill rung per combo-ladder discussion: **approach-fair expert**
  (opponent-distance bucket in the fine key + range-gated rules: far → dash
  in, close → jump-fair) + user records an approach-fair fixture. Then
  double-fair (hitstun-gated), then tech-chase vs tech_random, then PPO on
  combo reward.

Compact state for resuming. Read this + MEMORY.md; avoid re-deriving.
(Morning version of this doc described the delay-2 probe plan — all of that
landed and went much further; this is the end-of-day rewrite.)

## Where we are (one paragraph)

The scripted-expert DAgger drill system went from idea to self-improving in
one day. Fox multishine: 15 → 207 JCs/game over 6 relabel-retrain rounds
(drill closed). Mewtwo SH-fair/L-cancel drill: 3 → 49 fairs/game, 90% of
fairs landing on a standing Fox dummy, L-cancels 0% → 47.7% (human benchmark
80% from the metronomic fixture). Two silent-corruption bugs found via live
drill feedback and killed: the DAgger prev-channel leak, and **GOTCHAS #55 —
facing embedded +1.0 for BOTH directions** (-1 is truthy through bool_embed);
every checkpoint trained before 2026-07-08 is facing-blind, embedding cache
bumped to v3. Port-2 dummy infrastructure landed (scripted stand/shield/
jump/walk, cpu N, and `external` = Elixir drives the port via
`send_controller(%{...port: 2})` — the self-play plumbing).

## The system (one command each)

```bash
# Drill loop: retrain -> self-terminating probe game -> score -> grow pool
EXPERT=mewtwo_fair DUMMY=stand ROLLOUTS="a.slp,b.slp" bash scripts/dagger_loop.sh 2
# One-off retrain (bootstrap mode trains on fixtures alone if no rollouts)
mix run scripts/dagger_drill.exs --expert mewtwo_fair --rollouts "..." --out p.bin
# Scoreboards
mix run scripts/trace_mewtwo_fair.exs replay.slp   # fairs, L-cancel %, aerial mix
mix run scripts/trace_multishine.exs replay.slp     # JCs, shines, empty hops
```

Experts: `lib/exphil/agents/multishine_expert.ex`, `mewtwo_fair_expert.ex`
(hierarchical physics-keyed table + prev-keyed tap recovery + edge override).
Fixtures: `test/fixtures/replays/{fox_multishine_closed,mewtwo_fair_chains,
mewtwo_shfair_only}.slp`. Roadmap: docs/planning/DRILL_INFRASTRUCTURE.md.

## Protocol lessons (cost a failed run each — do not relearn)

- Rollout relabeling: targets = expert corrections, but the prev-action
  channel must carry the policy's ACTUAL press (`:prev_controller` frame
  override) — else "(state, own-mistake) → fix" pairs don't exist.
- Recovery taps alternate against the previously-landed input, never
  action_frame parity (af freezes on repeated frames; embedding saturates at
  af/60 cap 2.0 → coin-flip labels → plateau then NaN).
- Buttons edge, not hold: 582 held-X frames = zero jumps. Decode-side
  complement: `--press-threshold/--release-threshold` hysteresis (built,
  positive under interaction; untested solo).
- Expert safety rules must OVERRIDE the table for position-dependent hazards
  (table keys exclude x → fade-back drift walked off the edge).
- L in the air outside aerial-attack states (65-70) is an AIRDODGE.
- Drill trainer: cosine decay (warmup 1 epoch, floor at ~200 epochs of
  steps) killed the recurring late-NaN; best-epoch export ships a usable
  policy anyway if it still diverges.
- NEVER run `mix` while a training beam is live (SIGSEGV via second EXLA
  client, even with nothing to recompile). Inference-only sessions coexist.
  Automated loops run mix continuously — treat the whole loop as no-mix.
- dagger_loop.sh auto-seeds from today's >500KB replays — pass ROLLOUTS
  explicitly on mixed days or it ingests other drills'/human games.

## Scoreboard history (Mewtwo fair drill, per probe game)

| round | fairs | landed | L-cancel | length | note |
|---|---|---|---|---|---|
| bootstrap | 3 | — | 0% | 4.0k | fixture-only BC |
| +rules v1 | 15 | 10 | 10% | 14.5k | user nudging |
| cosine fix | 5 | 2 | 100%* | 4.5k | first L-cancels (*n=2) |
| rules v2 | 22 | 13 | 15% | 10.2k | airdodge/edge label fixes |
| facing fix | 50 | 23 | 39% | 16.5k | GOTCHAS #55 landed |
| dummy era | 16 | 8 | 25% | 5.9k | first vs standing Fox |
| edge override | **49** | **44 (90%)** | **47.7%** | **18.9k** | current best |

## Open threads (rough priority)

1. **E2 overnight** (task #22): 500 files × 50 epochs, mamba,
   `--prev-action --action-delay 2` — now also the first big facing-sighted
   model. SMOKE-TEST the exact command with --max-files 10 first.
   Compare vs clean_200_v3_long on diagnostics + live (decode sweet spot:
   `--temperature 0.4 --deterministic-buttons`).
2. More Mewtwo rounds: L-cancel 47.7% → chase the 80% benchmark. Try
   DUMMY=walk for spacing pressure; low-threshold hysteresis A/B
   (`--press-threshold 0.45 --release-threshold 0.3`) may lift L taps.
3. Wavedash drill (user records fixture) → forces DrillExpert behaviour
   extraction (rule of three).
4. tech_random dummy in ELIXIR via the port-routed send_controller
   (dummy_mode: "external") — first reaction drill; also self-play plumbing.
5. Backlog #23: polar action space, per-head decode temps, obs filters.
6. Mewtwo ship run (MEWTWO_TRAINING_PLAN.md) after E2.

## Session ops

- Dolphin: floats native-size on Hyprland ws 9 (window rule; NOT silent —
  hidden-workspace geometry changes desync its GL framebuffer). Bridge sets
  RenderWindowWidth/Height 1240x1023 in the temp User dir's Dolphin.ini.
  Swallow exception covers Slippi/Dolphin titles.
- Play sessions self-terminate on game end (`--on-game-end stop`) and free
  the GPU — game end is detected on the in-game→menu transition
  (POSTGAME_SCORES never fires in this Slippi setup).
- `--dummy MODE` claims port 2: dummy games are non-interactive for the user.
- Replays land in ~/Slippi; recordings for fixtures: port 1 = expert player,
  quit to menu to finalize; record during a loop's TRAINING phase (or after)
  so the loop doesn't ingest the recording as its rollout.
