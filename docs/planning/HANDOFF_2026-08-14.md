# HANDOFF 2026-08-14 — start the g16 overnight run

Supersedes HANDOFF_2026-08-12 (whose EVENING SESSION addendum is the raw
log of 08-12→13). This handoff's FIRST ACTION is a single command.

## IMMEDIATE: launch g16 (Bradley has cleared you to run it)

```
bash eval_runs/0814_g16_awbc/run_g16.sh
```

- g16 = the g15 champion recipe verbatim + `--awbc` (advantage-weighted
  BC, the offline-RL lever validated 08-12→13). Train ~1.5-2h + offline
  gates; prereg + references are in the script header. NO-MIX and no
  lib/*.ex edits while it runs (3 mix invocations = multi-stage loop).
- Monitor `eval_runs/0814_g16_awbc/train.log` (tail -F + grep for
  AWBC|Converged|diverged|NaN|exported — arm a Monitor).
- Expect an `AWBC weighting:` line (~375k frames, ratio ~7) after pool
  build; b1/b2 arms' absolute numbers are NOT comparable (weaker pool,
  nx 0.12.1).
- Morning scoring: analyze_shine_source per gate dir, break forensics
  (ShineChain.summary_for_replay), verdict vs the script's prereg, THEN
  Bradley's blind local g15-vs-g16 (decider protocol) gates any promote.
  Stand numbers never crown (g6 rule).

## STATE OF THE LAB (post 08-12→14 arc)

**Crowns unchanged:** ms_g15_oppmask_full (specialist, d4 netplay/d3
local) and convC2 (generalist, delay-0 local only). g16 is the first
challenger produced by training-signal change rather than data change.

**The AWBC arc (complete, eval_runs/0813_awbc_ms/RESULTS.md):**
- Offline arms: B2 (AWBC) +17% shines/+21% chain vs B1, cleaner breaks;
  B3 (shuffled placebo) COLLAPSED to chain 2 — the gain is outcome-
  information PLACEMENT, and loss-weight noise is actively destructive.
- Deploy rung (0813_awbc_decider, blind 4-game local d3): NO inversion —
  B2 took the session's best game (chain 21); Bradley's blind call
  guessed inversion and was wrong. B2>=B1, suggestive not decisive.
- Interp: AWBC is REGIONAL (textbook RTG gradient 1.78→0.15 by
  frames-to-next-shine; 100% of top-decile weight within 120f of a
  shine) and does NOT single out margin-critical frames (1.013 vs
  0.999) — complementary to the (buried) margin lever, not redundant.
  B3's collapse = destroyed gradient smoothness, not "noise regularizer".
  For the SPECIALIST, damping no-shine-ahead frames is the objective
  (Bradley's correction, 08-13); the anchor-protection concern applies
  only to future GENERALIST composition.
- Machinery: ExPhil.Training.AdvantageWeighting + Data loss_weights
  channel + dagger_drill --awbc/--awbc-shuffle/--awbc-beta/--awbc-air-reward.

**F3 KL-distillation anchor: IMPLEMENTED, UNRUN** (DISTILL_ANCHOR_SPEC
addendum): Policy.Loss.distill_kl (tested), 6-arg distill loss variant,
per-sequence teacher-logit precompute (f16), provenance anchor mask,
dagger_drill --distill-from/--distill-weight/--distill-tau. Smoke-
verified incl. live KL flow. A1/A2/A3 prereg runs AFTER the g16 arc
(one recipe change at a time). Composes with --awbc (smoke-tested
together on 0.13.1).

**nx 0.13.1 (08-13, Bradley's call):** feat/nx-fuzz-errors merged with
origin/main. Clip-grad fix #1779 now in the training path (min/max ±60
clamp in Policy.Loss STAYS — it's the exp-overflow guard). BONUS: the
44 backbone-test failures (HGRN/S5/RWKV/GLA/xLSTM defn semantics) are
GONE. Merge is local-only, not pushed. g16 = first champion train on it.

**Also closed this arc:** GOTCHA #94 trajectory death classifier
(GameEvents + analyze_behavior, validated on dose games); task #5
windmill (rate = decomp −0.5°/frame exact, 720f/rev; phase = per-water-
phase latent, viewer auto-fits from riders — fitWindmillPhases); dose
taste test (async rung partially validated; convC2 retains; C4 had 2
hidden SDs); GOTCHA #97 Dolphin orphan spawn shim (+ reap script);
EXPHIL_HUMAN_PORT=2 QoL (no controller menu); gpu-suspend-inhibitor
fixed (missing pipe — was NEVER working) + hyprlock wedge triage in
[machine stability memory]; AWBC decider protocol scripts reusable
(eval_runs/0813_awbc_decider/run_awbc_decider.sh pattern).

## AFTER g16 (order per results)

1. Bradley's rungs for g16: blind local g15-vs-g16, then netplay d4 if
   it survives — full promote ladder, rung-0 watched (g15's own 4.28
   carries the scramble calibration caveat).
2. F3 A1/A2/A3 prereg (anchor experiment) — next lever either way:
   retention if AWBC cost any, dose-raising for the generalist if not.
3. Generalist AWBC arm (Rewards.Standard on the human corpus — the
   "exceed the demonstrator" goal; spec's original target).
4. Item-3 backlog: delay-train convC2 (netplay unlock); bookmark
   scenario manifest from the 6 crown-decider games (loop is built,
   Bradley's M-key pass still owed); coach #5 value model (shares the
   F5 critic); erickfm decision; v3-edge arm.
5. Housekeeping: push the nx merge to the fork; FIXES 9.x
   reduce_stream_indexed race; ~700 untracked eval_runs/* in git status.

## GOTCHAS THIS ARC
#97 Dolphin orphan shim (POSIX background-stdin + pgrep-self-match
sub-gotchas inside). Unnumbered: sampling weights vs LOSS weights are
different channels (ConversionSampling family vs Data loss_weights);
`not nil` raises — use `!`; upstream squash-merge changes SHAs (verify
fixes by CONTENT); stale known-issue lists rot in both directions (the
44 backbone failures).

## ADDENDUM 2026-08-16: g16/g15r verdict + the retune program

The 08-14 arc closed: nx#1785 made while-grad O(n²) (GOTCHA #98, fixed
via unroll: :static); g16 ran on the fixed stack — G1 FAIL vs prereg
(253.6 vs hold 390) but mewtwo +36%, YS record c66, rung-0 ok. g15r
(no-awbc control, 08-16) resolved attribution: **grad-fix owns the
level drop; AWBC exonerated and carries the fixed stack** (+117% fox /
+307% mewtwo; g15r card: eval_runs/0814_g15r_gradfix/RESULTS.md).
ms_g15 stays champion pending Bradley's blind local g15-vs-g16.

**Open program: RETUNE the champion recipe around correct gradients.**
Every hyperparameter was implicitly tuned under the old (wrong)
while-grad flow; fixed-stack levels (117-254/min) vs g15's 430 are not
comparable history. Plan (cheap→expensive):
0. LR finder (scripts/find_lr.exs) on champion data config, fixed
   stack — minutes; old lr was tuned to wrong-grad magnitudes.
1. Fixture-drill coarse sweep (~90 s/arm): lr x {0.5,1,2,4}x, grad-clip
   x {0.5,1,2}x, select on 2-epoch loss slope. Optimization health
   only — fixture loss never ranks deploy strength.
2. Full arms (~80 min each, batch overnight): top-2 lr x {--awbc},
   +one 90-epoch arm (g15r hit max-epochs unconverged), champion gates
   as readouts. One change at a time vs g16 baseline.
3. If chains stay under g15-era: sweep truncate_bptt / window — correct
   BPTT reaches all 60 steps where the old rule effectively didn't;
   shorter credit horizons may recover the old inductive bias
   deliberately (and cheaply).
Also owed: g15r replicate seed (n=1, non-convergence could be luck);
AWBC-accelerates-optimization observation (10x lower final loss, fewer
epochs) is real if it replicates.
