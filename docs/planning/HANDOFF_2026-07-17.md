# Session Handoff — 2026-07-17 (215 solved, newera8, r13-r14 launched)

Successor to `HANDOFF_2026-07-16.md`. Ledger of record: `INTERP_ROADMAP.md`.
Rule (memory: handoff-includes-tasklist): FULL task snapshot embedded below.

## RUNNING RIGHT NOW

**r13-r14 on `scripts/overnight_newera8.sh`** (RELAUNCHED 2026-07-18
~00:30 after reboot #2 killed the 19:00 run at r13 epoch 70/100): the
epoch-70 `_latest` snapshot was PROMOTED to the r13 checkpoint (**r13 =
70-epoch round — ledger footnote**), so the loop resumed straight into
r13 probes. 48-game pool, `NEWERA8_DROPOUT=0.6`, GRU, headless-parallel
probes (pace_hz 60 + --stateful-step, 2×3 A/B). Log:
`logs/overnight_newera8_20260718.log`; PIDs in `logs/newera8.pids`.
r14 trains the full 100 WITH the new trainer-resume protection
(`OUT.trainer.ckpt` every 10 epochs; a future interruption resumes via
`--resume` with Adam state intact — fingerprint-guarded, 7519f25).
Expect done ~06:30-07:30 on 07-18. r13 is the FIRST round with: widened
DJ scrub (af<12 + tap-jump cap), per-column debounce, dropout 0.6, x/9
cards (gate 9 shield breaks).
Watch for: `[newera8] done` / `ENVIRONMENTAL WIPEOUT` (exit 3) / the
0-conversion POLICY gate (real games, distinct from wipeout).

## THE 07-17 STORY (one paragraph)

r11-r12 completed after three false starts (launcher red herring; the real
killer was binary_to_term(:safe) rejecting the un-interned external-spec
atom in fresh VMs — edifice fbee4e4, GOTCHAS #68): conversions 10→12→39,
r12 best (6.1/8), but liftoff→DJ p50 2-4f everywhere and opponent_behind
scenario regressed 4/4→1/4. The constant-215 mystery was then SOLVED:
scripts/shield_run_stats.exs showed 89 of 92 long shield runs across
r10-r12 end in ShieldBreakFly — 215f IS the hard-shield-break ceiling,
reached via the prev-action shield-lock (roadmap's own 215→16 ablation);
ReplayStats gained observed-`breaks`, the report card gate 9 (x/9 from
r13). The lightshield fix attempt found EXI inputs express analog presses
but RELEASES LATCH (dummy shield-broke every stock) — EXI is opt-in, #66
partial. newera8 was then validated through SIX iterations that peeled a
whole stack: dead EXLA_MEMORY_FRACTION config (VRAM starvation), analog
dummy shields (now digital R), and the big one — unthrottled headless
games quantize policy inputs to ~3.6 game frames (GOTCHAS #69): headless
probes now pace 60Hz from the AsyncRunner (the runner IS the throttle
under blocking pipes) + --stateful-step for every-frame acting →
knockdowns 11-16/game (windowed parity) at ~3× throughput from
concurrency. r13 prep landed: DJ scrub af<12 + tap-jump cap (fidelity
metric excludes scrubbed frames), per-column debounce edges (X→Y
alternation hole closed), fixture list single-sourced
(ExPhil.Agents.FixtureSets). Also: Slippi bot account EXPH#288 fully set
up (slippi-bot fish fn, Direct-only HARD RULE), Yeti#489 + D20#46 replays
uploaded, #13 scan-migration eval doc'd (migrate post-r12, GPU-FFI spike
gates).

## NEW-ERA SCOREBOARD

r1 6.3% → r2 21.4 → r3 30.8 → r4 0 → r5 30 → r6 8.3 → r7 0 → r8 0 →
r9 4 conv → r10 10 conv (7/8 best) → **r11 12 conv (4.0/8)** →
**r12 39 conv (6.1/8, best; symlinked)**. Cards are x/8 through r12,
**x/9 from r13** (gate 9: shield breaks == 0). Scenario suite r12 vs r10:
behind 4/4→1/4 (REGRESSED, DJ-rush), tech_chase 0/4→1/4, deadlock 3/3,
getup 1/1, edgeguard 1/3. Headless probe scores tagged build=headless;
shield gates not comparable to windowed history until a calibration round
(PROBE_MODE=windowed) anchors the delta.

## TASK SNAPSHOT (full, 2026-07-17 ~19:00)

### Open
- **#7 LEACE live** — iterative fit or α<1; nx#1729/#1730 constraint stands.
- **#9 Yeti debut** — account DONE (EXPH#288, user.json at
  ~/.config/SlippiOnline-bot/, `slippi-bot` fish fn; HARD RULE: Direct
  only, never ranked/unranked — xpilot confirmed Direct OK). Remaining:
  user_json_path through bridge→libmelee; wire Agent.snapshot_state/
  restore_state into rollback; delay-robust model. Custom code purchase
  pending (user plays one netplay game first, human-driven).
- **#11 nx fuzz-branch remainder** — (a) drop redundant clip commit,
  (b) re-author 10 batched-LinAlg pins, (c) push to fork (USER CALL),
  (d) PR CUDA OutputBuffer fix upstream.
- **#13 scan migration** — EVAL DONE (docs/planning/EVAL_SCAN_MIGRATION_
  2026-07-17.md): migrate AFTER r-rounds; Nx.Shared.optional is GONE
  upstream (mandatory eventually); gate = one-kernel GPU-FFI spike on the
  protocol at ~5ms in-graph.
- **#15 interp phase 2** — remaining: activation-capture port (consumer =
  edifice SAETrainer.fit), minibatch fit, quarantined module rewrites.
- **#19 checkpoint ladder** — now cheap (headless parallel + pace/stateful
  learnings apply). Compose with newera8 machinery.
- **#20 round velocity** — save-best-by-gates + per-phase TIMING SHIPPED
  (newera8 emits rounds/day). Remaining: analysis habit, 4-6 arm scaling.
- **#22 fixtures** — USER records: behind-response chains + FH-no-DJ air
  control. Add to ExPhil.Agents.FixtureSets (single source) when done.
- **#23 G&W track** — gate passed; needs user's G&W drill fixtures; then
  bake-off machinery applies per-character (NEWERA8_BACKBONE hook too).
- **#27 exla NIF isolation** — GOTCHAS #67; unblocks watcher co-tenancy;
  investigate per-consumer cache or post-build copy hook + env parity.
- **WS4 edifice renewal + bake-off** — adoption P4 (SAETrainer)/P6
  (Profile :step) + quarantined-module triage; bake-off shortlist
  mamba/min_gru/griffin vs GRU on the 48-game pool via NEWERA8_BACKBONE,
  one run each; compare loss/gates/scenarios/TIMING/step-latency.
- **teacher_coverage.exs prints 0 frames** (noticed 07-17 evening,
  pre-existing?) — diagnose before trusting before/after scrub audits.

### Completed 07-17 (context for resume)
- 215 mystery SOLVED + gate 9 (07ba8af); shield_run_stats.exs new.
- WS5 partial: EXI opt-in only, analog-release latch documented (#66).
- newera8 shipped + validated ×6 (937b952); GOTCHAS #69.
- r13 interventions (8534d86): scrub af<12 + tap-jump cap, per-column
  debounce, FixtureSets dedup, fidelity metric honesty.
- r11-r12 complete; manifest atom crash fixed (edifice fbee4e4, #68);
  resume guard (eb793b0); Stateful step path + rollback API (b9a15b7);
  manifest format (932a7e2 + 700b63c); Mamba.Common → edifice delegation
  (9021713); #66/#67/#68/#69 GOTCHAS; Yeti#489/D20#46 uploads; bot
  account EXPH#288.

## OPERATIONAL RULES (delta since 07-16 — all in GOTCHAS #64-#69)

1. **#67 is the big one**: mix compile RELINKS the shared exla NIF every
   invocation — NO concurrent mix across exphil/exphil-play/edifice/nx
   while any exla BEAM lives. newera8 encodes this (--no-compile
   fan-outs). Watcher co-tenancy interlocked off until #27.
2. **#69**: unthrottled headless + policy-driven = mistimed inputs. Probes
   need pace_hz 60 + stateful-step. Frame-locked REPLAY (scenario suite)
   is exempt (runs unthrottled fine).
3. **#68**: never trust one-VM round-trip tests where atom interning is
   involved.
4. **#66**: EXI inputs opt-in ONLY (analog release latches). Pipe mode
   drops analog (shim workarounds); TechRandom now shields digitally.
5. Probe replays: pick by SIZE (auto-menu boots stub second games at
   real-time pacing).
6. Slippi bot: EXPH#288 NEVER queues ranked/unranked. Direct only.

## FILE MAP DELTA (since 07-16)

**exphil scripts**: overnight_newera8.sh (CURRENT loop),
shield_run_stats.exs, scenario_suite.exs + scan_scenarios.exs +
scenarios/manifest.json + scenarios/replays/ (#18 suite),
parallel_probes.sh, watch_latest.sh (interlocked), report_card.exs (thin
printer). newera7 = historical.
**exphil lib**: interp/report_card.ex (gate math module, x/9),
interp/replay_stats.ex (breaks), eval/scenario_{scan,score}.ex,
agents/fixture_sets.ex, agents/agent.ex (stateful step + rollback API +
per-column debounce), bridge/async_runner.ex (pace_hz),
training/checkpoint.ex + training.ex (manifest dual-format loader),
networks/mamba/common.ex (thin edifice delegation).
**config**: runtime.exs EXLA_MEMORY_FRACTION now actually works (client
merge).
**edifice main**: external Specs, metadata :safe fallback, LEACE pins,
SSM.Common overrides (4051c02, fbee4e4).
**Environment**: ExiAI headless Dolphin at ~/.local/share/slippi/exi-ai/
(dolphin-emu-headless wrapper, GOTCHAS #64); bot Slippi User dir at
~/.config/SlippiOnline-bot/.
**Checkpoints**: r11/r12 + _latest; best → r12 (r13-r14 will update).
**Drive**: Tournaments/Yeti Weekly #489 + D20 Melee #46 uploaded.
