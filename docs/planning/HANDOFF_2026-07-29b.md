# HANDOFF 2026-07-29b — consolidated end-of-day: DAgger campaign state, mechanism found, mains runbook. Supersedes HANDOFF_2026-07-29.md (+ its addendum).

Everything on `main` through `0386052`. Laptop (Manjaro), written at
~15% battery after a car-day of offline science. Full evidence:
[LATENCY_ARCHITECTURE.md](LATENCY_ARCHITECTURE.md) (headless trust
campaign + DAgger sections at the end).

## READ FIRST — rules that save sessions

1. **The eval opponent is part of the distribution — NEVER compare
   across dummies.** Champion numbers are STAND-dummy; cpu-1 scores
   ~70/min on the same policy. This artifact burned three prereg
   failures; a human watching the screen caught it, not the numbers.
2. **Harness quality metric: offset-calibration sharpness**
   (`probe_cycle_margins.exs` cal spread; thirds-sliced variant
   `probe_cal_drift.exs`). Sharp peak >=0.75 = trustworthy timing.
   Peak POSITION is harness-specific — compare concentration only.
3. Pre-register predictions. This week: ~4 confirmed, 10 failed;
   the failures produced the findings.
4. Standing rules: >=3 runs; uptime check before blocks (but regime
   was NOT the 70/min cause — the dummy was); Output on STDERR; the
   mix hazard; metronome phenotype (~45-50/min chains 1).
5. Timing-critical evals need mains power — battery/car = garbage.

## State of play

**Champion: `checkpoints/ms_open_z.bin`** — 165-186 self/min, chains
15-30 (all-time records; chain 30 with --blocking-input), stand-dummy
windowed async, through the polling harness.

**DAgger through the delayed bridge (the campaign, rounds 1-4):**
- R1 (d1-fixture teacher + 3 rollouts): **DELAY-PREFERENCE INVERSION**
  — better AT delay-1 (35-75, c2-3) than delay-0 (11-44, c1-2). First
  learned policy to prefer the delayed harness; rerec never did this.
- R2 (12 rollouts): rate recovered (120-123/min) but chains 2-4.
  Forensics: all breaks unforced; post-break reflector dwell.
- R3 (+ transition-weight 2.0, prev-action-dropout 0.6): converged 12
  epochs again (convergence speed is NOT diagnostic — tiny state
  space). **NEVER EVALED** (no mains all day).
- R4 (margin fattening: --resume, target 3e-4, cap 22): stopped
  cleanly at epoch-10 snapshot.
  `ms_d1_dagger3_policy.bin` = R3 export (14:02);
  `.trainer.ckpt` = R4 epoch 10 (19:14) — resume with the same
  command or eval R3 as-is.

**Mechanism (found, offline):** encode-horizon probes
(`interp_d1_timing.exs`) show NO trunk-level timing shift; boundary
maps (`probe_cycle_boundary.exs`) show dagger3's decision boundary IS
shifted early (X from af0-1 vs champion's textbook af3; jumpsquat-B
at af1-2 = lands first airborne frame after +1) with THIN margins
(0.3-0.9 vs champion 2-5). Right place, barely held = chains 2-4.
Head/trunk swap hybrids were DEGENERATE — independently-trained
trunks have unaligned feature bases; head-swap is only valid within a
fine-tune lineage (hybrids deleted; lesson recorded).

**Harness (all landed + validated):** polling console default on
(LRAS 11/11 instant quits; --console-timeout 0 auto-gates LRAS off);
--blocking-input free windowed; --local-delay N delay queue for D>=2
(4a15dbb, offline gate green, live gate pending); headless recipe
--pace-hz 0 (chains 10-16, cal 0.67 — stationary sub-frame race, NOT
drift); --emulation-speed 0 fast menus (12s/run overhead, farms only);
mainline beta AppImage at
`~/.config/Slippi Launcher/netplay-beta/` (never AppImageLauncher-
integrate; headless replays need --replay-dir, glob monthly subdir).

## MAINS RUNBOOK (next session, in order)

1. Optionally resume R4 (~1h GPU-less) or skip. Then OFFLINE:
   `probe_cycle_boundary.exs` on the current dagger3 bin — did
   margins fatten at the shifted position?
2. Eval dagger3 at `--frame-delay 1` (3 runs, stand, windowed).
   Prereg: chains >=8, rate >=100, reflector-dwell shrinks.
3. --local-delay 1 live gate: cal shifts exactly one offset, stays
   sharp.
4. Task-5 build discriminator: MAINLINE build WINDOWED — cal ~0.78 =
   build innocent; ~0.67 = the build costs concentration.
5. Head-level test (replaces hybrids): --freeze-trunk flag in
   dagger_drill (small addition), fine-tune champion HEADS on d1
   labels; delay preference reproducing = adaptation is
   head-implementable and cheap.

## Strategic frame (end-of-day rumination, 2026-07-29)

Platform: unambiguous success (instruments, discipline, records).
Bot: not yet — records are one behavior, one character, idle
opponent. De-risked, not delivered. Watch for: model-organism
lock-in (schedule a Mewtwo drill / second behavior to test transfer);
falsification culture decay; compute mismatch (margin rounds and PPO
belong on the 5090/cloud, not the laptop). The BC->DAgger->PPO ladder
is visible: fatten d1 margins -> delay-distribution DAgger (queue for
D>=2 exists) -> delay-robust base -> PPO on headless farms -> transfer
off multishine. Next impossible-but-measurable candidates: chains
>=10 at delay-2, or first Mewtwo fair-chain through the delayed
bridge.

## Open problems (carried)

1. Shift pathology (six seeds, five falsified hypotheses) — wanted
   instrument: full-cycle offline closed-loop rollout.
2. Thin-margin problem (this campaign's version of the same story?).
3. Stateful carried-state divergence (O(1) win banked, opt-in stands).
4. Headless sub-frame race (task 5, discriminator queued).
5. Pin-bump spike (instant_match_restart + runtime speed; revalidate
   MenuHelper/EXI per requirements.txt warning).
6. 27d leftovers: Mewtwo transfer, ledge drill, DAgger/PPO on GPU.

## Machine facts

- eval_runs/ untracked-by-design. Today's dirs: dagger_d1_round{0,1,2}*,
  stand_windowed(+blocking), stand_headless*(4 variants), polling_ab,
  cal_drift.log, cycle_boundary.log, interp_d1_timing.log.
- Task lists do NOT survive session restarts — this doc is the state.
- r4 resume command lives in train_r4_margins.log's header
  (dagger_drill --resume --target-loss 3.0e-4 --max-epochs 22, same
  rollout list as r2/r3).
