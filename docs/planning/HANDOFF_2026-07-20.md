# HANDOFF 2026-07-20 (evening)

Supersedes HANDOFF_2026-07-17.md. Read this first, then
BRAINSTORM_2026-07-18.md (idea ledger + tiered program + steering/screen
results), INTERP_ROADMAP.md (experiment ledger). GOTCHAS #56-#70 are the
operational law — #67 (no mix while a BEAM lives), #69 (pace headless),
#70 (Slippi launcher beta fix) newest.

## RUNNING STATE (check before touching anything)

- **mamba_full run IN FLIGHT**: `overnight_newera8.sh`, NEWERA8_TAG=mamba_full,
  backbone mamba, 100 epochs, dropout 0.6, 56-replay pool (54 + 2 human demo
  games via new NEWERA8_EXTRA_ROLLOUTS env). At epoch 80 @ 17:39; training
  ends ~19:15, then windowed probe fan-out (PROBE_STATEFUL=0 — mamba has no
  Stateful impl). Log: `logs/overnight_newera8_mamba_full_20260720.log`,
  launcher log `logs/mamba_full_launcher2.log`. Was PAUSED once for a demo
  (epoch-50 ckpt) and RESUMED cleanly — the trainer-resume infra works.
- **NO mix commands until it exits** (#67). A watcher (this session) fires on
  launcher exit; the post-run queue below runs then.
- Machine also hosts: nothing else. Demo BEAMs all reaped.

## POST-RUN QUEUE (parked until the run exits — in order)

1. `mix compile` + `mix test --stale` — verifies ALL uncommitted work below
   (gate 10, StyleCard, punish expert, FixtureSets). Expect possible
   action-ID fix in punish expert (@opp_aerial_landing 70..74 is from
   memory; tests pin it).
2. Verify the 5 new fixtures parse + content sanity (tilt counts, offstage
   stretches, G&W move coverage) — identify_replays.exs already confirmed
   characters/durations.
3. Calibrate gate 10 (armed approaches/min) + StyleCard PROVISIONAL
   thresholds against `corpus/human_blewf/` (7 human games) and r14 probes.
4. Fix overnight_newera8.sh "/9" label strings -> "/10" (couldn't edit a
   RUNNING bash script).
5. Score mamba_full probes; compare vs screen (8.1/9 @30ep) and r14 (6.0/9).
6. Commit everything (see inventory), push.

## UNCOMMITTED WORK INVENTORY (all written, none compiled — GPU was busy)

- `lib/exphil/interp/replay_stats.ex`: approach_stats/2 (approach = P1
  closes 40->20 units covering >=60% of the closure, not in hitstun;
  "armed" = attack within 15f of arrival), percentile_or_nil/2, approach in
  summarize/1.
- `lib/exphil/interp/report_card.ex`: GATE 10 "armed approaches/min >= 1.0
  (PROVISIONAL)". Only activates with position data; legacy callers stay
  x/9. Cards from real probes become x/10 after compile.
- `lib/exphil/interp/style_card.ex` (NEW): 6 humanlike gates — reaction p50
  floor (anti-superhuman), press-interval CV (anti-metronome), APM band, DI
  presence %, recovery attempt rate, SD cap. Char packs stubbed for
  mewtwo/zelda/gnw/fox/ICs. ALL thresholds PROVISIONAL pending #30.
- `lib/exphil/agents/mewtwo_punish_expert.ex` (NEW): rules-only whiff-punish
  branch — opponent in aerial-landing-lag/special-landing/whiffed-grab/
  ground-attack-endlag + within 60 units -> dash in; <=18 units -> dtilt
  (stand) / dash-grab (dash). THE r15 fix for "never opens anyone up".
- `lib/exphil/agents/mewtwo_combo_expert.ex`: cascade now chase > punish >
  fair. Drill script needs no changes — r15 gets the branch automatically.
- `lib/exphil/agents/fixture_sets.ex`: mewtwo_neutral_extras() (dtilt/uptilt
  dense + ground_neutral_v2 — NOT in combo list until tilt frames have an
  expert home; see note in file), mewtwo_recovery(), gnw_starter().
- Tests: style_card_test.exs (NEW), mewtwo_punish_expert_test.exs (NEW),
  replay_stats/report_card tests extended (fixture integration now expects
  10 gates).
- 5 new fixtures in test/fixtures/replays/ (recorded 2026-07-20 on mainline
  beta.19): mewtwo_dtilt_uptilt_dense, mewtwo_ground_neutral_v2,
  mewtwo_recovery_mix, gnw_neutral_dense, gnw_movement_ledge.
- `corpus/human_blewf/` (gitignored): 5 Mewtwo + 2 GnW unranked games
  (Bradley's own, 2026-07-20) + 1 unsorted (Sheik vs Falco — WHICH SIDE WAS
  BRADLEY? ask). Seed of the #30 calibration corpus.
- `scripts/identify_replays.exs` (NEW): char/duration identifier.
- `scripts/overnight_newera8.sh`: NEWERA8_EXTRA_ROLLOUTS env added.
- GOTCHAS #70, BRAINSTORM demo-observation ledger entries, .gitignore corpus/.

## SCOREBOARD (results since 07-19)

- **Steering A/B (commit 74e27bc): CAUSAL.** r14 + steer α=1.0 = first 9/9
  card, 0 shield breaks, p95 215->32. α=0.5 WORSE than baseline (all-or-
  nothing axis) -> LEACE α<1 dead, r15 lever = probe-as-regularizer.
  Steering is a live mitigation: `--steer-vector checkpoints/r14_shield_steer.bin
  --steer-alpha 1.0`.
- **Bake-off screens @30ep (commit a6bbc89)**: mamba 8.1/9 + 5 conv
  (loss 0.072 — card winner, first-ever stable mamba, on edifice real conv);
  gru2x 6.0/9 + 8 conv (NaN@30 restored — wants f32/lower-LR rerun);
  min_gru 6.6/9 + 1 conv (fast but unstable). All >= r13's 100-epoch card.
- **DJ-recovery check: INCONCLUSIVE** — α=1.0 never went offstage in 2 games
  (0 situations; baseline 6, DJ'd 2/6 both returned). Offstage-forcing
  recheck ledgered; gates α=1.0 as a *default* (fine as opt-in).
- **Human demo observations** (2 sessions vs Bradley): shields under
  pressure + acts OOS (steering works live) BUT occasional shield breaks vs
  human pressure (attractor reachable under real pressure — supports
  training-time fix) and ZERO opening creation -> punish expert branch
  written (above).
- **Slippi launcher beta fixed** (GOTCHA #70): APPIMAGE_EXTRACT_AND_RUN=1 in
  `slippi` fish fn + local desktop entry; `slippi-fix` verifies. Root cause
  was FUSE-in-sandbox, NOT missing libs; shims/file-swaps do NOT work.

## r15 RECIPE (assemble after mamba_full verdict)

In: frame-domain debounce (fca25b0), new fixtures (wired), punish expert
branch (cascade), conversion-weighted pool sampling (TO BUILD),
probe-as-regularizer (TO BUILD — the steering-decided lever). Backbone:
GRU-256 default; if mamba_full @100ep holds its card lead AND converts,
consider mamba line + prioritize Edifice.Stateful mamba impl (needed for
stateful probes + 60fps netplay path).

## FIVE-CHAR PROGRAM (medium-term, Bradley's direction 2026-07-20)

Chars: mewtwo, zelda, gnw, fox, icies. Program = per-char instantiation of
the mewtwo pipeline (human-corpus BC -> drill fixtures + scripted experts ->
DAgger vs dummies -> per-char gate packs -> 5x5 cross-play ladder).
Order: GnW (fixtures recorded!) -> Fox (data-rich) -> Zelda -> ICs.
Key principles from Bradley: option-mix gates must be char-specific
QUALITY priors vs human baselines, NOT raw entropy (mewtwo fair-OOS-heavy
is correct); recovery/edgeguard attempt detection needs per-char enumerable
taxonomies; capability catalog (FH multi-aerials, autocancel windows,
Mewtwo DJC — note debounce tension) doubles as corpus curation filter.
Human corpus (#30) is the keystone: calibrates every PROVISIONAL threshold
AND supplies neutral vocabulary BC. Bradley's own unranked sessions feed it
continuously (corpus/human_blewf/).

## TASK LIST SNAPSHOT (rebuild CLI list from here — it does NOT survive restarts)

| # | status | task |
|---|--------|------|
| #5 | DONE | Parallel probes (3x throughput) |
| #7 | pending | LEACE live — α<1 DEAD per steering A/B; only iterative-fit variant remains relevant |
| #8 | DONE | Script hygiene + GOTCHAS batch |
| #9 | pending | Yeti debut (August) — netplay path: user_json wiring -> Direct smoke -> rollback -> delay A/B -> match flow |
| #11 | pending | nx fuzz-branch remainder |
| #13 | pending | Nx.block/CustomCall migration eval |
| #15 | pending | Interp remediation phase 2 (edifice) |
| #16 | DONE | Mid-training play/eval remaining pieces |
| #18 | DONE | Scenario savestate suite |
| #19 | pending | Checkpoint ladder [user priority] — generalizes to 5x5 char ladder (#33) |
| #20 | in_progress | Round velocity meta-metric (timing lands per-run; analysis piece open) |
| #22 | ~DONE | Fixture recording — 2026-07-20 session recorded 5 (tilts, neutral v2, recovery, 2x GnW) |
| #23 | pending | G&W parallel track — UNBLOCKED (gnw_starter fixtures exist) |
| #24-26 | DONE | Edifice/Nx usage evals, test coverage audit |
| #27 | pending | Per-consumer exla NIF isolation (proper per-checkout cache, upstreamable) |
| #28 | pending-verify | StyleCard batch 1 (WRITTEN, uncompiled) |
| #29 | pending | Char option-quality priors (KL-to-human, not entropy) |
| #30 | pending | Human calibration corpus (seeded: corpus/human_blewf/) |
| #31 | pending | Char capability catalog + corpus curation filters |
| #32 | pending | Recovery/edgeguard taxonomies per char |
| #33 | pending | Five-char training program (regimens/corpora/round-robin) |
| #34 | pending-verify | Whiff-punish expert branch (WRITTEN, uncompiled — in combo cascade) |
| WS4 | pending | Edifice adoption renewal + architecture bake-off (screens DONE; full mamba run = tonight) |

Also open (not numbered): upstream drafts (UPSTREAM_DRAFTS_2026-07-17.md)
await Bradley's review — DO NOT POST; probe migration to mainline build;
one idle_deadlock scenario prefix re-curation; "openings created/min" stat
(10-line ReplayStats addition); tech-chase/punish shared reach model;
custom connect code purchase (Bradley — netplay game requirement now met).

## FILE MAP (today's hot files)

- Steering: lib/exphil/interp/steering.ex, scripts/extract_steer_vector.exs,
  checkpoints/r14_shield_steer.bin
- Cards: lib/exphil/interp/{replay_stats,report_card,style_card}.ex
- Experts: lib/exphil/agents/mewtwo_{punish,combo,fair,tech_chase}_expert.ex
- Fixtures: lib/exphil/agents/fixture_sets.ex, test/fixtures/replays/
- Loop: scripts/overnight_newera8.sh (+ NEWERA8_TAG/MAX_EPOCHS/DRILL_FLAGS/
  DROPOUT/EXTRA_ROLLOUTS envs), scripts/dagger_drill.exs (--resume,
  --hidden-size)
- Corpus: corpus/human_blewf/{mewtwo,gnw,unsorted}/
- Slippi: ~/.local/bin/slippi-fix, ~/.config/fish/functions/slippi.fish,
  ~/.local/share/applications/slippi-launcher.desktop, GOTCHAS #70
