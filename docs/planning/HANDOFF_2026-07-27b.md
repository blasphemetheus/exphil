# HANDOFF 2026-07-27b — perturbation verdict, scheduled sampling, harness health. Supersedes HANDOFF_2026-07-27.md.

Everything is on `main` (through `6bff753` + the commits after it; `git log
--oneline -8` first). Laptop session, Manjaro, no GPU. Full evidence trail:
[EXPOSURE_BIAS.md](EXPOSURE_BIAS.md) — items 0c, 5, 6 were all updated today.
This doc is the resume point; it is written so a fresh context can act
without re-deriving anything.

## READ FIRST — rules that save you from wasted sessions

1. **≥3 live RUNS per policy** (variance is in runs, not seeds — item 0a).
   Use `scripts/eval_live_protocol.sh` — it IS the protocol: refuses to run
   beside heavy jobs, logs loadavg, prints staleness, batch-scores.
2. **Never run live evals with training or the test suite active.** Under
   load the harness COLLAPSES (~1 fps, stuck at stage select). The protocol
   script guards this; don't FORCE=1 unless measuring the collapse itself.
3. **~16% stale sends is this laptop's STEADY state** during live runs
   (uniform 15.7–17.2% across loadavg 2–5). Comparisons are valid because
   it is uniform; treat outliers as measurement failures and discard.
4. **`ExPhil.Training.Output` writes to STDERR.** `2>/dev/null` on a mix
   script eats ALL its output silently (this bit the protocol script's own
   first run).
5. **The `mix` hazard is live here**: any `mix` invocation recompiles
   changed `.ex` files and kills a running training with SIGSEGV. Editing
   a `.sh` while a bash instance of it runs is also unsafe (bash reads
   lazily) — it survived once only because the file fits one read buffer.

## Where the work stands

### Perturbation harness: RUN AND MEASURED — underperforms synth on chains

First-ever run of the built-but-never-run harness (item 5, now closed):
120s teacher recording with `PERTURB_EVERY=180 PERTURB_FRAMES=6` (mode
random), 25.1% of rollout frames relabeled, trained to loss 0.00154
(epoch 56, `checkpoints/ms_perturb.bin`, epoch snapshots at
`ms_perturb_e{25..56}.bin`).

| condition | self/min | max chain |
|---|---|---|
| synth (n=11 runs) | ~45 (26.9–58.4) | **3–7** |
| perturb, loss 0.0015 (n=3) | 32.4 · 44.1 · 38.8 | 2 · 2 · 1 |
| perturb e40, loss 0.09 (n=3) | 33.9 · 44.9 · 47.7 | 1 · 1 · 2 |

Rate equivalent, chains do NOT overlap — LOW side. Same at both loss
bands, so the "statue at the loss floor" lore did NOT reproduce (it came
from the conversion metric on r-series drills — don't port it blindly).
`chain_break_forensics.exs`: ~half of breaks UNFORCED, 156–262 frames/run
pooled in the GOTCHAS #81 trap (361 af>2) at both bands. Suspects
(untested): fixture dilution (20% cycle data), death/respawn frames in
corrections (random mode side-B'd Fox offstage ~3×), ~40 recovery episodes
spread thin. Gentler `PERTURB_MODE=stick|release` exist and are untried.

### Scheduled sampling: IMPLEMENTED, TESTED, first policy trained

`ExPhil.Training.ScheduledSampling` (commit `25ddb5e`): swaps the last
window position's prev-action slice for the model's OWN decoded prediction
on P of samples; decode pinned to the live path; slice found empirically
(`Attribution.prev_action_dim_range/1`); ss_fn lives on the trainer
struct, never in config. Wired into BOTH `dagger_drill.exs` and the main
config.ex pipeline as `--scheduled-sampling P --ss-ramp N`. 6/6 new tests
green (`test/exphil/training/scheduled_sampling_test.exs`), full stale
suite green. Loss under SS is a harder objective — never compare loss
curves across the flag.

First SS policy: `checkpoints/ms_synth_ss.bin` = synth + prev-action +
`--scheduled-sampling 0.5` via `train_multishine_policy.exs` (4,063
frames, loss 0.001, 29 epochs). NOTE: plain synth (`ms_synth_a`) has NO
prev-action, so the comparison set is synth / synth+PA (`ms_pa_*`: 32.3 &
44.3, chains 6 & 4) / synth+PA+SS.

### Evals in flight at handoff time (score these first!)

Three 3-run blocks were launched sequentially; results live under
`/tmp/claude-1006/-home-dori-git-melee-exphil/9475edd6-32b8-4f78-b205-f799b6f4d0b2/scratchpad/`:

- `synth_a_idle/` — ms_synth_a vs `--dummy stand` (IDLE opponent):
  the clean sustain-capability ceiling. ~half of chain breaks vs the CPU
  involve the opponent, so all historical chain numbers are opponent-capped.
- `ss_cpu/` — ms_synth_ss vs level-1 CPU (baseline-comparable).
- `ss_idle/` — ms_synth_ss vs idle opponent.

If the run logs are gone, re-run: each block is
`scripts/eval_live_protocol.sh checkpoints/<policy>.bin <outdir> --runs 3
--seconds 60 [--dummy stand]`. Score any replays with
`mix run scripts/analyze_shine_source.exs <slps>` and
`mix run scripts/chain_break_forensics.exs <slps>`.

## Tools built today (all committed)

- `scripts/eval_live_protocol.sh` — the measurement protocol as one command.
- `scripts/chain_break_forensics.exs` — classifies every chain break
  opponent-hit / under-pressure / UNFORCED + post-break state pooling +
  #81-trap counter.
- AsyncRunner staleness telemetry — `Staleness: N/frames (x%), longest
  stale run M` in play_dolphin_async final stats.
- `PERTURB_MODE=random|stick|release` in record_multishine.exs.
- `--scheduled-sampling/--ss-ramp` in dagger_drill, train_multishine_policy,
  and the main config.ex path (4-step checklist done, TRAINING.md rows).

## Queue, by leverage

1. **Score the three in-flight eval blocks** and write results into
   EXPOSURE_BIAS.md (item 6 for SS, item 0c for the idle-opponent ceiling).
2. **Synth + perturb COMBINED aggregate** — complementary by construction,
   never trained together. Cheapest route: `dagger_drill.exs` with the
   perturb rollout + a synth-augmented fixture, or extend
   train_multishine_policy to also ingest relabeled rollouts.
3. **Re-record perturbation with `PERTURB_MODE=stick` and `release`** —
   kills the death-frame suspect; then retrain and protocol-eval.
4. **Longer idle-opponent runs** (`--seconds 180+`) — with the opponent
   removed AND 3× the window, the 3–7 chain ceiling gets its first honest
   measurement.
5. **DAgger to convergence** — GPU only; resume
   `checkpoints/multishine_dagger1_policy.bin.trainer.ckpt`.
6. **Apply synthesis to Mewtwo/Fox-recovery drills** — same latent asset
   (live recovery rules never trained on), first step toward the Mewtwo bot.

## Open problems

1. Chains 1–7 vs the teacher's 791 — every intervention so far moves rate,
   barely moves sustain. If idle-opponent + longer runs still cap at ~7,
   suspect the architecture/window can't represent the 9-frame periodicity,
   or the ~16% staleness floor breaks it mechanically (1 in 6 frames stale
   ≈ a slip every ~2 cycles — do the math against measured chain lengths).
2. Training/eval delay mismatch (item 0c-4): fixed `--action-delay` vs
   variable live pipeline phase + staleness. Now measurable, unmeasured.
3. Measurement cost: a 3-run block ≈ 8 min wall. Idle-opponent runs may be
   less noisy (opponent RNG removed) — if so, fewer runs needed; check
   spread of `synth_a_idle` vs the CPU baselines before designing more
   experiments.
