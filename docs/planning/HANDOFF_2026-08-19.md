# HANDOFF 2026-08-19 — GPU queue first, planning fills the gaps

**Resume protocol: launch GPU task #1 DETACHED immediately, then do
planning-queue work while it runs. On each completion: score vs the
script's prereg header, launch the next GPU task, return to planning.**
Detached launch pattern (session-tied background tasks get reaped on
this machine — twice observed 08-14; setsid survives):

```bash
cd ~/git/exphil && setsid nohup bash -c \
  'exec devenv shell -- bash <RUN_SCRIPT>' > <OUT_DIR>/pipeline.log 2>&1 < /dev/null &
```

NO-MIX + no lib edits while any run is live (GOTCHA list; also do not
edit sibling deps mid-run — a libmelee_ex edit killed sweep arms 3-5 on
08-17). Progress: grep `epoch|GATE|DONE` in the run dir's logs.

## ADDENDUM (evening session, agent-run): GPU #1+#2 done, #3 live

- **g17 (GPU #1): NEGATIVE — lr is not the recovery lever**
  (eval_runs/0819_g17_lr/RESULTS.md). 4e-4: fox 151.8 c88 / mewtwo
  129.8; 8e-4: 103.9 c5 / 94.9. Quality monotone in LOWER lr; fixture
  speed signal INVERTED at full scale (stability transferred, quality
  didn't). Note: 4e-4 beat g16 on mewtwo (+18%) — hot lr flattens the
  specialist profile.
- **g15r2 (GPU #2): the big one** (eval_runs/0819_g15r2/RESULTS.md).
  (a) RC answered: training NOT deterministic — seed knob not needed,
  planning item CLOSED. (b) Run COLLAPSED at epoch 51 (loss 0.0105 →
  2.5e-8 in ONE epoch; exported ckpt dead: 12.0 fox / 0.0 mewtwo).
  (c) **Epoch-50 rescue gate: fox 362.5/min c353 — best fixed-stack
  fox EVER, no awbc** (mewtwo 0.0, pure specialist). VERDICT R1 PARTLY
  OVERTURNED: the "+117% fox" AWBC claim is dead (no-awbc variance is
  enormous: 116.8 vs 362.5 vs collapse); what survives = AWBC's
  generalization (mewtwo 109.8 vs 27/0/0) and stability (awbc arms
  never collapse, no-awbc 1-for-2). (d) DECISION FOR BRADLEY:
  replicates-first (n>=3 of champion+awbc to get the distribution) vs
  more single-run knob arms — single runs at this scale are draws, not
  verdicts. (e) FIX candidate: drill convergence exit trusts a single
  epoch's loss — require 2 consecutive sub-target epochs or flag a
  >100x one-epoch drop; would have exported ep50 and saved the arm.
- **g18 (GPU #3, 90ep @ 2e-4 + awbc): attempt 1 COLLAPSED at epoch 10**
  (same one-epoch loss→~0 signature as g15r2; export dead 0.0/min; no
  rescue — the every-10-epoch snapshot landed ON the collapse epoch).
  So the collapse mode is recipe-INDEPENDENT (awbc on/off), 2-of-3
  tonight, all lr 2e-4, never seen before tonight; ROOT CAUSE OPEN.
  Forensics (scratchpad inspect/diff scripts): no NaN/Inf/zeros in dead
  exports; the collapse epoch moved weights like a NORMAL epoch (mean
  |Δw| ~6% of weight scale) while reporting loss 2.5e-8 — reported
  loss and optimization state diverged. **GOTCHA #99 + collapse guard
  SHIPPED** (dagger_drill: loss<1e-5 or >100x one-epoch drop → treated
  like NaN: never best/converged, restore-best-and-continue).
  **Attempt 2 (with guard): fox 419.4/min c415 — best fixed-stack G1
  EVER, essentially old-grad-champion level, landed at EPOCH 13**
  (genuine loss 2.06e-4; trajectory violently unstable en route, ep7
  spiked to 1.01); mewtwo 99.9 c2 (marginal fail of the >=100 hold,
  chains regressed). Full card + the night's verdict:
  eval_runs/0819_g18_ep90/RESULTS.md. **Checkpoint ms_g18_ep90.bin =
  play-rung candidate.** The 90-epoch read was NOT obtained (exited
  ep13) — the G1 pass is a DRAW, not a lever effect.
- **NIGHT VERDICT: run-to-run VARIANCE dominates every knob** (dead,
  12, 103.9, 151.8, 362.5, 419.4 across near-identical recipes in one
  night). Knob-science should pause: replicates-first (n>=3) or find
  the variance source. Retroactively taints single-run g16-era reads.
- **Stack-delta finding: nx moved a7497612 -> f843aa1a** (10 fuzz
  commits; defn/expr donatable, exla defn output-donation, dispatch
  changes) — the ONLY delta vs the calm 08-14/16 runs (exphil lib
  mtimes all pre-g15r; edifice unchanged; no donatable use in
  exphil/edifice, so mechanism unconfirmed). **nx-pin dynamics probe
  LAUNCHED 23:37** (eval_runs/0819_nxpin_probe/, g18 recipe @
  a7497612, prereg reads N1 implicate / N2 exonerate in script
  header). **nx is now CHECKED OUT at a7497612 (detached HEAD)** —
  restore to `integration` f843aa1a for fuzz work, keep the pin for
  training until the probe verdict.
- **PROBE VERDICT (01:33): N1 — nx bump IMPLICATED**
  (eval_runs/0819_nxpin_probe/RESULTS.md). At a7497612 the same recipe
  trained CALM (smooth 0.135→0.0011 over 83 epochs, max swing ~4x, no
  collapse) but WEAK (fox 87.9 c3, mewtwo 83.9 c6). Era-consistent:
  every a7497612 run calm, every f843aa1a run wild. Second finding:
  the wild stack's best draws (362.5, 419.4) beat everything the calm
  stack ever produced — HEAD acts like a higher-temperature optimizer;
  "HEAD + collapse guard + best-of-N" won the night empirically.
  OPEN: which of the 10 commits, and is it a correctness bug (bisect
  plan + strategic fork in the probe RESULTS — Bradley's call).
- **08-20 MORNING RETRACTION + REAL MECHANISM
  (eval_runs/0820_collapse_forensics/RESULTS.md — read this one):**
  forensics found NO stack difference (single-step replay, batch-loss
  distributions, 2-epoch cumulative damage all identical; the wild/calm
  signature was the last-batch epoch-readout lottery). #1814 likely
  innocent; N1 retracted. Real discovery: **chain skill = transient
  peaks; continued training destroys them (362.5 → ~87 in 2 epochs on
  BOTH stacks); loss anti-correlates with behavior; outcomes =
  stopping-point luck.** Recipe fix queued: per-epoch snapshots +
  post-run behavioral gate-sweep, export argmax-gate. New drill tools:
  --init-from, --collapse-forensics (scene traps), --replay-scene,
  per-batch loss series. nx back on integration.
- **g19 GATE-SWEEP ARM (08-20 13:04, eval_runs/0820_g19_gatesweep/):
  the recipe fix VALIDATED — `ms_g19_ep4.bin` gates 437.4/min c438 x3,
  NEW ALL-TIME STAND RECORD (old: g15 430.4 old-grads), from EPOCH 4;
  the same run's final epoch gates 122.8.** First-ever full behavioral
  trajectory (60 gate points): wide peak ep3-8, spiky revisits to
  ep42, terminal decay. Loss anti-correlates with skill after ep8.
  New standing recipe: `--snapshot-all` + `scripts/gate_sweep.sh
  <prefix> <outdir> --confirm`. All future knob questions = "does it
  raise/widen the peak". Caveats: stand/FD only; ep4 mewtwo 93.9 c1;
  YS/scramble/human unknown; g6 rule — no crown from this.
- **Interp instrument audit over the 60 snapshots (08-20 evening,
  eval_runs/0820_g19_gatesweep/INTERP_FINDINGS.md):** decay is 100%
  closed-loop — teacher-forced fixture agreement FLAT (~99.8%) from
  ep2 to ep60 while gates swing 437↔82; the ep8→9 cliff is a smaller
  weight step than a within-peak step. Verdict: no static instrument
  ranks checkpoints; gate-sweep stays the selector. Mechanism
  direction found: monotone jc-X-fatten / aerial-shine-B-flip seesaw
  on peak states (new `scripts/margin_trajectory.exs`), era-level not
  epoch-predictive. CycleSim confirmed unable to score queue-embed
  lineages — its repair is the only candidate offline gate predictor
  (build when sweep time bottlenecks, not before).
- Planning queue: ALL four writable items done — F3 A1/A2/A3 prereg v2
  (DISTILL_ANCHOR_SPEC.md + eval_runs/0819_f3_distill/run_f3_arms.sh,
  GPU #4 unblocked), Phase-0 adapter spec
  (TECH_DRILL_PHASE0_ADAPTER.md: fixture-first, Situations-owned
  triggers, 3-class granularity menu), erickfm decision
  (ERICKFM_RANKED_DECISION.md: ACCEPT staged, pilot-gated), rung-
  composition theory + ping plan (LATENCY_ARCHITECTURE.md 08-19
  section; old-grad caveat on all composition facts). --seed item
  closed (not needed). libmelee_ex: uncommitted work inspected — it's
  the next-round routines already implemented (+tests); its pre-commit
  contract (dolphin suites) couldn't run next to the GPU queue; commit
  when GPU drains.

## Where we are (2 lines)

Fixed-grad stack landed (GOTCHA #98: nx#1785 made while-grad O(n²);
`unroll: :static` fix; ~82 s/epoch). g16 (champion+awbc) is the best
fixed-stack checkpoint (fox 253.6 c203 / mewtwo 109.8 c14); AWBC
exonerated and carries the stack (g15r control: 116.8/27.0). ms_g15
(old grads) remains champion; the fixed stack scores lower because the
recipe was tuned for wrong-grad flow → RETUNE program is live.

## GPU QUEUE (in order; each script has its prereg in the header)

1. **g17 LR arms** (~3 h total, 2 arms × train+2 gates):
   `bash eval_runs/0819_g17_lr/run_g17.sh`
   Fixture sweep said lr 8e-4 / 4e-4 >> baseline 2e-4 (0817_lr_sweep_fixture).
   Read: >=304/min fox with mewtwo >=100 → new base recipe.
2. **g15r replicate** (~80 min): rerun
   `bash eval_runs/0814_g15r_gradfix/run_g15r.sh` with OUT dir changed
   to `eval_runs/0819_g15r2` (copy script, edit OUT + --out name).
   Purpose: the "+117%/+307% AWBC" claims are n=1. CAVEAT: dagger_drill
   has NO --seed flag — if the rerun reproduces g15r's losses
   byte-identically, training is deterministic and the replicate needs
   a seed knob added first (small config.ex change, do while GPU idle).
3. **90-epoch arm** (only if g17 reads negative): champion+awbc at the
   best g17 lr with `--max-epochs 90` — g15r hit max-epochs unconverged,
   so epochs is the next knob after lr.
4. **F3 A1/A2/A3 distill arms** (implemented 08-13, unrun; prereg owed
   BEFORE launch — write it as planning work): KL-anchor composition on
   the retuned base. One recipe change at a time: only after #1/#3
   settle the base.
5. **Generalist AWBC arm** (Rewards.Standard on the human corpus,
   always --replay-dir GOTCHA #84): unblocked extra signal channels by
   libmelee_ex GameEvents (L-cancel events, conversions) — needs the
   Phase-0 adapter spec (planning queue) before wiring new channels.

## PLANNING QUEUE (no GPU; do while runs are in flight)

- **Tech-drill curriculum Phase-0 adapter spec** — the decision doc for
  wrapping Melee.Tech routines as drill experts:
  `docs/planning/TECH_DRILL_CURRICULUM.md` (written 08-19; Phase 0
  section lists the open design questions). Curriculum arms themselves
  WAIT for the retune verdict (attribution discipline).
- **F3 A1/A2/A3 prereg** (unblocks GPU #4).
- **erickfm dataset decision doc** (850k rank-labeled, pre-3.18:
  accept/reject analysis).
- **--seed flag for dagger_drill** if GPU #2 proves deterministic
  (config.ex 4-step flag checklist in CLAUDE.md).
- **Rung-composition theory + real-friend ping plan**
  (LATENCY_ARCHITECTURE.md, delay campaign leftovers).
- **libmelee_ex next tech round** (its docs/HANDOFF.md: edge-cancelled
  aerials, no-impact land, V-cancel, Thunder Jacket, walljump, wobbling
  — V-cancel and edge-cancels feed curriculum Phases 1-2). NOTE: has an
  uncommitted `lib/melee/tech.ex` modification — commit/stash first.

## Human-required (cannot be done by the agent; schedule with Bradley)

- **PLAYED AND WON 08-21 (eval_runs/0820_g19_decider/RESULTS.md):
  3-arm blind decider — ms_g19_ep4 chained 96 & 163 vs Bradley
  (all-time human-session records; g15 c3-4; g20_ep13 c6-9, its
  mewtwo-invariance did not transfer). ep4 = PRODUCTION CANDIDATE;
  full crown rides the netplay rung (d4 --delay-id-override 3) on the
  next remote Direct session. ms_g15 champion-of-record until then.**
- Original entry (superseded): blind local decider ms_g19_ep4 vs ms_g15
  (ep4: d3 437.4 c438 record / d4-id3 388.4 c389 / YS no-collapse /
  mewtwo 93.9 c1 / d2 excluded — full card in
  eval_runs/0820_g19_gatesweep/RESULTS.md). The old g15-vs-g16
  matchup is superseded; g16 keeps only the mewtwo-generalization
  interest.
- Shield-loop / camping flaw measurement (human sessions only).

## Key artifacts of this arc

- `eval_runs/0814_g16_awbc/RESULTS.md` — g16 gates + grad-fix confound.
- `eval_runs/0814_g15r_gradfix/RESULTS.md` — attribution verdict (R1).
- `eval_runs/0817_lr_sweep_fixture/` — LR sweep logs.
- GOTCHA #98 — the nx regression + bisect-bench pattern.
- `docs/planning/TECH_DRILL_CURRICULUM.md` — the drill program.
