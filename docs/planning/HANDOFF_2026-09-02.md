# HANDOFF 2026-09-02 — corpus corruption FOUND+FIXED (E1b/E1c), v1.3 primary PASSED (approach inverted), F4 = state-visitation, drill designed+mined, mode-of-N live-dead

**Supersedes HANDOFF_2026-08-31** (read it second; its §5 gotchas hold).
Written ~00:30 by Claude Fable 5. Bradley was present all evening: played
three live looks (v1.2-ARrefit, v1.3-ARrefit, mode-of-N), directed the
stop-and-fix, the bug-class hunt, the drill design, and the knob rerun.

## 1. State of the world (verified 00:25)

- **NOTHING RUNNING.** All units done: v13-portfix (arms), v13-part2
  (refit+evals), v13-knobs (F4+ladder), drill-mine4. No beams, no
  monitors.
- Branch `main`, HEAD **`8a9a50d`**, **32 commits ahead of origin**
  (Bradley's habit: do not push unasked). Working tree CLEAN.
- **DISK — URGENT-ish (GOTCHA #106)**: root 43 GB free (93%); **/data
  100% FULL (6.5 GB free)** after the contaminated-cache quarantine
  moves. The NEXT cache-building launch on / may fit; anything moved to
  /data will NOT. Bradley owns the deletions:
  `/data/exphil/old_port1_embedding_cache` (206 GB, 08-31 move) and
  `/data/exphil/old_arhead_cache_e1c` (47 GB, 09-01 move — provably
  corrupted, safe to delete).
- New deployables: `checkpoints/fox_gen_v1.3_{AR,IND}_20260901_*_policy.bin`
  (joint arms, val 5.3242 / 5.7643) and
  `fox_gen_v1.3_{ARrefit,INDrefit}_policy.bin` (clean-capture refits —
  **v1.3_ARrefit is the current live-look candidate**). New artifacts:
  `checkpoints/dynamics_fox_v11AR.bin` (saved G3b dynamics model),
  `eval_runs/0901_critic_v13/critic.nx` (clean critic).
  Production `ms_g19_ep4.bin` untouched.
- Memory resume point: `~/.claude/projects/-home-blewf-git-exphil/memory/project_status_2026-08-31.md`
  (appended through "09-02 ~00:20 — night close 2").

## 2. The FULL task list

1. **Drill episode driver — NEXT BUILD, NOT STARTED.** Implement
   DRILL_HITCONFIRM.md Route B on the existing dagger/dummy-external
   plumbing: setup phase (drive dummy to position/percent) → scripted
   opener (up-throw) on the bot port → HANDOFF to policy at the victim
   hitstun/thrown rising edge → 240 f continuation → score vs the mined
   expert cell. **Read docs/planning/DRILL_INFRASTRUCTURE.md protocol
   lessons FIRST (07-08 rule).** Drill 1 reference (banked,
   eval_runs/0901_drill_table/RESULTS.md): uthrow/0-19%/mid = 3.9 hits,
   87% ≥3, 27.0 dmg. Drill 2 candidate: dair/0-19%/mid (n=181, 5.3
   hits, 92%). Then: 20-episode smoke → 500-episode bank → AWBC retrain
   arm → drill re-measure → live-look transfer check.
2. **Critic-selector LIVE decode — NOT STARTED.** The only
   untested-live knob (mode-of-N failed live 09-01; oracle is
   unplayable by definition). Wire `--critic PATH` into the agent: at
   decision, K coherent samples via the kn sampler, score with the
   bilinear S from `eval_runs/0901_critic_v13/critic.nx` (needs
   CriticFeatures.raw_features computed live), argmax. Then the
   pre-registered LIVE gate: frozen-input ≤ 0.20, 7/8 to cap, F1
   airdodge-in-danger, F2 commitment rate, F3 approach_delta.
   Expectations lowered by mode-of-N's live death; in-dist margin +6.2
   cleared the ≥5 bar (first ever), fresh +2.1 under.
3. **v1.3 recovery ambiguity — OPEN.** died% 44.4/43.8 vs v1.2's 28.6
   is UNDER the 2.5x deaths floor at n≈36 episodes (formally
   unresolved); airdodge-first-route back up (31/47% vs 18.4).
   Resolve with EITHER a bigger-n arhead_score run (RUNS=24) OR more
   Bradley live-look games. Blocks nothing but blocks belief in "v1.3
   strictly ≥ v1.2".
4. **AWBC curation round — READY, QUEUED** (one knob at a time; it is
   only port-correct AFTER the E1b remap). Recipe: v1.1/v1.3 COMMON
   block + `--awbc --awbc-reward standard`; consider an
   `--awbc-shuffle` control arm (B3 placebo). Motivations: F4's uniform
   punish-class deficit + F3c ("approach data exists, BC loses it").
5. **Clean re-baselines — QUEUED** (all pre-fix instrument numbers are
   E1c-diluted ~44%): (a) coincidence re-probe of 8a heads + v1.2
   refits on the fixed instrument (the L_cond 2.39/2.67 references are
   dirty-scale; v1.3's 1.49 is clean-scale — not comparable); (b)
   retrain the dynamics model on clean embeds (current one trained on
   swapped-perspective data, still passed action-sensitivity 68.7%);
   (c) F4 re-run with the throw-aware hit detector (drill_table_mine's
   fix — F4's depth counter missed throw links; flat verdict
   qualitative-safe, depths shifted); (d) mark diluted RESULTS files.
6. **INFRA_HARDENING §7 implementation — QUEUED**
   (docs/planning/INFRA_HARDENING.md is the spec): ExPhil.Eval.Corpus
   library (resolve_subject/load_frames/pick_files/embeds/rtg — 30+
   copy-pasted blocks), test/support/replay_fixtures.ex (promote the
   port-2 synthetic builder from character_port_test.exs),
   corpus_doctor.exs semantic smoke, char enum-space fix in
   interp_blind_audit.exs (external ids used on internal fields),
   use_prev_action default normalization, mirror×stage_internals
   composition fix, delay_id folded into cache keys.
7. **Warn-once review — SMALL.** The new Peppi remap warning fired
   inside v13-part2 (a sub-class-A site that passes explicit ports
   correctly but is unmarked — likely CriticFeatures.extract_replay's
   own parse or train_ar_head's label parse). Find it, either add
   remap+port-1 usage or a `:ports_explicit` acknowledgment option so
   the warning stays meaningful.
8. **Disk decisions — BRADLEY**: delete or keep
   `/data/exphil/old_arhead_cache_e1c` (47 GB, provably corrupted) and
   `/data/exphil/old_port1_embedding_cache` (206 GB, stale
   generations). /data is FULL; root at 43 GB.
9. **Status board republish — OWED** (last 08-30; missing the entire
   08-31 + 09-01 arc).
10. **Blind B1-vs-B2 pair — OWED, BRADLEY** (carried from 08-31).
11. **Transfer decider (D7) — OWED, BRADLEY** (carried; arms in
    `~/decider2_archive`, never `ls -l` it; prereg
    `eval_runs/0824_transfer_decider/PREREG.md`).
12. **`--head` default flip — STILL GATED** (pre-registered): v1.3
    live look was favorable-not-crowning; recovery ambiguity (task 3)
    is part of the gate.
13. Parked (carried): G5 style separability; V-rollout VETO-selection
    idea (plan-c campaign was NEGATIVE for master-matching but
    catastrophe-veto on 10-frame rollouts stays plausible); improoover
    bot-in-.gci plumbing (COACH_ROADMAP #6) as the drill fidelity
    upgrade; interp leftovers.

## 3. What I was doing when this was written

Session wrap after the drill-table mining landed (task 1's step-1
complete). Nothing in flight. **Exact next action: task 1** — read
`docs/planning/DRILL_INFRASTRUCTURE.md` protocol lessons, then build the
episode driver.

## 4. Findings and decisions this session (2026-09-01→02; git messages carry the rest)

Chronological, each with a RESULTS file:

1. **E1b — THE CORPUS CORRUPTION** (GOTCHA #107, commit 06abf6d):
   `--select-character-port` resolved the imitated port but the
   streaming loader never passed opponent_port — on the 3,429
   non-port-1 fox files (~44%) the real opponent was DROPPED (distance
   0) and the embedding saw an ALL-ZERO SELF with the imitated fox in
   the OPPONENT slot. v1.1/v1.2 trained on this. Found by auditing
   AWBC's port handling; verified empirically on a real port-2 file.
   Fix: opponent_port_for + `remap_ports: true` (players normalized to
   %{1=>subject, 2=>opponent}) + `:r2` cache-key generation.
2. **E1c — the same class in the INSTRUMENTS**: capture/embed paths
   hardcode own_port=1, so every capture-based instrument embedded a
   SWAPPED perspective on non-p1 files (critic extracts, coincidence
   probes, the 8a/v1.2 refits, dynamics model, Leg S — all diluted
   ~44%, not void; raw-frame scorecards + livelook-based probes clean).
   Fix: choke-point remap in capture_replay + `:r2`/`_r2` cache/store
   keys + **own_port stamp + embedding RAISES on mismatch** (the
   class is now a loud crash, not a silent swap) + warn-once.
3. **Bradley's stop-and-fix directive** (now standing policy): on
   finding a corruption mid-training, STOP the run at a stage seam,
   fix, relaunch — do not preserve dirty stages for comparability. The
   v13 chain was stopped at the IND-end seam; both arms were clean
   (trained through the fixed loader); only refit+evals reran.
4. **v1.3 PRIMARY PASSED** (0486a10, `eval_runs/0901_v13_portfix/VERDICT.md`):
   approach_delta INVERTED from v1.2's −0.20..−0.26 (retreat, all
   ranges) to the EXPERT's signature (~0 close, +0.04/+0.11/+0.19 at
   60/90/130; IND +0.22). The corruption WAS the root cause of the
   retreat/positional-blindness cluster. AR≈IND → trunk property.
5. **F3c corpus check — Bradley's pushback CONFIRMED, my retreat-zone
   story RETRACTED** (`eval_runs/0901_neutral_range/`): expert Fox
   APPROACHES at range (toward 0.095→0.609 with distance; dash-dance
   band at 40-70; lasers a seasoning). Also matched his "no SH laser"
   read (bot aerial-laser below expert, grounded-laser above =
   standing spam).
6. **Plan (c) V-rollout selector — offline NEGATIVE, diagnosis clean**
   (`eval_runs/0901_vrollout/RESULTS.md`, 3 designs recorded): dynamics
   model exonerated (new action-sensitivity diagnostic; G3b's gate
   CANNOT detect action-blindness — keep the diagnostic); neither
   RTG-V nor discriminative g on imagined-window-through-trunk features
   separates candidates (shuffled-state control equal). Synthesis with
   the ladder's own control: offline master-matching is mostly not
   selectable from short horizons.
7. **F4 combo-depth probe** (`eval_runs/0901_combo_depth/RESULTS.md`),
   answering Bradley's "how do we show it the deeper layers":
   **depth-collapse REJECTED** — match FLAT across hits-into-punish
   (pass@16 30.3→27.4) while live punishes cap at 1-2 hits ⇒
   STATE-VISITATION is the gap (drills primary); uniform punish-class
   deficit vs neutral (4.2 vs 7.8 pass@1; hardness confound noted) ⇒
   AWBC secondary. (Caveat: throw-link gap, task 5c.)
8. **Clean knob ladder** (`eval_runs/0901_critic_v13/RESULTS.md`):
   whole ladder ~doubled on the clean stack (sampling 5.2 / mode 18.3
   / selector 24.5 / oracle 32.8 in-dist); **selector−mode +6.2 clears
   the ≥5 bar in-dist for the FIRST time** (fresh +2.1 under);
   state-conditional component +8.7 (was +3.4).
9. **Mode-of-N live = argmax-class collapse** (Bradley, N=16 and 8:
   edge-rolls, offstage suicides, "boring"). VERIFIED the live path
   uses the coherent AR sampler (sample_autoregressive_from_features
   honors mode_of_n) — the failure is genuine, L9's third confirmation.
10. **Convention-drift bug-class audit** (Bradley's "like this but not
    the same", INFRA_HARDENING §6b): 4 more instances — stick
    coordinate spaces (raw 0..1 vs embedded −1..1; bit
    dynamics_action_sensitivity the day it was written — FIXED, verdict
    survived a fortiori at 68.7% of drift), char enum spaces (external
    CSS 2=Fox vs internal 1=Fox; interp_blind_audit affected, queued),
    use_prev_action fallback drift (latent), mirror×stage_internals
    composition gap (latent). Coverage answer recorded: boundary
    fixtures + typed constructors + invariant stamps, NOT per-module
    unit coverage.
11. **v1.3 arms**: AR val 5.3242 / IND 5.7643 — the AR conditioning
    gap reproduces on the clean corpus. L_cond after clean refit: 1.49
    (vs joint 1.09; old 2.39/2.67 references are dirty-instrument
    scale — do not compare cross-instrument).
12. **Drill-table mining detector lessons** (3 iterations, in
    `eval_runs/0901_drill_table/RESULTS.md`): opener needs a 10-f IASA
    lookback; thrown victims have hitstun_frames_left == 0 (count
    THROWN 239-243 + captured 223-232 as hit states); thrower ids are
    fthrow 219 / bthrow 220 / uthrow 221 / dthrow 222 (were off by
    two); grab openers resolve FORWARD to the throw (≤120 f).

## 5. Gotchas / operational law (new this session; older in HANDOFF_2026-08-31 §5 and GOTCHAS.md)

- **GOTCHA #107** (full text in GOTCHAS.md): port-convention drift =
  corpus-corruption factory. Any feature changing WHICH player is
  imitated must be audited against every port-1 hardcode; corpus
  changes are smoked by INSPECTING one non-default file's frames —
  "loss descending" is NEVER corpus validation.
- **The embedding now RAISES** on a stamped own_port mismatch
  (game.ex get_players_ego). Live-safe (agent embeds with the detected
  port). If a new instrument crashes with "port-convention violation",
  it is parsing with explicit ports but embedding un-remapped frames —
  add `remap_ports: true`, don't remove the assert.
- **Cache-key generations**: training non-p1 keys `{path, port, :r2}`;
  capture non-p1 keys `{path, port, :r2}`; train_ar_head store dirs
  `_r2` suffix. Pre-fix entries under old keys are CORRUPTED for
  non-p1 files — never resurrect them by "simplifying" keys.
- **Stick spaces**: raw controller 0..1 (0.5 neutral) vs
  embed_continuous −1..1 (0 neutral). Hand-building 13-dim action
  vectors in raw space silently mislabels every action.
- **Char id spaces**: metadata/CSS external (Fox=2) vs in-frame
  internal (Fox=1). Resolve-by-character uses metadata (external);
  anything comparing `player.character` needs internal ids.
- **Mode-seeking decodes die live** (L9, third confirmation) — offline
  match-rate ladders NEVER justify a decode without the live gate.
- Melee frame-data for detectors: thrown 239-243, captured 223-232,
  throws 219-222, grabs 212-218, blaster ground 341-344 / air 345-348,
  dash/run/turn {18,20,21}, jumpsquat/rise {24,25,26}.
- /data is FULL — do not move anything else there without a deletion
  first (task 8).

## 6. Resume instructions

```bash
cd ~/git/exphil
git log --oneline -3          # expect 8a9a50d at HEAD, clean tree
systemctl --user list-units 'v13*' 'drill*' --no-pager   # expect none
df -h / /data                 # root ~43G, /data FULL (task 8)

# Task 1 (next build): read the protocol lessons FIRST, then the design:
#   docs/planning/DRILL_INFRASTRUCTURE.md   (07-08 protocol lessons)
#   docs/planning/DRILL_HITCONFIRM.md       (the design to implement)
#   eval_runs/0901_drill_table/RESULTS.md   (Drill 1 reference numbers)

# Bradley's live-look command (v1.3 candidate; also resolves task 3):
# devenv shell -- mix run scripts/play_dolphin_async.exs \
#   --policy checkpoints/fox_gen_v1.3_ARrefit_policy.bin \
#   --dolphin "$HOME/.config/Slippi Launcher/netplay-beta-nixos" --iso "$HOME/isos/melee.iso" \
#   --character fox --stage final_destination --frame-delay 0 \
#   --temperature 0.5 --buttons-temperature 0.5 --port 1 --opponent-port 2 --human-port 2 \
#   --replay-dir eval_runs/0902_livelook_v13ar --verbose

# Bigger-n score run for task 3 (when GPU free):
#   RUNS=24 OUT=eval_runs/0902_v13_score24 \
#     AR_POLICY=checkpoints/fox_gen_v1.3_ARrefit_policy.bin \
#     IND_POLICY=checkpoints/fox_gen_v1.3_INDrefit_policy.bin \
#     systemd-run --user --unit=v13-score24 --working-directory=$PWD --collect \
#     -p StandardOutput=append:$PWD/logs/v13_score24.log \
#     -p StandardError=append:$PWD/logs/v13_score24.log \
#     devenv shell -- bash scripts/arhead_score.sh
```

Memory: `project_status_2026-08-31.md` is current through "09-02 ~00:20
night close 2" — a fresh `project_status_2026-09-02.md` should replace
it as the MEMORY.md resume pointer next session.
`docs/planning/EVAL_DIRECTIONS.md` remains the living tracker (F3/F3b/
F3c/F4 all logged); `docs/planning/INFRA_HARDENING.md` is the hardening
spec; `eval_runs/0901_v13_portfix/VERDICT.md` holds the primary verdict.
