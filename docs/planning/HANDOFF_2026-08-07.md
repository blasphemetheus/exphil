# HANDOFF 2026-08-07 — resume point

Supersedes HANDOFF_2026-08-06.md. Session: the owed g12-vs-g10b human
netplay A/B ran, plus a local showcase attempt that produced the
sharpest fight-state-gap data yet.

## HEADLINES

1. **Margin lever A/B: NEGATIVE.** g12 did not out-chain g10b at d4
   (strict chains: g10b 2/3 vs g12 1-2 per game — see caveats below).
   `--margin-weight` does NOT fold into the champion recipe. **g10b
   stays production.** The delay-gap theory moves off decision margins
   and onto observation-side candidates (rollback jitter, delay
   variance — HANDOFF_2026-08-06 next-moves 1 is CLOSED, its successor
   is delay-break attribution on today's corpus).

2. **NEW HUMAN-SESSION RECORD: strict chain 58** (`ms_g10b_human`,
   local d3, vs a still human Fox — eval_runs/0807_video_fox_test).
   Beats the 08-05 record of 22/23. The stack (native bridge, nametag,
   memory-card) is fully validated by this number.

3. **Opponent-CHARACTER is the dominant fight-state variable.** Same
   hour, same recipe: vs still Fox chain 58; vs still MEWTWO chain 1
   (103 shines at 81.6/min — it tries constantly, completes zero
   cycles). Motion is second-order (moving Fox also breaks chains).
   Every historical human record was a Fox MIRROR; today was the first
   non-Fox human opponent ever. Mechanism is the absorber shape:
   off-manifold opponent features silence the jump-cancel link.

4. **Metric correction (GOTCHA #89):** loose onset-gap "chains"
   (chain_break_forensics) counted sloppy air-shine loops as chains.
   All numbers in this handoff are STRICT (ShineChain). The 08-05
   records were strict-recounted and stand (local 22/23, netplay 3-4).
   The loose-counter netplay numbers quoted this morning (max 4/5)
   correspond to strict 2/3.

## A/B DETAIL + CAVEATS

- 2 games/arm, both d4, same session, DBTD#411 as the human. Latency
  sharp at lag 6 both arms (native bridge matches the crowned regime).
- **Caveat: the human played MEWTWO** — internally consistent between
  arms, but not comparable to the 08-05 Fox-mirror "chain 4" record.
  Given headline 3, a Fox-mirror re-run would be needed before any
  margin-lever burial is FINAL; at minimum the lever showed nothing
  while g10b showed its usual netplay band.

## FIXES SHIPPED TODAY (all in-tree)

- **Black-screen netplay launch** — mainline rejects SlotA/SlotB=255
  (GOTCHA #87); `memory_card: true` in play_dolphin_async.
- **JIT warmup now compiles the real sampling path** (`warmup_sample`
  in agent.ex — Policy.sample fused graphs + both prev_buttons
  variants). The first-game freeze that forced a human Dolphin restart
  has not recurred since.
- **`--nametag EXPH`** end-to-end (CLI flag → MeleePort → MenuHelper
  nametag flow, memory_card :folder auto-enabled). Two crashes fixed on
  the way (GOTCHA #88: `connect_code ""` hijack).
- **DOLPHIN.md**: "Local showcase vs a human" + "Direct netplay"
  recipes documented (the exact commands, qtrace check, teardown,
  wf-recorder notes).
- NO-MIX recurrence (third kill): analysis mix-run SIGBUS'd a live play
  session. Memory index corrected: **no mix beside ANY live beam**;
  replay analysis strictly after teardown.

## NEXT MOVES

1. **Mixed-opponent drills** — the new #1. The drill corpus is
   Fox-mirror-only; today proved character generalization is the wall
   (58 vs 1). Cheapest first: opponent-dropout/jitter augmentation in
   dagger_drill (mask/perturb opponent features so the cycle can't
   anchor on them — the g6 lesson generalized); then Mewtwo/varied
   dummy characters in the drill recorder; then moving-opponent
   scripts. Promotion gains a moving-opponent rung.
2. **Delay-break attribution** on today's 6 netplay games + 08-05
   corpus (the margin-negative result points at observation-side
   causes: rollback correction jitter, delay variance).
3. Retrained-trigger audit round (unchanged from 08-06, still owed a
   replant on a policy-read variable).
4. Video: Fox-mirror showcase footage works today (chain 58 regime);
   vs-Mewtwo footage is the fight-state-gap demo until #1 lands.

## SESSION ARTIFACTS

eval_runs/0807_ab_g10b_d4 + 0807_ab_g12_d4 (netplay A/B, +6 human
games for the corpus) · 0807_video_fox_test (the chain-58 game) ·
0807_video_mewtwo_still (the chain-1 discriminator) ·
0807_video_g10b_local (first vs-Mewtwo local games).

## SHUTDOWN NOTE (end of session, ~12:00)

The g13 pipeline was stopped mid-run for a machine shutdown — GATE 0
(g10b stand-Mewtwo baseline) had not finished; NO g13 checkpoint exists
yet. Nothing is lost: the runner is idempotent.

**Resume next session, in order:**
1. `nohup devenv shell -- bash eval_runs/0807_oppmix_g13.sh > eval_runs/0807_oppmix_g13.log 2>&1 &`
   (reruns GATE 0 from scratch; ~1h total; NO-MIX for the duration).
2. After its verdict: run-1 full-game IL —
   `mix run scripts/train.exs --backbone gru --replays replays/fox_il_v1 --train-character fox --name fox_il_v1`
   Corpus is STAGED: replays/fox_il_v1 = 4,465 hardlinked games (all
   HF Fox games minus 453 unfrozen-Stadium; census + filter scripts
   were session-scratch — the corpus dir is the durable artifact).
   Direction decision (this session): full-game IL on real games FIRST,
   RL second via F5's offline path anchored on the IL base; RL-direct-
   on-multishine deprioritized.
3. Bradley owes: G&W game history from the other computer/backup
   (future fine-tune arm), Greg's Zelda + local mewtwo corpus queued
   behind run 1.

Uncommitted work in tree: warmup fix (agent.ex), nametag plumbing
(melee_port.ex, cli.ex, play_dolphin_async.exs), memory_card fix,
opp-randomize lever (dagger_drill.exs), g13 runner, DOLPHIN.md +
GOTCHAS #87-89 + this handoff + roadmap rows. Commit next session.

## ADDENDUM 2026-08-08 (overnight): g13/g13b opponent-redress rounds

Full row in INTERP_ROADMAP_V2. Short version: the lever MOVES
opponent-conditioning hard (g13 inverted the policy: Mewtwo c22 / Fox
c1!), but coherent redress teaches character-CONDITIONING, not
invariance — g13b (confound fixed) keeps the core (Fox c382) with only
modest transfer (Mewtwo c3) and rung-0 still elevated (2.64). Neither
promotes; g10b stays production. Priority ordering confirmed with
Bradley: LOCAL multishine robustness first, delay work parked behind it.

Next lever candidates (pick one): many-small-random-lists redress;
true per-frame opponent MASKING (the original dropout idea — likely the
right shape, teaches ignoring); recorded varied-dummy drills.
New standing instrument: the stand-MEWTWO headless gate (reproduces the
human finding at 60s cost) — add to promote_check.
Also fixed overnight: eval_live_protocol now passes --replay-dir
(native-bridge regression, NO FRESH REPLAY otherwise) + headless evals
need the exi-ai build + ISO env (see runner comments).
fox_il_v1 IL run still staged, unlaunched.

## ADDENDUM 2026-08-08 #2: g14 mask round — mechanism found

g14 (`--opp-scramble-frames 12000`, per-frame char/percent/x scramble):
**stand-Fox 429.4 c430 = ALL-TIME STAND RECORD, rung-0 dependence
1.0822 = lowest ever** — the dropout shape does exactly what redress
couldn't. But stand-Mewtwo 8/min c2: no transfer, because the scramble
covers only 3 fields and the policy learned to ignore exactly those;
the UNMASKED opponent features (height/action/animation) still gate the
cycle. Next move = **v4 full-block masking** (scramble every opponent
field, or zero the opponent embedding slice — cleaner). g14 checkpoint:
`ms_g14_oppmask.bin` — NOT promoted (fails the Mewtwo rung) but holds
the stand record and the dependence floor; keep. g10b remains
production. All rounds logged in INTERP_ROADMAP_V2.

## ADDENDUM 2026-08-08 #3: g15 + human session — the arc pays out

g15 (`--opp-scramble-frames 12000`, FULL opponent block): stand-fox
430.4 c426 (record rate), stand-mewtwo 80.9 c2, YS 13/8/19, rung-0
**4.28 — above the g6 red flag** (full scramble taught scramble-
DETECTION; in-support subtle noise (g14) taught ignoring at 1.08 — mask
plausibility is the design variable). BUT the human session tells a
better story: **vs Bradley's live Mewtwo, g15 chained 12-13 strict**
(games in eval_runs/0808_video_g15_local) where g10b managed 1 vs a
STILL Mewtwo. The stand-mewtwo gate (c2) underestimates live play —
spawn-distance dummy is a harsher proximity case; calibration note for
the rung. g15 = candidate pending full promote_check + rung-0 caveat
(the probe may not calibrate for scramble-trained policies). Also
shipped: `--postgame-delay N` (autostart grace after game end, any exit
route incl. quit-outs; CLI + DOLPHIN.md). g10b remains production until
g15 passes a full protocol.

## ADDENDUM 2026-08-08 #4: g15 full promote_check — offline case complete

All rungs recorded (eval_runs/0808_promote_g15.log): rung-0 4.28
(scramble-probe caveat open) / stand d3 430.4 c426 x3 BIT-IDENTICAL
(record) / YS c24/21/13 no-collapse (n=3 bucket; n>=8 owed vs g10b's
0-of-11) / vs-CPU c13 x3 consistent under hits / + live human Mewtwo
chains 12-13 from the afternoon session. All script checks passed.
**Remaining for the crown: the human netplay rung** — g15-vs-g10b at
d4, Fox mirror + Mewtwo, Bradley-gated. promote_check.sh DOLPHIN_DIR
now defaults to the exi-ai headless build (env-overridable).
fox_il_v1 full-game IL run LAUNCHED (4,465-game corpus, GRU,
eval_runs/0808_fox_il_v1.log) — the harness-validation track.

## ADDENDUM 2026-08-08 #5 (evening): fox_il_v1 run 1 — HARNESS VALIDATED

**The full-game IL question is answered YES.** fox_il_v1 (900 HF games,
5.4M frames, stride 30, GRU/temporal/learned-embeds, 10 epochs, ~25
min): val_loss 5.49→4.95 monotone; head losses sane (movement carries
the entropy: mx 1.55/my 1.23 vs buttons 0.42). Smoke vs level-1 CPU:
full game completes, 42 distinct actions, full-stage traversal,
loses 2 stocks takes 0 — plays varied-and-badly, exactly v1-grade.
Artifacts: checkpoints/fox_il_v1_20260809_023217_best_policy.bin (+
loss plot html, config json).

**Scaling findings (the run's real payoff — file in FIXES):**
1. --train-character filter NEVER worked (compared external char id int
   to a name string; dropped all 4,465 files) — FIXED in pipeline.ex
   with external-id map. External-vs-internal id trap strikes again.
2. Streaming chunk path leaks RAM in chunk prep — chunk-size
   INDEPENDENT (300/100/40 files all OOM a 62GB box). Real bug, unfixed.
3. Embedding cache is keyed by the FULL sorted file set — shards can't
   compose; per-file keying would unlock incremental builds.
4. mmap_embeddings.save requires materialized embeddings — an
   INCREMENTAL writer (parse→embed→append per file) is the unlock for
   the full 4,461-game corpus. Tomorrow's engineering item #1.
5. Dense temporal sequences cap the in-RAM path at ~400k frames at
   stride 5; stride is the lever (900 files fit at stride 30).
6. eval_live_protocol defaults fixed: exi-ai headless DOLPHIN_DIR +
   existing-ISO detection (two failed smoke launches from bad defaults).

Next on this track: incremental mmap writer → full-corpus run →
live-behavior eval protocol for generalist bots (shine metrics don't
apply; stocks/action-diversity/APM do).
