# HANDOFF 2026-09-13c — recorded recovery context and next multishine fit

This handoff is for Claude. It supersedes the recovery-context notes in
`HANDOFF_2026-09-12b.md` for the current state, while preserving that file's
historical training ledger.

## Goal

Build a local Fox multishine proof that sustains the technique across varied
starts and recovers after interruption. The finite cold-start proof passes;
warm-history recovery is the remaining gap. Do not reset recurrent history on a
hit and do not promote a checkpoint based on loss alone.

## Current checkpoint and controls

- Candidate: `eval_runs/0913_no_dropout_fit/round21/candidate.bin`
- SHA256: `404a1f543889ddd4193380745fbc603c6c94bb4a200a3caa7cbca6ddf823182a`
- Contract: windowed GRU F32, zero state per window, window 16, queue depth 3,
  previous action enabled, reaction/training delay 2.
- Cold matched-teacher responses: 12/12, chains 13–14, valid timing and no
  drift/errors.
- Warm familiar controls after the embedding fixes: 6/12. Neutral and sustain
  starts pass; recovery handoffs 4, 75, and 146 fail both repetitions (chains
  1/0/0). All 12 runs have valid timing, no drift, no errors or truncation.
- The unchanged model was used for this recheck; no fitting was started.

## Completed this turn

- Teacher restart validation passes all three isolated-hit cases (handoffs
  502/1389/3152): strict cycles 39/36/37, chains 40/37/38, no deaths.
- Teacher input readback: 1,080/1,080 commands match. Delay-2 targets: 1,074
  match. Case 502 is on-loop; `--allow-on-loop` is recorded explicitly and
  relaxes only the off-loop-coverage requirement.
- Real replay prefixes are exported as cold/warm pairs for nine clips in
  `eval_runs/0913_context_recovery/clips_v6/`. The 18-frame warm prefix is
  input-only; all 3,564 supervised targets and all early targets remain.
- Agent/training parity: 324 first-window checks pass, max absolute difference
  `1.1920928955078125e-7` (tolerance `1e-6`). Source replay hashes are checked.
- Fixed live/batch normalization mismatches for negative countdown frames and
  negative hitstun/action-frame counters. Focused regression suite is 16/16.

## Files to read

- Experiment and protocol: `docs/planning/RECORDED_CONTEXT_2026-09-13.md`
- Implementation guide: `docs/guides/RECORDED_TEACHER_TRAINING.md`
- Context implementation: `lib/exphil/training/recorded_context.ex`
- Boundary-aware batching: `lib/exphil/training/data.ex`
- Export/audit script: `scripts/prepare_recorded_context.exs`
- Repro wrapper: `eval_runs/0913_context_recovery/prepare.sh`
- Warm result: `eval_runs/0913_context_recovery/warm_recheck/eval_candidate.json`
- Teacher result: `eval_runs/0913_context_recovery/teacher/validation_with_on_loop.json`

Only `clips_v6` is ready for training. Earlier `clips`, `clips_v2` through
`clips_v5` contain failed or partial attempts and must not be globbed.

## Recommended next work

1. Add/finish per-case fit reporting that distinguishes cold versus warm clips
   and indexes the first 18 *supervised* targets (never the input-only prefix).
2. Train one bounded context-aware candidate. Freeze the budget first: the
   canonical pool plus the 18 clips is 10,641 targets; 64x early weighting is
   31,053 draws / 486 batches per epoch. Nine epochs is 4,374 updates. Do not
   blindly repeat the old 21-epoch budget.
3. Validate the candidate in this order: cold familiar controls, warm familiar
   controls, isolated interruptions, then replay-opponent interruptions. Keep
   strict cycles, readback, drift, timing, and truncation gates unchanged.
4. Preserve the current candidate so it remains the playable fallback. Do not
   install a new default until the closed-loop gates pass.

## Resume commands

```bash
cd /home/blewf/git/exphil
pgrep beam.smp || true
git status --short
bash eval_runs/0913_context_recovery/prepare.sh eval_runs/0913_context_recovery/clips_next
jq '.summary' eval_runs/0913_context_recovery/warm_recheck/eval_candidate.json
```

For local play, use `bash scripts/play_multishine_proof.sh`; it now source-loads
the constants and player normalization fixes. Avoid overlapping GPU jobs and do
not run a native NIF build in this environment.


## Addendum — from the 09-12 evening session (Claude Fable; detail in `HANDOFF_2026-09-12b.md`)

Infrastructure Astra's line now runs on, and the items that session left open.

**Landed (all committed; latest 9e7241b2):**

- **One delay knob.** `--reaction-delay k` on both runners and the scenario
  suite; `--frame-delay N` is a deprecated alias for k = N+1;
  `--delay-id-override` still wins but warns when it disagrees. The
  checkpoint's delay-id is derived from k (`ExPhil.Eval.HarnessRung`);
  drill ids are physical (`--pipeline-offset` retired). `ExPhil.Bridge.
  LatencyProbe` measures the real latency in every game's countdown
  (sync runner raises on mismatch, async logs; `--allow-latency-mismatch`).
  Verified against Slippi's recording: sync fd N = latency N+1, async
  policy path N+2, suite response-delay N = N+1. Astra's "reaction/training
  delay 2" contract plays at `--reaction-delay 2`.
- **`Agent.observe/4`** (observe-only history warm-up; `probe: true` reads
  the heads teacher-forced) — the suite warms a mid-game handoff with the
  true history; INVARIANTS 13.
- **INVARIANTS 14.** Frames carry `:label_source`; `ExPhil.Training.
  Labels.at_delay/3` is the one producer of delayed labels (recorded ->
  shift along the recording; expert -> `mod.label_ahead/4`);
  `Data.shift_actions/2` raises on an expert-tagged list. `scripts/
  audit_ms_pool_labels.exs --shifts` audits a pool AT its training shifts
  and gates the ms preregs (the g25 pool: 10/11 loop states conflicting
  at shift 4 under recorded-future labels -> 0 under item-14 labels).
  Note: `lib/exphil/training/labels.ex` was further hardened on disk by
  another session (mixed-source rejection, `require_projection`) —
  uncommitted at write time.
- **Instrument 2, coverage map:** `scripts/probe_ms_coverage_map.exs`
  (teacher-forced, 43 counterfactual cells, offsets 0..6, ~5 min).
- **Snapshot health:** `scripts/probe_snapshot_health.exs` (param/logit
  finiteness + an independent fixture BCE per snapshot).

**Findings that constrain the next fit (RESULTS.md §6-10):**

- ep57's failure is state coverage, not latency; its map is red on opp
  shield/grab/shine, opp behind/close, and the mirrored stage.
- Redress augmentation (g25a) moved the map but not the live gates:
  synthetic context is not the real dynamics; Bradley wants real
  varied-start rollouts for coverage.
- g26a (item-14 labels, g24a pool): 0 label conflicts and a 0.998
  fixture fit, yet the WORST stand floor (67/min c4; ep57 296). Leading
  suspect: `MultishineExpert.label_ahead/4`'s off-loop rule returns the
  recovery input HELD at every k (no press edge). The k-ahead label from a
  grounded off-loop state should follow the recovery INTO the loop (phase
  k-1 after the shine press); hold only where the entry frame is unknown.
  Astra's recorded-context line is a different answer to the same
  off-loop question — the two should not be trained blind of each other.
- **The drill's low-loss guard (`loss < 1e-5` -> restore best, GOTCHA #99)
  is wrong under conflict-free labels:** g26a's "collapse" epochs 34/36/38
  are finite, fit the fixture, and play the same as the accepted ones.
  Replace with evidence (non-finite params/logits, or a drop on a fixed
  behavioral probe set). NOT done. Any drill run that reaches ~1e-5 will
  loop restore/retrain and halt at the 5th restore; snapshot-all keeps
  every epoch's own weights, so select from the per-epoch tables.

**Open decisions for Bradley** (unchanged from 12b §4): v3 deploy card
(reaction-0 checkpoints have no exact async rung; sync plays them at
`--reaction-delay 0`); coverage-round design (real varied starts);
ep57's id-1 weakness.

**State at write time:** nothing running; no `ms_g26a.bin` final export
(training stopped by hand at epoch 48; use `ms_g26a_ep33.bin` if needed).
