# HANDOFF 2026-08-23 — memory-watch program COMPLETE; every menu now observable

**Resume protocol: read this + `MEMORY_WATCH_PROGRAM.md`. This
supersedes HANDOFF_2026-08-22c (same arc, now finished).**

## State (all committed, suites green: libmelee_ex 552/0, exphil bridge 59/0)

The memory-watch program's build checklist AND diagnosis items are
done; every claim below was validated either live over netplay
(eval_runs/0823_live_observability) or with screenshot ground truth.

- **Selection**: `css_pN_selected` (0x8043208C stride 8, GAME-external
  ids, 0x21=none) — validated at the ONLINE CSS; BlindCss skips A when
  locked (toggle-off impossible); post-game CSS RETAINS the pick.
  Entry-state can read garbage (26): only :none→{:character,c}
  transitions count.
- **Code entry (was the last blind menu)**: typed-text buffer at
  **0x804A0740, static** — 3 bytes/char (SJIS pair + NUL), NUL-term.
  `MemoryMap.direct_code()` + `decode_direct_code/1`; MeleePort
  watches it and logs the readback on change. The field autofills and
  the FIRST keystroke replaces it; stream menu_selection is FROZEN at
  the keyboard (blind typing — readback is the fix). Next consumer:
  verify-before-confirm in enter_direct_code (type → read → clear &
  retype on mismatch), which makes unattended reconnects closed-loop.
- **RAM menu merge**: default ON at the online CSS, static fields
  (EXPHIL_RAM_MENU: static default / full / 0). Cursors offline-only —
  the online cursor hunt was INCONCLUSIVE and is DEPRIORITIZED:
  selection-word-feedback steering (heading → A → read) is the
  adopted online pattern (used successfully by the hunt harnesses;
  the online hand SPAWNS OFF-GRID at the left edge — A there is a
  no-op, which burned two hunt rounds).
- **Delay (#10)**: ram_frame = slippi_frame + 123, flat across 48/48
  netplay arrivals — local pipeline adds ZERO observation lag; the
  22→4 chain gap is input-delay regime + rollback/network.
- **StuckPolicy**: suppressed a real 30s connect hold live (game
  started 2.4s later); bot14 wedge class still alarms.
- **Menu-time profiler** (Bradley's ask): `scripts/analyze_menu_time.exs
  <session.log>` — phase durations vs budgets + waste flags. First run
  flagged 14s of post-confirmation retry windows; FIXED same hour:
  BlindCss window-end with {:character,_} → :handback (safe: the RAM
  merge lets the helper press START itself). Expect press→handback
  ≈7s next session (was 21s).

## Ops laws (pinned tonight)

- Observability sessions MUST launch `--verbose` (CLI default sets
  Logger :warning and eats every science line).
- Teardown: `pkill -f "[p]lay_dolphin_async"` + `"[l]ibmelee_"` +
  `"[m]elee_bridge.py"` + **`"[s]lippi-dolphin-bot"`** (the
  EXPHIL_NETPLAY_HOME dolphin matches none of the old patterns; an
  orphan sat searching a truncated code).
- Probe work vs free-running dolphin: wall-clock holds only.

## Next (order of leverage)

1. **Verify-before-confirm code entry** (MenuHelper/MeleePort): use
   the buffer readback to close the typing loop; then unattended
   rematch/reconnect is fully trustworthy.
2. **#6 stage internals**: re-run tmp/mw_quartet.exs on FoD/PS.
3. **Stage-pinned crown decider** (FD + one agreed stage; ep4 vs g15)
   — protocol law from 08-22; now cheap and the CSS cycle is ~14s
   faster.
4. #4 scenario asserts / #7 outcome channels (consumers of
   MemoryMap.game()); generalist Fox restart per the standing
   direction.

## Session logs

eval_runs/0823_live_observability (RESULTS.md, g1b.log);
libmelee_ex tmp/mw_visual/*.log + PNGs (hunt rounds, screenshots);
harnesses tmp/mw_online_hunt.exs, tmp/mw_codebuf*.exs,
tmp/mw_quartet.exs.
