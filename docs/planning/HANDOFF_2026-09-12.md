# HANDOFF 2026-09-12 — multishine as the methodology test: sampled gating, first sampled chains (g23a), CPU-gate correction, DAgger round 1; NEXT = the interp instruments (label-conflict auditor first)

**Supersedes HANDOFF_2026-09-09** (its §3 structural items and §7 v3
command still hold; v3 is STILL NOT LAUNCHED). Written 09-12 by Claude
Fable 5.1. Bradley directed: "multishine bot with v3's recipe, local, no
delay"; "relying on deterministic was silly"; the corrected goal ("no
matter where Fox is, what the opponent does — it must multishine most
of the time; it's a methodology test"); and the interp-tool question
that sets the next list.

## 1. State of the world

- **NOTHING is running.** Last beam (an overnight play session left
  open by accident) was killed 09-11 14:01; its ~40 idle games deleted.
- **Disk: / at 100%, ~5.6G free.** Growth is mostly other repos' Rust
  caches (`~/git/platfighter*/target`, 5.2G) and the 30G checkpoints
  dir (657 `.axon` trainer states back to July). Bradley decides what
  to delete; the ms snapshots from this week are 1.5G and worth keeping
  until the DAgger line settles.
- exphil `main` @ **8daa527d**. **UNCOMMITTED, NOT MINE:** another
  session is unifying the training delay flags into `--label-delay`
  (23 files: parser, config, pipeline, streaming, cursors, peppi,
  label_convention, checkpoint, mix_frames, eval_model, docs, tests).
  Left untouched; commit only your own paths (`git add <paths>`) until
  that lands. It compiled at write time.
- edifice `main` @ 0edf76c.
- **Candidates:** `checkpoints/ms_g23a_ep57.bin` (technique: 438/4/423
  chains sampled vs stand dummy) and `checkpoints/ms_g24a_ep55.bin`
  (coverage: 69-71/min c8-13 vs level-1 CPU, floor c27). Deploy both at
  async `--frame-delay 3 --delay-id-override 2 --temperature 1.0
  --buttons-temperature 1.0 --stateful-step` (id in NEW numbering).
- **Preflight before any windowed session:**
  `bash scripts/x11_socket_fix.sh` (GOTCHA #114: Xwayland's X0 socket
  path gets unlinked while Bradley is away; deleter unidentified).
- Full ledger of the two days: `eval_runs/0910_g20_causal/RESULTS.md`
  §1-4g. Prereg scripts: `eval_runs/0910_g20_causal/run_g20.sh`,
  `0910_g21_sharp/run_g21.sh`, `0910_g22_converge/run_g22.sh`,
  `0910_g23_consistent/run_g23.sh`, `0911_g24_dagger/run_g24.sh` +
  `gate_cpu.sh`.

## 2. What was established (each measured; RESULTS.md has the tables)

1. **The g19 "record" (437/min c438) was argmax.** Sampled at T=1.0
   it is 45/min c3. Every ms card before 09-10 was an argmax number.
   `gate_sweep.sh` samples by default now (`GATE_TEMP=0` = argmax).
2. **Under sampling, convergence is the lever**, not the ep-4 peak:
   g19 late epochs chain 9-12 (loss 0.010). The peak/decay lore was
   the argmax metric.
3. **AR head is out for the ms line**: g20a releases B through the
   aerial reflector (p 0.33); argmax makes it worse. Mechanism unknown
   (instrument #4 below). Independent head is decisive (0.91-1.0).
4. **Chain length under sampling ~ 1/(1 - p_cycle)**: a 9-frame cycle
   at 0.95/frame chains ~3; game-long chains need ~0.99+ on every loop
   decision. Per-state probe: `scripts/probe_ms_state_confidence.exs`.
5. **The loss floor is label conflict, not capacity**: g22b fit its
   pool to 5e-5 yet sat at 0.95 on two boundary frames. Found:
   `snippet_mine.exs` built its expert table from `closed.slp` while the
   drill uses `closed_d1.slp` (one frame earlier) — 12.5k snippet
   frames one frame off at the boundaries, in g19's pool too. Re-mined
   -> g23a ep57: 0.98 on the boundary frames, chains 438/4/423.
   `--action-frame-buckets 24` (now drill-wired) took the loss floor
   5e-3 -> 5e-5 but did not move chains by itself.
6. **Bradley's local look: "not impressive"** -> 35.7/min c2 vs him;
   level-1 CPU 54-62/min c2-3; stand dummy 296/min c277. Delay ruled
   out (async d3 id2 is the aligned local rung; sync d3 == async d2).
   STATE COVERAGE is the failure: fixture-like states only.
7. **Gate corrected**: shines/min + chains vs `--dummy cpu` (async, d3,
   id 2, T=1.0, 2x90s) is the goal metric; stand dummy = technique
   floor. `eval_runs/0911_g24_dagger/gate_cpu.sh`.
8. **DAgger round 1 (g24a)**: Bradley's session + 8 CPU games relabeled
   (expert corrected 63-75% of frames). CPU 49-56 -> 69-71/min, chains
   2-3 -> 8-13; stand floor 296 c277 -> 111 c27. Real trade-off; the
   loop must gate both.
9. **Rung law (drill line, empirical)**: async live N <-> new id N-1
   (d3 <-> id 2 == g19's old id 3); sync d3 == async d2. The sync
   runner (`play_dolphin.exs`) no longer copies the live flag into the
   id (it had; every "d1" gate on 09-10 morning was id 1).
10. **Harness (GOTCHA #114)**: global `DOLPHIN_DIR` pointed gates at the
    netplay AppImage (needs a display); sweeps now force the exi-ai
    headless build, kill TERM-immune survivors, and run as
    `systemd-run --user` units (retires the 10-min chunking).

## 3. NEXT — the interp instruments (Bradley: "let's start down this list")

1. **Pool label-conflict auditor as a PRE-TRAIN GATE.** Extend
   `scripts/probe_ms_label_dilution.exs`: entropy of training labels per
   state key (action, af, grounded + opponent buckets + prev-input
   queue) over the assembled pool (fixture, rollouts relabeled,
   snippets, openers, scramble). Refuse to train where the pool
   disagrees with itself on a loop state. Seconds; would have caught
   the snippet conflict before g19.
2. **Counterfactual coverage map** (the goal, measured offline): perturb
   fixture loop states along own position / opponent position /
   opponent action / facing / percent; batch-embed; p(correct input)
   per cell. Aims DAgger rollouts instead of collecting blind.
3. **Chain-break attribution**: per break in a rollout, teacher-forced
   replay of the window, p(correct) at the break frame, gradient x
   input over embedding groups (own, opponent, prev-action queue,
   delay-id). Needs Activations/attribution + CycleSim to support the
   queue/delay-id layout (that repair also gives an offline chain
   predictor).
4. **AR-head conditional entropy along the chain** at loop states —
   the mechanism behind g20a's mid-air release; needed before the AR
   head is declared unfit, since verifying it is the point.
5. **Re-entry profile** after a break (frames to next JC, which state it
   parks in) — the absorber-entry detector with the ms embed config.

## 4. Resume

```bash
cd ~/git/exphil && git log --oneline -3      # 8daa527d + the other session's --label-delay work uncommitted
pgrep beam.smp; systemctl --user list-units 'exphil*' --no-pager
df -h /                                       # ~5.6G free — clean before training
bash scripts/x11_socket_fix.sh                # before any windowed Dolphin
# Reproduce the CPU gate on a candidate:
#   bash eval_runs/0911_g24_dagger/gate_cpu.sh checkpoints/ms_g24a eval_runs/<out> "55"
# Per-state confidence: mix run scripts/probe_ms_state_confidence.exs --policy <p> --delay-id 2
# Label conflict (pre-train): mix run scripts/probe_ms_label_dilution.exs
```

Memory: `project_status_2026-09-05_v2_launch.md` (09-11 04:35 update)
is the MEMORY.md pointer. v3 launch command: HANDOFF_2026-09-09 §7 —
still parked behind this methodology test by Bradley's call.
