# HANDOFF 2026-08-10 — resume point

Supersedes HANDOFF_2026-08-08.md (whose addenda #1–#13 are the raw log
of 08-08→08-10). This is the orientation layer. Everything below is
COMMITTED (exphil through b436da6, libmelee_ex through ce11aed).

## STATE OF THE LAB

**Production policy: `checkpoints/ms_g15_oppmask_full.bin`** (CROWNED
2026-08-09), deploy `--frame-delay 4` netplay / d3 local. Won a gated
7-game blind human netplay decider vs g10b: chains {3,1,46,6} vs
{2,2,2}; **chain 46 = all-time netplay record** (old: 4; set on
Dreamland, through platforms), replicated with chain 6; qtrace lag peak
verified 6@98.7–99.6% every game. Local FD d3 vs a human: chain 15 @
74/min (0809 session). Trained = champion recipe + full-block opponent
scramble (`--opp-scramble-frames 12000`). Fallback: `ms_g10b_human.bin`.
Open caveats (do NOT gate — the decider was the gate): rung-0 4.28
scramble-probe calibration, YS n>=8 bucket.

**The two products / the frontier.** SPECIALISTS (g15, g10b): drilled
on multishine vs a dummy — execute the technique, don't play neutral.
GENERALISTS (fox_il_v2 family): full-game IL from real HF games — play
whole matches but badly (lose, run off edges, 0 kills). Every open
training arm is a bet on bridging these.

## THE v2-long VERDICT (why the next work is what it is)

fox_il_v2_long (constant LR 1e-4, 40 epochs from v2's 10, full fox_v2
corpus): best val **5.334 ~epoch 14** vs v2's 5.374 — 4x epochs bought
~0.04 val, then plateaued 10 epochs and NaN-diverged at epoch 25 (no
grad clip). Checkpoint `fox_il_v2_long_20260810_003606_best_policy.bin`.
**The cheap "just train longer" lever is nearly exhausted** — imitation
on this corpus plateaus ~5.33 by epoch 14. Next gains need the cleverer,
now-evidence-backed arms below.

## NEXT WORK (in order — the queue Bradley set 2026-08-10)

1. **Behavior-eval v2-long vs v2** (pool, n=8, ~3 min each via
   `scripts/eval_behavior_pool.exs`, temperature 0.3). Confirm whether
   the 0.04 val gain shows up in PLAY (stocks, SD rate, action
   diversity, damage). Expect small. Compare pool-vs-pool ONLY
   (EVAL_PROTOCOL regime rule); this is a v2-vs-v2long relative test so
   the regime cancels. Checkpoints: fox_il_v2_20260809_041730_best_policy
   vs fox_il_v2_long_20260810_003606_best_policy.

2. **Edge-correction DAgger arm.** The probe-confirmed #1 lever
   (`scripts/probe_edge_attribution.exs`, 0809: position IS read at
   near-edge sites, |Δstick| 0.033 — the SD loop is a COVERAGE gap, not
   a representation gap). Build: harvest the bot's own SD segments
   (`Melee.GameEvents` emits `{:stock_lost, kind: :sd}` live; or
   post-hoc from replays), relabel the ~30 frames before each
   edge-crossing with a scripted turnaround expert (grounded + dashing
   toward edge + ledge_dist small → dash-back), retrain the IL base on
   the mix. This is the existing dagger_drill pattern pointed at edges
   instead of shines. Measure vs the v2-long baseline with pool evals.

3. **Per-stage ledge distance (task #25, RETRAIN-GATED).** FINDING
   (0809, quantified): `embed_ledge_distance` hardcodes stage edge
   x=85 at TWO sites (player.ex:508 fast-path + :883), but real edges
   are 58.9 (YS) … 90.7 (PS). Error is +26 on YS in the DANGEROUS
   direction: at x=60 the bot computes ledge_dist 0.29 ("safe") on YS
   when it is actually OFFSTAGE (−0.02). Champions dodged it (chain-46
   on DL +5, FD −3.5 conservative). IMPL: thread per-state stage edge
   into `PlayerEmbed.embed`/`embed_batch` (stage is on game_state, not
   player) → replace both 85.0 sites → behind a default-off config flag
   `per_stage_ledge` (existing checkpoints stay valid) → its own v3-edge
   training arm (NOT folded into a baseline — change one thing). Edge
   table: `Melee.Stages.edge_ground_position` (teeter x) is the right
   one. Existing checkpoints are calibrated to 85-everywhere, so a
   fresh run must opt in.

4. **Later / structural:** conversion-kill training (231% dealt / 0
   stocks taken); F5 offline RL on the IL base (the decided long-term
   direction). g16 subtle-full opponent mask (the g14×g15 sweet spot,
   solo-runnable). Low-tier / personal-corpora fine-tune arms.

**"Could we start another run?" checkpoints:** after #1 (eval only, no
training) we are NOT mid-run. After #2's arm is BUILT but before its
training launches = a clean spot to start a run. #3 produces a
ready-to-launch v3-edge arm. Bradley wants a heads-up at each such spot.

## INFRASTRUCTURE SHIPPED 08-08→08-10 (all committed)

- **MmapCorpus** (`lib/exphil/training/mmap_corpus.ex` +
  `scripts/build_corpus.exs` + `--corpus` flag): incremental
  parse→embed→append corpus, RAM O(batch). Built `cache/corpus/fox_v2`
  (4,418 games / 41.4M frames / 47.7GB). Fixed the v1 port bug (62% of
  v1 imitated the wrong player).
- **Pool behavior evals**: `ExPhil.Eval.LiveScorer` (folds
  `Melee.GameEvents`) + `scripts/eval_behavior_pool.exs` (N parallel
  headless sessions, n=8 in ~3 min, live-scored). `analyze_behavior.exs`
  = the replay-scored equivalent. Regime rule in EVAL_PROTOCOL: pool =
  sync input path, async = the promote rung; compare pool-vs-pool only.
- **Netplay ergonomics**: `--require-stage` (LRAS-requeue on wrong
  stage; Direct game-1 is random, loser picks after);
  analyze_shine_source port autodetect (bot lands port 1 or 2 by
  connect order — phantom-zero artifact twice); MeleePort dummy release
  at stage select (undulation fix); MeleePort `:on_stuck` watchdog
  wired (login-wedge → signal).
- **libmelee_ex arc** (suite 281→307): proportional `steer_toward`
  (Chebyshev, deadzone-safe); `declare_ports/2` (no adapter grab);
  menu watchdog; session invariants (replay-dir⇒save, "" connect-code,
  spectator-port-in-use fail-fast); `Melee.GameEvents`; `SessionPool`;
  direct Slippi-online CSS portrait selection (kills the ~40-press
  random-reroll); typed unknown scenes (raw_scene preserved + logged);
  Frozen-Stadium toggle only-on-Stadium + edge-driven (~1s off every
  stage select).
- **Interp**: audit game round 3 (own-y replant) = champion recipe
  RESISTS label-flip poisoning (EFFECT FAIL at 14% mass on a read
  variable — a 2-point resistance result). Blind auditor still awaits a
  valid target (round-4 ideas: flip-to-PRESS, higher mass, fine-tune
  planting).

## GOTCHAS added

- **#90**: post-reboot CPU-flavor libexla loses CUDA symbols; eval
  protocol default is now `XLA_TARGET_EVAL=cuda12`.
- **#91**: `--lr-schedule cosine` decay horizon defaults to 10k steps
  → freezes long runs (froze v2-long attempt 1 at epoch 1). Use
  `--decay-steps <total>` or `constant`.

## OPEN TASK LIST (harness)

- #25 per-stage ledge distance (retrain-gated; full finding attached) —
  the only carried task; becomes work item #3 above.
