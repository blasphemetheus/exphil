# HANDOFF 2026-07-30 — 5090 resume: cross-machine delay offset found; SYNC+DELAY-2 IS THE HARNESS (cal 0.99, chains 79-176, headless equivalent)

## EVENING UPDATE — the harness is solved on this machine

**Canonical eval harness: `--runner sync -- --frame-delay 2` (windowed
OR --headless — identical).** Results, stand dummy:

| config                       | self/min | maxchain   | cal        |
|------------------------------|----------|------------|------------|
| sync d2 windowed (3 runs)    | 258-280  | 79-145     | 0.991 @ -3 |
| sync d2 --headless (3 runs)  | 264-288  | 79-176     | 0.982 @ -3 |
| sync d2 first probe (1 run)  | 287      | **264**    | 0.997 @ -3 |
| async windowed --frame-delay 1 | 124-157 | 8-12      | 0.68 @ -3  |
| laptop all-time best (07-28) | 165-186  | 30         | 0.78 @ -3  |

Sweep was sharp: sync d1 = 67/min c1 (cal 0.876@-2), d2 = the lock,
d3 = 112/min c1 (cal split -4/-2). One frame either way = metronome.

Two reinterpretations this forces:
1. **The laptop's "sync runner collapses ALL policies to chains 2"
   (aa0913b jitter experiment) was delay MISALIGNMENT, not jitter** —
   the frame-locked runner is the best harness we have once its
   deterministic delay is dialed to the policy's trained offset.
   Nobody swept delay under sync on the laptop.
2. **The headless concentration loss (cal 0.67, task 5) was the ASYNC
   poll race, not the Null-gfx path.** Under sync, headless == windowed
   (0.982 vs 0.991). Task-5 discriminator: resolved by deletion.

Sync determinism on this box: 3600/3600 inferences, 0 skips, and an
un-tuned block scored IDENTICAL 70.0/min three times. Residual run-run
variance at d2 (258-288) is game-side, not harness-side.

`--emulation-speed 0` under sync+headless preserves fidelity
(255/min c102, cal 0.964) but does NOT beat realtime — the sync loop
still paces at 60Hz somewhere; future throughput lever for farms.

**Dagger lineage re-scored through the trusted harness** (headless
sync, stand dummy): the delay ladder is real and one frame wide.

| policy       | sync d2       | sync d3          |
|--------------|---------------|------------------|
| champion     | 258-288, c79-176 | 112, c1       |
| dagger3 R3   | 141, c1       | 133-194, c2-84   |
| dagger3 R4e10| 136, c1       | 130, c2          |

- **R3 is the d1 champion**: home = champion's home +1, chains up to
  84. The DAgger-through-delayed-bridge campaign WORKED.
- **R4 margin fattening was live-counterproductive** (c2 at home
  despite fatter offline margins) — the offline margin probe does not
  predict live chaining; retire the R4 objective, keep e10 only as a
  probe artifact.
- R3 robustness gap vs champion: run-to-run chains 2-84 (champion
  79-176), offline jc_event flip 95% — the JC margin is still the
  weak joint, but fix it with data/rollouts, not the R4 loss.

## FARM 9 (5090, trusted harness): training is now the bottleneck

Champion recipe (farm-6 ARM B), 3 fresh seeds, ~40s/seed to train on
the 5090, evaled sync-d2 headless:

| seed | self/min (runs)        | chains          | offline jc     |
|------|------------------------|-----------------|----------------|
| a    | 83,83,224 + n6: 125,132,83,107,201,83 | 1..213; 4/6 escape | p10 +9.7, flip 0 |
| b    | 133,133,131            | 5,5,5           | p10 +1.0       |
| c    | 77x3 (d1/d3 too: c1)   | 1 always        | p10 -7.8, flip ~1 |

Reads:
1. **The harness is no longer the source of inconsistency — training
   is.** Same recipe, three phenotypes: opening-lottery bistable (a),
   stable-mediocre (b), wrong-side JC boundary (c, unrescuable by any
   delay).
2. **The metronome is a deterministic absorber**: seed a's failed runs
   score EXACTLY 82.9/min every time; its successful runs chain
   34-213. Per-run opening escape ~2/3. "Opening is the last lottery"
   (2026-07-27) — now isolated, per-run, on a 0.98-cal harness.
3. Offline jc margins do NOT predict live escape (a: p10 +9.7 yet 1/3
   metronome) — consistent with the R4-margin lesson. The opening is
   decided in states the offline probe never visits.

Farm 10 (SS alone) aborted twice over recipe drift: (a)
--scheduled-sampling requires --prev-action; (b) farm 9/10 had only
174 synthetic opening frames vs farm 6's 870 — farm 6 passed
--opening-replays (dead seeds' real absorbed openings). ALSO FOUND: a
missing checkpoint is NOT fatal to the live runner — three "evals" ran
a phantom policy and scored 0 (fix queued: loader must halt).

## FARM 11 — THE GATE PASSES

Champion recipe + `--opening-replays "eval_runs/0730_farm9_*/r*.slp"`
(this machine's own live replays, 3132 opening frames). Two arms x two
seeds, 4-run sync-d2 headless evals:

| seed | arm                  | self/min        | maxchain per run  |
|------|----------------------|-----------------|-------------------|
| a1   | opening cov          | 246-248 x4      | 248,180,248,248   |
| a2   | opening cov          | **294 x4**      | **294 x4 (all unbroken)** |
| b1   | + prev-action, SS 0.5| 250 x4          | 250 x4 (all unbroken) |
| b2   | + prev-action, SS 0.5| 254-257 x4      | 257,217,257,257   |

**4/4 seeds, 16/16 runs, every run an unbroken or near-unbroken
chain.** Training is ~40s-11min per seed on the 5090; the whole
train->eval loop is ~10 min/seed. The 2026-07-27 "opening is the last
lottery" is CLOSED by opening-replay coverage harvested from the
machine's own failed runs — the flywheel works: trusted harness ->
cheap farms -> dead runs become the coverage that fixes the next farm.

**Bradley's consistency gate (stated 2026-07-30): "consistent and easy
training of the multishine bot is the confirmation that we've found
enough of the inconsistencies to proceed." — PASSED.** The rig's
inconsistencies found and fixed today, in order: stale libmelee pin,
XLA-target NIF mismatch, mainline AppImage on NixOS, stale-replay
copying, missing-checkpoint phantom evals, monthly replay dirs,
cross-machine +1 delay, async poll race (deleted via sync runner),
delay-untuned sync verdict from 07-28 (reversed), recipe drift
(opening-replays + prev-action defaults undocumented in farm logs).

Remaining known softness: metronome absorber still exists as a basin
(farm-9 seeds prove it's reachable); Mewtwo transfer untouched
(model-organism lock-in warning stands). Phantom-policy loader: FIXED
(agent init is fatal on load failure). ScheduledSamplingTest has one
pre-existing backend-mixing failure on this machine (runtime SS is
fine — farm 11 arm B trained through it).

## Sync farm throughput (late evening)

**Found: play_dolphin.exs (sync) accepted --headless/--emulation-speed
but never plumbed them to the bridge** — every "headless sync" block
above was actually windowed at 1.0x (their equivalence was
self-equivalence; the conclusion "sync fixes the race" stands, the
windowed/headless comparison was vacuous). Plumbed now. Two farm tiers
on this machine (ms_farm11_a2, sync d2, --emulation-speed 0):

| tier | env | wall (60s game) | fidelity |
|------|-----|-----------------|----------|
| record-grade | EXLA_TARGET=cuda | 51s (1.6x incl. menus) | 294/294 EXACT, n=3 |
| screening    | EXLA_TARGET=host | 30s (~3x)              | 232-292, occasional break |

Use screening for rollout collection and coarse comparisons;
record-grade for anything entering a table.

## FARM 12 (overnight): the delay ladder does NOT yield to BC

Goal (Slippi Direct): Direct adds online_delay >= 2 over the pipeline's
intrinsic +2, so a Direct bot needs a seed locking at sync-d3/d4+.
Arms (all with farm-11 opening coverage), n=1 screening sweeps d2/d3/d4:

| seed    | mechanism           | d2       | d3       | d4      |
|---------|--------------------|----------|----------|---------|
| shift1a/b | --action-delay 1  | 80-82 c1-2 | 102 c2 | 90 c2   |
| shift2a | --action-delay 2    | 113 c1   | 117 c1   | 117 c1  |
| fix1a   | d1-shifted fixture  | **289 c289** | 120 c1 | 110 c1 |

1. **Shift pathology survives opening coverage** — label-shifted seeds
   metronome at EVERY delay. The absorber-was-the-mechanism hypothesis
   is falsified (running count: 6+ dead explanations). Do not spend
   more BC arms on --action-delay.
2. **fix1a is another PERFECT d2 bot** (289 unbroken) — training on
   the one-frame-early fixture does not move the lock point. BC
   collapses onto the harness the DATA was recorded through, not the
   labels' nominal shift.
3. The only mechanism that has climbed a rung remains **DAgger through
   the actually-delayed bridge** (R1-R3, +1 inversion). Path to
   Direct: DAgger from a farm-11-grade base through sync-d3 collect,
   relabel with the d1 teacher table, retrain WITH opening coverage;
   iterate rungs to Direct's measured delay. Plumbing exists end to
   end (dagger_drill + --connect-code in bridge); blocker for a real
   Direct session: the bot Slippi account (Bradley).

Ops notes from the farm-12 run: EXLA_TARGET=host must be eval-only
(script-wide it put training on CPU, 30+ min/seed); fish config
exports DOLPHIN_DIR to the Ishiiruka build and ${VAR:-default}
defers to it — farm scripts now pin the wrapper build explicitly
(NOTE: farm 9/11 eval blocks therefore ran Ishiiruka windowed; their
comparative conclusions stand, but absolute numbers are that build's).

## OVERNIGHT (07-31, 1am-5am): the d3 rung — 12 arms, one invariant

Goal: a policy locking at sync-d3 (first rung toward Direct). Every
arm below evaled sync d2/d3/d4 screening. NEW RECORDS fell as side
effects; the rung did not.

| arm | mechanism | d2 | d3 |
|-----|-----------|----|----|
| rung1 (ad2, identical rollouts) | full dagger | - | INERT (jump-only) |
| rung1 ad3 | full dagger | 35 | 48 c1 |
| loop r2/r3/r4 (aggregate) | full dagger | - | inert / 124 c1 / 66 c2 |
| freeze1 (heads-only, no openings) | heads | 0 | 0 (neutral fixed point) |
| freeze2 (+openings) | heads | **387 c387 RECORD** | 42 c1 |
| full2 (+openings) | full | 46 | 124 c1 |
| diverse1 (3-policy temp collects) | full | **387 c387** | 84 c1 |
| widen1 snapshot (+x-hold-extend 3) | full | 339 c339 | 73 c1 |
| ad2 on diverse pool | full | 116 c1 | 120 c2 |
| ad4 on diverse pool | full | 0 | 0 |

Findings, in importance order:
1. **Everything trained on data collected through THIS deterministic
   pipeline locks at d2** — labels (d1 fixture), label shifts (ad2/3/4),
   openings, x-hold widening, stochastic collection, heads vs full:
   none move the lock point. The one d3-chainer (laptop R3) was trained
   on SMEARED-harness rollouts. **Determinism is poison for delay-
   adaptation data**: six sync-d3 collects are byte-identical.
2. **Boundary-map mechanism**: R3 keeps a soft positive X tail at every
   af (miss the JC -> retry next frame); all 5090 arms learn razor
   windows (positive at one af, hard negative elsewhere) -> one miss =
   metronome. The laptop harness's smear was accidental label widening
   PLUS state diversity. x-hold-extend alone did not reproduce it.
3. **d1-labels at d2 = frame-perfect regime**: freeze2/diverse1 both
   hit 387/min with the WHOLE RUN one chain (prior record 294). The
   one-frame-early teacher tightens the cycle ~1 frame. Also: the
   opening-coverage port to dagger_drill (--opening-replays) fixed
   heads-only inertness — head fine-tuning works as a mechanism.
4. **Next move (aligns with the 07-31 option discussion): stop trying
   to climb fixed rungs with BC/DAgger; go delay-DISTRIBUTION** —
   collect one pool across d2/d3/d4 (+temperature), i.e. reproduce the
   laptop's beneficial smear deliberately, and/or the in-flight action
   queue input (slippi-ai parity). The rung-climb matrix above is the
   evidence that motivated it.

New tooling landed overnight: --opening-replays + --x-hold-extend in
dagger_drill(+freeze variant); dagger_drill_freeze.exs (heads-only
fine-tune: grad-mask optimizer wrapper + donor trunk splice);
boundary probe covers the new arms. Checkpoints ms_d3_* on B2.

## SLIPPI DIRECT LOOPBACK (07-31 late morning): FIRST BOT NETPLAY GAMES

EXPH#288 (bot, this machine, freeze2 policy) vs DBTD#411 (Bradley,
same machine, launcher instance). Two full Direct games. Setup that
works: bot login snapshot at ~/.config/slippi-dolphin-bot (launcher
logins clobber netplay-beta/Slippi/user.json — snapshot per account);
EXPHIL_NETPLAY_HOME selects it; bot on --slippi-port 51442 (launcher
holds 51441) and default netplay port 2626 (Bradley's side forced to
2660 via Dolphin GUI). Fixed en route: no-frame watchdog killed the
bot mid Direct-search (netplay search screens emit no frames — now
30min tolerance under connect_code); SlpRepair.repair_gaps (mainline
ONLINE replays carry small mid-stream zero gaps that break peppi AND
py-slippi — resync at next FRAME_START recovers ~everything).

**THE MEASUREMENT** (cal probe on the repaired Direct replay):
offset calibration is FLAT — 0.41-0.49 at EVERY offset (local sync
harness: 0.99 at one). Direct's effective delay is a multi-frame
SMEAR (rollback + jitter; 3.3%% stale sends), not a number. Live
result matches: 26 self/min, chains <=2 (in a real fight, platform
stage — both caveats noted; a controlled FD idle-opponent Direct
session would give a cleaner cal).

Consequence: a fixed-delay policy cannot lock over netplay in
principle. The netplay path is (a) delay-DISTRIBUTION training with
the pool shaped like the measured smear, and/or (b) the in-flight
action queue as an input (slippi-ai parity). We now have the target
distribution from a real Direct game:
eval_runs/loopback_direct_game2_fixed2.slp + interp/cal_direct_game2.json.

Resumed from the trip (HANDOFF_2026-07-29b) on `nixos_slanka` (5090).
All laptop artifacts recovered from B2 (`sync-all-up` had run): champion,
dagger1-3 policies, R4 trainer ckpt, eval_runs/, rollouts.

## THE FINDING — this machine is one frame FASTER than the laptop

Champion `ms_open_z.bin` windowed async on the 5090: **69-75/min, chains
1, cal smeared bimodal 0.63@0 / 0.63@-2** (laptop: 165-186, chains
15-30, cal 0.78@-3). Bradley watched the screen: shine -> full jump ->
no aerial shine -> repeat. The aerial B (anticipatory, tuned to laptop
pipeline latency) arrives while Fox is still in jumpsquat and is eaten.

Falsified on the way (all 3-run stand-dummy windowed blocks, all ~70/min
chains 1, cal unchanged):
1. Dolphin build (slippi-nix netplay vs mainline beta 19) — identical.
2. --blocking-input — identical.
3. Inference backend (EXLA cuda vs host CPU; 2.3 vs 7 inferences/frame)
   — identical.
4. Headless --pace-hz 0 — identical.
5. libmelee pin (venv had upstream 0.47.2, requirements pin vladfi1
   v0.43.0; reinstalled) — identical (but keep the pin).
6. af convention: state-stream pair recorded ON THIS MACHINE
   (EXPHIL_STATE_TRACE=1) diffs EXACTLY to the committed
   action_frame_map.json — live state stream is faithful; task-8
   normalization is machine-independent. (Pair kept:
   eval_runs/0730_statestream_5090.{slp,live-trace.log})

**Confirmed prereg**: champion + `--frame-delay 1` -> **124-157/min,
chains 8-12**, cal peak moves to **-3 (the laptop's exact position)**
at 0.68, jc flip 0. The harness pipeline here is one frame shorter;
one artificial delay frame restores the champion's trained alignment.

**Rule for this machine**: capability evals of laptop-trained policies
need `--frame-delay 1`. Laptop "delay-k" corresponds to
`--frame-delay k+1` here. Residual: 0.68 < 0.75 gate with -2 leakage —
chains 8-12 vs laptop 15-30; the remaining sub-frame race is still
worth the task-5 discriminator treatment.

## Harness/NixOS plumbing landed today

- **Mainline beta on NixOS**: raw AppImage fails (missing host libs).
  Fixed via `appimage-run` wrapper dir:
  `~/.config/Slippi Launcher/netplay-beta-nixos/` (wrapper script +
  Sys symlink). libmelee execs it happily; `--version` =
  4.0.0-mainline-beta.19. Headless runs work through it.
- **eval_live_protocol.sh**: `XLA_TARGET_EVAL` env (default cpu keeps
  laptop behavior; use `cuda12` here — XLA_TARGET=cpu can't load the
  CUDA-built NIF cache); monthly-subdir replay glob (mainline writes
  ~/Slippi/2026-07-Mainline/); **freshness guard** — a replay older
  than run start is a failed run's leftover, never copied (kills the
  stale-copy artifact that faked 3 identical runs today).
- **EXLA_TARGET=host** (runtime env) forces CPU inference without
  rebuilding; XLA_TARGET (compile-time) must stay cuda12 on this box.
- devenv required for everything: `devenv shell -- env ISO=... bash
  scripts/eval_live_protocol.sh ...`

## R4 margin round: finished, verdict nuanced

- Resume command recovered by brute-forcing the trainer-ckpt
  fingerprint (rollouts = explicit 12-file list: round0 r1-3 +
  round1_collect r1-6 + round1_eval r1-3; expert multishine, gru,
  dropout 0.6, tw 2.0, max-epochs 22).
- Epochs 11-22 ran in 100s on the 5090 (~40x laptop). Loss BOUNCED
  (0.03-0.30, ended 0.037, never hit 3e-4; epoch 10 was 0.0073).
- Boundary probe: **epoch 10 remains the best margin profile**
  (af1-2 X ~1.6-1.9, tails 0.8-1.1); epoch 22 slightly thinner
  (af1-2 ~1.4-1.5, tails ~0.5). Margins wobble epoch-to-epoch with the
  bouncing loss — "train past convergence" needs an LR drop, not more
  epochs at the same LR.
- Checkpoints preserved: `ms_d1_dagger3_r3.bin` (R3 export),
  `ms_d1_dagger3_r4e10.bin` (best margins), `ms_d1_dagger3_r4e22.bin`.
  (`ms_d1_dagger3_policy.bin` = e22 now; the resume overwrote the
  originals locally — B2 still has them.)

## In flight / next

1. dagger3-r4e10 delay-preference eval, machine-corrected — DONE:
   - --frame-delay 2 (laptop delay-1, its home): 86-105/min, chains 2-4
   - --frame-delay 1 (laptop delay-0): 64-122/min, chains 2
   Prereg "chains >= 8" FAILED again; rate difference across delays is
   <2x = unresolved. **Fatter offline margins (e10) did NOT lift live
   chains** — margin thinness looks non-binding, or the R4 objective
   fattened the wrong thing. Next lever is the freeze-trunk head test
   and/or more rollouts, not more margin epochs. Champion at ITS home
   (delay 1 here) still strictly dominates (124-157, chains 8-12).
2. --local-delay 1 live gate (cal shifts exactly one offset, stays
   sharp) — now doubly interesting: compare --local-delay vs
   --frame-delay implementations.
3. Task-5 residual: cal 0.68 vs 0.75 gate at the aligned delay —
   chase the remaining -2 leakage (same stationary-race family).
4. Seed-farm train-consistency block on the 5090 (champion recipe,
   n>=3) — NOTE: GPU training loss bounce (R4) suggests checking
   backbone defaults / LR schedule under the much faster step rate
   before trusting farm comparisons.
5. R4 margin fattening with a proper LR drop from the e10 snapshot.

## Numbers (all 3-run stand windowed unless noted)

| config                         | self/min   | chains | cal |
|--------------------------------|------------|--------|-----|
| champion, delay 0 (any config) | 68-75      | 1      | 0.63@0 / 0.63@-2 smear |
| champion, --frame-delay 1      | 124-157    | 8-12   | 0.68@-3, -2 leak |
| laptop reference (07-28)       | 165-186    | 15-30  | 0.78@-3 |
