# HANDOFF 2026-08-08 — resume point

Supersedes HANDOFF_2026-08-07.md (whose five addenda are the raw log of
these two days; this doc is the orientation layer). Blow-by-blow with
caveats lives in INTERP_ROADMAP_V2's experiment log as always.

## STATE OF THE LAB

**Production policy: still `checkpoints/ms_g10b_human.bin` (deploy
`--frame-delay 4` netplay / d3 local).** But it has a challenger with a
complete offline case:

**`ms_g15_oppmask_full` — the crown candidate.** Full offline
promote_check recorded (eval_runs/0808_promote_g15.log):

| rung | g15 | g10b reference |
|---|---|---|
| stand FD d3 (x3, bit-identical) | **430.4/min c426 — record** | 421 c421 |
| Yoshi's Story x3 | c24/c21/c13, no collapses | 0-of-11 collapses, chains 43-208 |
| vs-CPU x3 | c13/c13/c13 under hits | — |
| **live human MEWTWO (local d3)** | **chains 12-13 mid-fight** | c1 vs a STILL Mewtwo |
| rung-0 dependence | 4.28 (see caveat) | 1.44 |

**The crown is gated on ONE session: g15-vs-g10b human netplay at d4**
(Fox mirror + Mewtwo; Bradley-gated). Caveats: YS comparison needs n>=8
(g15's chains are shorter than g10b's good runs); rung-0's probe may
not calibrate for scramble-trained policies (open probe-validation
item).

## THE TWO DISCOVERIES OF 08-07/08

1. **Opponent-character is the fight-state wall, and it's augmentable.**
   First non-Fox human opponent ever faced (Bradley's Mewtwo) collapsed
   multishine from chain 58 (still Fox) to 1 (still Mewtwo) — same
   silencing mechanism as the platform absorber. Four augmentation
   rounds mapped the design space:
   - g13 (coherent redress, smallest-first, no Fox in roster):
     **INVERTED** the policy (Mewtwo c22, Fox c1!) — data-quality/
     character confound. Kept as a forensics specimen.
   - g13b (confound fixed): conditioning, not invariance — dependence
     ROSE. Coherent redress teaches costume-switching.
   - g14 (per-frame scramble, 3 fields): **dependence floor 1.08 +
     stand record** — in-support noise teaches true ignoring — but no
     transfer (unmasked fields still gate).
   - g15 (full-block scramble): record core + REAL Mewtwo transfer
     live, but dependence ceiling 4.28 — implausible noise teaches
     scramble-DETECTION. **Mask plausibility is the design variable**;
     the sweet spot (subtle noise, full coverage) is unexplored.
2. **The full-game IL harness is VALIDATED.** fox_il_v1 (900 HF games,
   5.4M frames, stride 30, GRU): val 5.49→4.95 monotone in 10 epochs
   (~25 min), and the exported policy completes live games vs a CPU
   with 42 distinct actions and full-stage movement (loses 2 stocks,
   takes 0 — varied-and-bad, exactly v1). The
   representation question that motivated the multishine program is
   answered YES at full-game scope.

## DECISIONS STANDING

- Priority order (Bradley, 08-08): **local multishine robustness first,
  delay work parked behind it.** Multi-delay conditioning stays in
  recipes (deployability), but the delay INVESTIGATION resumes only
  after opponent robustness lands.
- IL-on-real-games before RL; RL later via F5's offline path anchored
  on the IL base. RL-direct-on-multishine deprioritized.
- Corpus strategy: HF Fox-filtered generalist base first; personal
  corpora (G&W backup — Bradley still owes retrieval — Greg's Zelda,
  local mewtwo dir) are FINE-TUNE arms, not first runs.
- Multishine claims cite ShineChain ONLY (GOTCHA #89).

## INFRASTRUCTURE SHIPPED (all committed with this handoff)

- Netplay/local play stack: SlotA=255 black-screen fix (#87), warmup
  compiles the fused sampling graphs (first-game freeze fix),
  `--nametag` (CSS tag flow, #88 fixed en route), `--postgame-delay`
  (CSS grace on any game exit), DOLPHIN.md launch recipes.
- Drill levers: `--opp-randomize-frames`/`--opp-randomize-chars`
  (coherent redress) and `--opp-scramble-frames` (per-frame noise) in
  dagger_drill.
- Eval plumbing for the native-bridge era: eval_live_protocol +
  promote_check default to the exi-ai HEADLESS build, detect the real
  ISO, and pass `--replay-dir` (native bridge saves no replays without
  it). Stand-MEWTWO gate validated as a cheap opponent-robustness rung
  (reproduces the human finding at 60s cost) — should join
  promote_check.
- Pipeline: `--train-character` filter FIXED (compared external char id
  ints to name strings — broken since birth, dropped every file).

## KNOWN-BROKEN / ENGINEERING QUEUE (in order)

1. **Incremental mmap embedding writer** — the unlock for the full
   4,461-game corpus (parse→embed→append per file). Currently: parsed
   frames alone (~50GB for 28M frames) exceed the 62GB box; cache is
   keyed by the full file set (no shard composition); mmap save needs
   materialized embeddings; dense temporal sequences cap the in-RAM
   path (stride is the lever — 900 files fit at stride 30).
2. **Streaming chunk-prep RAM leak** — chunk-size INDEPENDENT (40/100/
   300-file chunks all OOM); reproducible in minutes; unfixed.
3. Mask-plausibility sweep (g16): full-coverage SUBTLE scramble —
   in-support values across all opponent fields.
4. Behavior-eval protocol for generalist bots (shine metrics don't
   apply: stocks, action diversity, APM, approach rate).
5. Probe-validation: does rung-0 calibrate for scramble-trained
   policies?

## NEXT MOVES

1. **Human netplay session: g15 vs g10b at d4** — the crown decider
   (also doubles as more human corpus; +14 local games already banked
   from 08-08: eval_runs/0808_video_g15_local).
2. Incremental mmap writer → full-corpus fox_il_v2 → behavior evals.
3. g16 subtle-full mask (the g14×g15 sweet spot).
4. Unchanged from 08-06: retrained-trigger audit replant on a
   policy-read variable; W3 snippet pre-filtering.

## ARTIFACTS OF THE TWO DAYS

Checkpoints: `ms_g15_oppmask_full` (candidate) · `ms_g14_oppmask`
(dependence-floor specimen + prior stand record) · `ms_g13_oppmix`
(inversion specimen — never deploy) · `ms_g13b_oppmix` ·
`fox_il_v1_20260809_023217_best_policy.bin` (first full-game IL bot).
Corpus: `replays/fox_il_v1` (4,465 hardlinked Fox games, unfrozen-PS
filtered). Human games: 0807_ab_* (netplay A/B), 0807/0808_video_*
(local sessions incl. the chain-58 record game and the 12-13-chain
vs-Mewtwo games). Beginner artifact updated with the field chapter:
https://claude.ai/code/artifact/f1bab691-cd46-420d-a891-8246b8a0dca7

## ADDENDUM 2026-08-08 #6: incremental mmap corpus SHIPPED — fox_il_v2 unblocked

Engineering item #1 (the full-corpus unlock) is built, tested, and
running:

- **`ExPhil.Training.MmapCorpus`** (lib/exphil/training/mmap_corpus.ex,
  9 tests): incremental writer (parse→embed→append per file, headers +
  manifest patched per append = crash-safe/resumable), reader (labels
  packed 6 bytes/frame in RAM — the ~50GB frames-list problem was only
  ever about label lookup; embeddings stay on disk, one contiguous
  pread per window), and a batch stream emitting the same
  `%{states, actions, frame_weights}` maps as the lazy in-RAM path.
  Sequence windows are generated per file range — they never cross a
  game boundary (the in-RAM lazy path silently allows this).
- **`scripts/build_corpus.exs`**: resumable builder. Fixes a v1 data
  bug en route: v1 parsed player_port 1 ALWAYS, so every game with Fox
  on port 2 trained on the OPPONENT's controls; the builder picks the
  Fox port per file from metadata (`--character fox`, port recorded in
  the manifest).
- **`--corpus DIR` on train.exs** (Config + Pipeline corpus mode):
  skips parse/embed entirely, val split = whole-file holdout,
  button_pos_weight :auto served from packed labels. Temporal only.
- Validated end-to-end: 20-file smoke corpus (179k frames, 13s) →
  1-epoch GRU train → policy export, val 6.997.

Full build launched: `replays/fox_il_v1` → `cache/corpus/fox_v2`
(4,465 files @ ~1.5 files/s ≈ 50 min, ~41M frames / ~47GB — the 28M
estimate undercounted; log eval_runs/0808_build_corpus_fox_v2.log).
Next: fox_il_v2 = `mix run scripts/train.exs --corpus
cache/corpus/fox_v2 --backbone gru --window-size 60 --stride 30
--batch-size 64 --epochs 10 --name fox_il_v2`, then the
behavior-eval protocol item.

## ADDENDUM 2026-08-09 #7: fox_il_v2 TRAINED — full corpus, 49 minutes

The corpus paid off immediately. fox_il_v2 (GRU w60 s30 b64, 10 epochs,
`--corpus cache/corpus/fox_v2`): **val 5.687 → 5.374 MONOTONE, still
descending at epoch 10** (constant LR; more epochs are free — each is
~5 min). Whole-game holdout: 442 files / 2,211 val batches. Wall time
49 MINUTES for 10 epochs over 41.4M frames — the mmap batch path runs
~25ms/it at GPU 61%, disk is not the bottleneck. RAM flat.

Artifacts: `checkpoints/fox_il_v2_20260809_041730*` (best_policy.bin =
the deploy file; registry name double_jumping_pummel).

**v1-vs-v2 val numbers are NOT comparable** — the port audit shows only
38% of corpus games have Fox on port 1 (1663/831/840/1084 across ports
1-4), so v1's fixed-port parse trained on wrong-player or missing-player
data in 62% of its files AND validated on the same pollution. v2 is the
first clean full-game IL number.

Next on this track (unchanged from #6 but now unblocked): live behavior
eval of fox_il_v2_best_policy vs CPU (stocks / action diversity / APM —
shine metrics don't apply), then the generalist behavior-eval protocol.
Cheap follow-ups: rerun with more epochs (still descending), and the
personal-corpora fine-tune arms (G&W, Zelda, mewtwo dir) now that
build_corpus.exs makes any-size corpora one command.

## ADDENDUM 2026-08-09 #8: behavior eval — the argmax absorber, temperature is the deploy rule

First use of the generalist behavior protocol (new
`scripts/analyze_behavior.exs`: stocks/SD-vs-KO/damage/shield/breaks/
action-diversity/offstage, MEAN row per dir; reuses
Interp.ReplayStats.shield_stats). 3x120s vs level-1 CPU, n=3 per arm
(eval_runs/0809_fox_il_{v2_cpu,v1_cpu,v2_cpu_t03}):

| arm | survive | stocks lost (SD/KO) | dmg | actions | offstage% |
|---|---|---|---|---|---|
| v2 argmax | 33s | 4.0 (4.0/0) | 25 | 30 | 21.7 |
| v1 argmax | 70s | 3.7 (3.3/0.3) | 30 | 48 | 14.5 |
| **v2 t=0.3** | **123.7s (cap)** | **2.3 (0.7/1.7)** | **231** | **65** | **9.3** |

- **Argmax inverts the ranking** (v1 "beats" v2 deterministically —
  shield-camping with 9 shield breaks in its long game) and drives v2
  into a pure run-off-the-edge absorber: 12/12 deaths were SDs, 0.0%
  shielding. Same lesson as g6 at full-game scope: offline metrics and
  argmax evals both mislead.
- **Sampling escapes the absorber** (the 07-27 mechanism, confirmed at
  full-game scope): SDs 12/12 -> 2/7, survival x4, damage x9, live play
  shows grabs/lasers/rolls/side-B (Bradley eyeballed both arms live).
- **DEPLOY RULE: full-game IL policies never run argmax — temperature
  ~0.3.** Extends the 08-03 "collection runs need --temperature" rule.
- Fixture agreement 0.86 (v2) vs 0.66 (v1) was RIGHT about imitation
  quality but silent on closed-loop mode collapse — it is not a
  behavior metric.
- Remaining real gap: 0 stocks taken in all 9 games (percent without
  conversions) — the kill-conversion problem is the next training
  target, not a decode setting.
- inputs/min under sampling (~3400) is a decode artifact (per-frame
  flicker) — ignore that column across decode modes.
- Ops en route: post-reboot the CPU-target libexla lost its NIF
  (undefined cudaGetErrorString) — eval protocol needs
  XLA_TARGET_EVAL=cuda12 until rebuilt; melee_bridge.py now zeroes
  adapter ports (SIDevice2/3) for HEADLESS sessions but the protocol
  doesn't pass --headless yet, so bot evals still grab the GC adapter
  (blocked Bradley's Slippi session today — runner fix owed).

## ADDENDUM 2026-08-09 #9: libmelee_ex improvement arc — all six shipped

Six-item conceptual plan executed in ~/git/libmelee_ex (commits
ec96d91..d375a8c, suite 281 -> 299, 0 failures):

1. **steer_toward** — proportional diagonal cursor steering replaces the
   Python-ported axis-at-a-time bang-bang ("hand goes up, then over").
2. **declare_ports/2** — sessions declare the complete controller
   layout; undeclared ports unplugged, so bot Dolphins never claim the
   GC adapter (today's adapter fight, fixed at the source; the
   melee_bridge.py headless ini rewrite is now the interim shim for the
   python-bridge path only).
3. **Menu watchdog** — stuck?/2 + :on_stuck (once per stall episode);
   today's silent 9-minute login-screen wedge becomes a signal.
4. **Session invariants** — replay_dir implies save_replays (the 08-07
   no-fresh-replay regression class); "" connect-code hijack (GOTCHA
   #88) warned once per process.
5. **Melee.GameEvents** — semantic event stream (game start/end,
   stock_lost :sd|:ko at the <20% pre-death threshold the behavior eval
   validated, shield_break, menu transitions) — live self-scoring evals.
6. **Melee.SessionPool** — N parallel sessions, per-instance UDP
   spectator ports + homes; the eval-farm shape for n>=8 protocol rules.

Next wiring opportunity: ExPhil eval protocol on SessionPool +
GameEvents = parallel live-scored behavior evals (no post-hoc replay
parsing, n=8 in the wall time of n=2).

## ADDENDUM 2026-08-09 #10: pool behavior evals SHIPPED (SessionPool+GameEvents wiring)

- **ExPhil.Eval.LiveScorer**: Melee.GameEvents fold (SD-vs-KO, shield
  breaks, game boundaries) + per-frame accumulators — emits the exact
  analyze_behavior.exs row LIVE, no replay parsing.
- **scripts/eval_behavior_pool.exs**: N parallel headless sessions in
  one BEAM; per-instance UDP spectator ports, one warmed Agent per
  parallel worker amortized over its runs, fresh Dolphin per run.
  **4 runs x 30s scored in 1m37s** (n=8 ~ 3 min wall).
- **steer_toward live bug found+fixed en route** (libmelee_ex bd7ab35):
  euclidean normalization split the 0.22 fine tilt to ~0.156/axis =
  inside the analog deadzone -> hand froze outside tolerance, stage
  select never completed (3/4 sessions, ~170k menu frames). Chebyshev
  (dominant-axis) normalization fixes it. Lesson: write-assertion tests
  can't catch closed-loop convergence; first live consumer found it in
  minutes. The #3 menu watchdog would have flagged it — wiring
  :on_stuck into MeleePort is now queued.
- **REGIME CAVEAT (do not mix numbers)**: pool = sync blocking-input
  loop; the 0809 baseline = async runner at realtime. Different
  effective delay; v2 (delay-untrained) shows regime-sized behavior
  deltas (pool t0.3: SD-heavy, 0 dmg in 30s vs async t0.3: 2/7 SD,
  231 dmg in 120s). Compare pool-vs-pool; --emulation-speed 1.0
  approximates the async regime. Open question: which regime predicts
  human-session behavior (the g10b lesson says test at the deploy rung).

## ADDENDUM 2026-08-09 #11: audit round 3 — label-flip poisoning does NOT take (the replant negative)

The owed Stage-4b replant ran on OWN-Y (the proven-read variable; band
drawn from the pool's y-quantiles, in-band mass preregistered 5-15% —
both round-2 failure modes designed away). Champion recipe + sealed
poison, 60 epochs, prev-dropout 1.0 (checkpoints/audit_planted3.bin,
eval_runs/0809_audit3.log):

- **EFFECT FAIL / COMPETENCE PASS** — replicated on two pool replays
  (491 and 495 in-band states, ~14% mass: the mass prereg was met and
  measured, unlike round 2's ~2%).
- First check attempt scored 0 in-band states: the DEFAULT check replay
  (stand multishine) never visits the pool-drawn band — check on a
  POOL-representative replay (0802_d2pool). Measurement-site
  distribution matters; gate fixed by --replay choice.
- **Round-3 conclusion: the champion drill recipe absorbs 5-15%
  label-flip poison WITHOUT installing a >=2-logit trigger, even on a
  variable the policy demonstrably reads.** With round 2 (unread
  variable, ~2%) this is now a two-point resistance result — relevant
  beyond interp: the training corpus is public HF data anyone could
  poison, and this recipe class shrugs off band-suppression label
  flips at these masses.
- The blind-audit instruments (audit_game_audit2.exs, contract widened
  to the two-variable menu) STILL have never received a valid target.
  Next-round plant ideas (recorded, not run): flip labels to PRESS
  (installing a positive action may be easier than an absence),
  raise mass past 20%, or plant during fine-tune rather than
  from-scratch retrain.
- Stand control produced a truncated/unparseable replay
  (instrumentation gap, competence covered by the sealed gate).

## ADDENDUM 2026-08-09 #12: THE CROWN DECIDER — g15 CROWNED

The gated human netplay session ran (7 games, Bradley live via Direct,
d4, blind A/B, eval_runs/0809_ab_decider — sealed_order.txt has the
blinding record):

| g | policy | stage | opp | shines/min | strict chain |
|---|---|---|---|---|---|
| 1 | g15 | PS | Mewtwo | 33.7 | 3 |
| 2 | g10b | YS | Mewtwo | 43.4 | 2 |
| 3 | g10b | FoD | Mewtwo | 61.1 | 2 |
| 4 | g15 | BF | Fox | 35.8 | 1 |
| 5 | g10b | FD | Fox | 29.3 | 2 |
| 6 | **g15** | DL | Fox | **86.2** | **46** |
| 7 | **g15** | FoD | Fox | **69.8** | **6** (replication) |

- **NEW PRODUCTION POLICY: `checkpoints/ms_g15_oppmask_full.bin`**,
  deploy `--frame-delay 4` netplay / d3 local. g15 chains {3,1,46,6} vs
  g10b {2,2,2} at the deploy rung; chain 46 is the ALL-TIME NETPLAY
  RECORD (old: 4; local record 58 stands), replicated >=4 with chain 6.
  Timing verified every game (qtrace lag peak 6 @ 98.7-99.6%).
- Stages were UNCONTROLLED (Direct random: PS/YS/FoD/BF/FD/DL/FoD) —
  the 46 came on DREAMLAND, i.e. through platforms. Mask training
  appears to have bought platform-stage robustness at the deploy rung
  too. FD appeared once (g10b, chain 2).
- Session bugs found live: analyze_shine_source wrong-port artifact
  AGAIN (bot lands port 1 OR 2 by connect order; briefly reported the
  whole session as 0 shines — task #20 autodetect); netplay CSS picks
  characters by RANDOM-REROLL (~40 presses; task #19 direct portrait
  selection); Direct stage selection unbuilt (task #18).
- g15 caveats that stay open: rung-0 4.28 scramble-probe calibration;
  YS n>=8 bucket owed. Neither gates the crown (the 08-08 gate was
  exactly this session).
- +7 netplay human games banked for the corpus.
