# HANDOFF 2026-08-02 — resume point

Supersedes the "next moves" of HANDOFF_2026-07-31.md (which remains the
record of the SS-on-queue breakthrough, the three live-netplay bug fixes,
the Direct loopback verification, and the Hyprland-crash postmortem).
Everything through `c32c958`+ is on origin/main. Nothing is running on
the machine at handoff time; `claude-noidle.service` (suspend inhibitor)
IS active — stop with `systemctl --user stop claude-noidle` if unwanted.

## State of play (one paragraph)

`ms_g2_mdq_ss` (Fox multishine, SS-on-queue) is verified over Direct
loopback — sharp 5f latency at `--frame-delay 3`, real multishining,
launch recipe + between-games analyzer protocol in the
HANDOFF_2026-07-31 ops section. The Friday friend session (ACAB#182)
NEVER HAPPENED — Hyprland plugin crash killed the session tree at launch
(postmortem in 07-31 handoff); **ACAB#182 is still owed their match**,
and the RC-hardening rules (tmux session, systemd-run inhibitor, nohup
bots) are now documented and partially in place.

## Today's work: Mewtwo SS-on-queue arm (mc_g1_mdq_ss)

Trained `checkpoints/mc_g1_mdq_ss.bin` — `--expert mewtwo_combo`
(composite approach→fair→knockdown→tech-chase), full grind-2 recipe
(multi-delay {2,3,4} + pipeline-offset 2 + queue-depth 4 + delay-id +
`--scheduled-sampling 0.5 --ss-ramp 10`), 130,847 fixture frames × 3
delay pools, 60 epochs, best loss 0.58. Survived an OOM (VRAM 0.25 too
small for the SS graph's 4 extra forwards — use **0.7** for queue-SS
training) and a compositor-crash interruption via `--resume` from
trainer snapshots (worked flawlessly twice).

Eval vs stand dummy, sync headless (scoreboard:
`scripts/trace_mewtwo_fair.exs`, NOT the shine table):

| policy | delay | fairs | landed | L-cancel | jumps |
|---|---|---|---|---|---|
| mc_g1_mdq_ss | d3 (3 runs, identical) | 3 | 3 | 1/3 | 15 |
| mc_g1_mdq_ss | d2 | 2 | 2 | 0/2 | 19 |
| mewtwo_combo_poolgrow_r1 (July champion) | d1 | 4 | 4 | 4/4 | 13 |

Verdict: activity is lineage-normal at d3 (champion parity, two rungs
deeper), L-cancel possibly degraded (tiny sample), and **the eval is
blind to the policy's real job** — a stand dummy never gets knocked
down, so the tech-chase/conversion half is untested. Netplay-safe as a
curiosity; NOT demo-grade on this evidence. Fox remains the showpiece.

## Next moves (in order)

1. **Conversion eval for mc_g1_mdq_ss** — vs `--dummy tech_random`,
   score with the conversion metric (July's 36.4% protocol; see
   `scripts/trace_tech_chase.exs` + drill docs). This is the number
   that decides whether SS-on-queue GENERALIZED (kept/improved the
   punish cycle under delay) or merely didn't break anything.
2. **The ACAB#182 rematch** (Fox multishine, FD): launch recipe in
   07-31 ops. Checklist: session in tmux; `claude-noidle` active;
   Bradley's dotfiles plugin update (Hyprspace/hyprexpo/hyprsplit —
   the crash suspects) ideally first; bot launches AFTER the friend
   searches only if on the SAME machine as a launcher (moot remotely);
   analyze game 1 with `scripts/analyze_qtrace.exs`, shift rung to
   (peak − 2) if the peak isn't 5.
3. **DAgger-vs-live-humans** for the Fox fight states (hold-B under
   pressure — the qtrace'd Direct games in
   eval_runs/direct_rematch_20260801.log are collection targets).
4. **d2 inversion mystery** (mdq_ss weakest at d2, 139.8 c6, inverted
   slope) + the delay-id interp patch experiment (play at d2 with id 3
   / jq at d3 with id 2-4) — doubles as the jq-d3-collapse diagnosis.
5. Parked: Mewtwo transfer at scale, PPO under delay, forward-model
   compensation.

## Ops facts worth re-reading

- **VRAM fraction for queue-SS training: 0.7** (0.25 OOMs at ~epoch 13).
- NO-MIX beside any live beam; second EXLA client kills training beams.
- pgrep/pkill self-match: patterns like "dagger_drill" match your own
  wrapper shell — scope kills by "libmelee_" for bot Dolphins, and
  don't trust `pgrep -f <pattern>` liveness checks run from a shell
  whose own cmdline contains the pattern.
- Checkpoints on B2: ms_g2_mdq_ss + g1 arms synced 08-01,
  mc_g1_mdq_ss synced 08-02
  (`rclone copy checkpoints/ b2:exphil-artifacts/checkpoints/`).
- Headless-vs-windowed gap is real: 381/min c367 headless vs 105 c19
  windowed for the same Fox checkpoint — live behaves like windowed.
