# HANDOFF 2026-07-22 (morning)

Supersedes HANDOFF_2026-07-21.md. Companion reading:
INTERP_NEXT_RESEARCH_2026-07-20.md (adopted research queue),
docs/research/AGENT_SURVEYS_2026-07-20.md (verbatim lit surveys),
edifice/INTERP_AUDIT_2026-07-15.md (remediation ledger).

## RUNNING STATE — NOTHING TRAINING (r16 DIED, needs relaunch)

**CAUSE FOUND 2026-07-22 (supersedes the SIGKILL/OOM theory below): r16
did NOT OOM — it crashed with a deterministic Elixir error at the
epoch-10 probe-eval.** The drill log's last lines (overlooked on first
read) are a full stack trace:

    ** (ArgumentError) invalid dimension in axis 0 ... got 0 in shape {0}
        lib/exphil/interp/probe.ex:252: Probe.mask_rows/2
        lib/exphil/interp/training_probes.ex:101: TrainingProbes.eval/5

A probe feature had ZERO labeled rows on one side of the positional
75/25 split (the BC archive frames, appended at 10.1% of pool, pushed
some feature's rows entirely out of the eval slice), and mask_rows'
"empty" fallback itself built an illegal {0}-shaped tensor. Confirming
evidence: exit status 1 (not 137), journal has NO kernel-OOM and no
systemd-oomd event at 03:05. Relaunch-as-was would have died at epoch
10 again, deterministically. FIXED 2026-07-22: probe empty-split guard
(nil result, regression test), snapshot now saved BEFORE probe-eval,
--preflight dress-rehearsal mode + memory ledger + launcher exit-code
logging (see SHIPPED). Separately noted: kernel OOM-killed GNOME's gjs
at 54 GB RSS on Jul 21 13:52 — a desktop leak that competes for RAM on
overnight runs; a real OOM DOES log to journalctl -k on this box.

- ~~Clean SIGKILL — no Elixir error, log just stops mid-epoch~~ (WRONG:
  the error was in the log tail all along). NO trainer snapshot survived
  (died before the epoch-10 save — ordering now fixed), so relaunch =
  FROM SCRATCH.
- GPU idle. A stray Phoenix beam (~/git/shine, 472967) is Bradley's —
  the #67 guard now ignores it (cwd-scoped).

**MONITORING LESSON (why the death went 7h undetected):** the Monitor
watched the DRILL log for error STRINGS (FATAL/diverged/NaN) — and the
actual death line, `** (ArgumentError)`, matched none of them. Correct
monitor for a training run = LIVENESS + HEARTBEAT (pid alive AND log
mtime advancing within N min) — that catches every death class,
including crashes whose message you didn't predict AND true silent
kills. Also: repeated reaping of my `while kill -0` watchers was a real
signal I rationalized away. GOTCHA candidate.

## r16 RELAUNCH PLAN (updated 2026-07-22 after root cause)

The experiment: mamba_2, 100ep, full r15 recipe (conversion 3.0 /
probe-reg 0.1-every5 / probe-eval 10) + **archive-human BC**. THE test:
does direct human BC move gate-10 (initiation)? The old (a)/(b) memory
options are moot — the death was the probe empty-split crash, now fixed
and regression-tested. Relaunch UNCHANGED (same --bc-sample 20; memory
was never shown to be a problem) with the new guards:
  NEWERA8_PREFLIGHT=1  — dress-rehearses the exact pool/flags (incl.
  probe-eval, the killer) in ~15 min before committing the epochs, and
  records the big-pool memory-ledger point.
  Monitor = liveness + heartbeat (pid alive AND drill-log mtime
  advancing), not error-grep.
Ledger caveat: until the r16 preflight lands its 2M-frame point, the
model has only the 1,679-frame smoke anchor — single-point proportional
extrapolation over-predicts wildly, so expect a spurious memory-check
WARN on the first big run (default mode proceeds; don't use
--memory-check strict until a big-pool point exists).

Launch cmd (as it was, env on overnight_newera8.sh):
  NEWERA8_TAG=r16 NEWERA8_BACKBONE=mamba_2 NEWERA8_MAX_EPOCHS=100
  NEWERA8_DROPOUT=0.6 PROBE_STATEFUL=0 NEWERA8_CONVERSION_WEIGHT=3.0
  NEWERA8_PROBE_REG=0.1 NEWERA8_PROBE_REG_EVERY=5 NEWERA8_PROBE_EVAL=10
  NEWERA8_BC_REPLAYS='corpus/archive/mewtwo/*.slp' NEWERA8_BC_SAMPLE=20
  NEWERA8_EXTRA_ROLLOUTS=<2 demo + 6 r14 probes>

## r15 RESULT (DONE 2026-07-21, mamba_2, full recipe, 100ep, mainline)

- Card **8.3/10**, export loss 0.084, rounds/day 7.0.
- **Conversions 7** (per game 1,1,1,0,3,1; best 25%) — UP from
  mamba2_screen 4 and mamba_full 2. Recipe helps the bot CAPITALIZE.
- **Gate 10 (armed approaches/min) ~0** — converts better, still does
  NOT initiate in neutral. Conversion-weighting can only upweight
  approaches that HAPPENED. → r16's BC targets exactly this.
- Calibrated deficit report: r15 representation ≈ screen (opp_kd 0.889),
  HEALTHY — probe-reg did not over-suppress; flat in-training curves were
  the trend-tool under-reading. Representation was formed by epoch 30;
  the recipe changed BEHAVIOR not KNOWING = the acting-not-knowing
  bottleneck restated.
- Exports: checkpoints/mewtwo_combo_newera_r15_{r13_policy,best_policy}.bin

## DECISIONS LOCKED (Bradley, 2026-07-22)

1. **r16 = BC + #38 combined** (BC built + launched; #38 deferred to r17,
   see blocker). r16 currently BC-only after the #38 blocker.
2. **Netplay SHELVED** until the bot plays better.
3. **INVEST in stateful mamba-2 step** — long-term goal is a
   multi-architecture x multi-character LADDER, every arch optimized
   (mamba-2 the current front-runner, but MEASURE the others, don't
   assume). Stateful step = the 60fps/rollback path.
4. **PPO smoke — run it** (repaired; novel mamba-2 PPO).
5. **Interp — push to where it unlocks NEW analysis.**
6. Arch bake-off broadened LATER (after Mewtwo pipeline solid on mamba-2).
7. r16 recipe: BC + #38 combined; archive-BC now, #38 as r17 once seeds
   finalize.

## #38 STATUS — FINALIZE FIX WRITTEN (needs one live test), r17

- gen_scenario_manifest.exs works (scans FD-Mewtwo(p1)-vs-Fox(p2) for
  punish situations; 24 probe replays -> 120 candidates: 83
  opponent_behind, 36 tech_chase, 1 edgeguard). scenario_suite
  prefix-replays with drift 0.0 and saves the bot's play as .slp.
- **BLOCKER (was):** scenario_suite STOPPED each game mid-play (after the
  response window) -> Dolphin never hit GAME! -> .slp never finalized
  (Slippi writes raw-data length at game-end) -> ALL seeds failed Peppi
  parse. Now GOTCHA #73.
- **FIX WRITTEN 2026-07-22 (scenario_suite.exs `--finalize`):** after the
  scored window, a new `:finalize` phase drives P1 off-stage (`@suicide`
  = main-stick full-left, P2 neutral) to force GAME! so Slippi closes the
  .slp; capped by `@finalize_step_limit`. Eval path unchanged when the
  flag is off (score still comes from the window obs). **UNTESTED** —
  written while r16 held the GPU (no mix). FIRST TEST once mix is free:
  run one scenario with `--finalize`, confirm the .slp parses under Peppi
  AND that Mewtwo actually SDs from center on FD with that input (if a
  float/DJ saves it, adjust @suicide — add a down-hold / walk from a known
  x). Manifest + loop wiring (NEWERA8_SEED_ROLLOUTS_DIR) READY.
- DESIGN INSIGHT (keep): the EXPERT is the initiation bottleneck —
  seeding + expert-relabel can't teach going-in (teacher can't). So
  BC=initiation (bypasses expert), #38=conversion reinforcement
  (expert-competent). This is WHY r15 raised conversions but not gate-10.

## r17 PLAN (draft 2026-07-22, pending r16's gate-10 verdict)

r16's result gates the branch. r16 tests: does archive-human BC move
gate-10 (initiation)? Two outcomes:
- **BC moved gate-10 (>0 armed approaches/min):** initiation is learnable
  from human BC. r17 = BC + **#38 conversion seeds combined** (decision
  #1's original intent) — keep the BC that unlocked going-in, add
  expert-relabeled scenario seeds to reinforce the punish once in.
  Prereq: land the #38 `--finalize` test (above) so seeds parse; generate
  the seed dir; launch with NEWERA8_SEED_ROLLOUTS_DIR set.
- **BC did NOT move gate-10:** initiation isn't coming from more
  imitation data — escalate. Candidates, in order: (a) heavier BC weight
  / BC-only warm-start before the drill; (b) a reward/exploration signal
  for approaching (the acting-not-knowing bottleneck is a BEHAVIOR gap,
  so a small approach-shaping reward or PPO fine-tune on top of the BC
  policy is the natural next lever — PPO is repaired + unrun); (c)
  revisit whether gate-10's detector is even firing on the eval fan-out.

Independent of the branch, r17-adjacent build items (decisions #3/#4):
- **stateful mamba-2 step** (NEXT BUILD, decision #3): O(1) recurrent step
  for the 60fps/rollback path + the multi-arch ladder. Biggest structural
  investment; unblocks netplay-quality inference later.
- **PPO smoke** (decision #4): run the repaired mamba-2 PPO demo (r15
  policy, cpu3, 20k steps) — cheap, proves the inner loop, and is a
  prerequisite for outcome-(b) above.

Operational prereqs now in place for any r17 launch: `--preflight` gate,
memory ledger (now has a real 2M-frame mamba-2 point), `train_watchdog.sh`
liveness monitor, launcher exit-code logging, per-epoch heartbeat.

## SHIPPED 2026-07-21/22 (all committed + pushed unless noted)

exphil (a6bbc89..8531f64):
- r15 recipe + eval infra (0a01c9e): ConversionSampling, ProbeRegularizer,
  StyleCard, MewtwoPunishExpert, Eval.Elo + PolicyOpponent + checkpoint
  ladder, gate-10 approach detection, openings/min.
- P5 DeficitReport + P4 tech-reaction curves + TrainingProbes
  (--probe-eval-every) — interp instruments; first cross-arch scorecard
  (mamba-2 beats GRU on every non-saturated feature).
- Nx.block migration Elixir half (edifice 75bb851); dead XLASelectiveScan
  deleted; Activations delegates capture to edifice Capture.
- SSD scan bench + train-step profiler: scan is NOT the bottleneck (seq
  8.9ms/step < mamba-1 10.8); r15's 2x epoch cost = the RECIPE (probe-reg
  2nd trunk fwd+bwd, not CSE'd). No NIF justified.
- PPO inner loop repaired (93dc4e5): one EXLA-jitted update_step_fn.
  UNRUN — demo queued.
- Netplay #9 (f931633): mainline-format bot home fixes the ScheduleEvent
  crash; reaches Direct dial screen; matchmaking handshake UNPROVEN;
  EXPHIL_GFX=Null-applies check UNVERIFIED. SHELVED.
- B2 corpus (c27fab2): pull_replays.sh; exphil-replays-blewfargs reached
  (mewtwo 129 slp pulled to corpus/archive/mewtwo; greg 3446 slp/12.5GiB
  BY CHARACTER = five-char corpus). exphil-artifacts too.
- r16 BC ingestion (5a67fc5): --bc-replays/--bc-sample, out of expert
  table, empty-filter, fingerprint. #38 generator (9f9288b). Loop wiring
  + corrupt-replay skip (38dd41d).
- r16 post-mortem hardening (2026-07-22, committed 3fb521f on main, NOT
  pushed): root cause found (probe empty-split crash, NOT OOM — see
  RUNNING STATE). Probe.mask_rows/mask_to_indices empty guards +
  regression tests (probe_empty_split_test.exs); Training.MemoryLedger
  (VmHWM bytes-per-frame scaling model, logs/memory_ledger.jsonl, predict
  + headroom_check w/ 1.2x margin, 19 tests); dagger_drill --preflight
  (full-pool dress rehearsal: embed -> 1 JIT step -> probe-reg refit ->
  probe-eval -> export+snapshot -> ledger point, exit 0/1) +
  --memory-check warn|strict|off (pre-embed refusal gate) + per-epoch
  heartbeat+rss line (was every-5 — GOTCHA #72) + snapshot REORDERED
  before probe-eval (death at eval now costs 0 epochs, not 10);
  overnight_newera8.sh: describe_rc exit-status decoding +
  NEWERA8_PREFLIGHT=1 gate; fixed latent wrong-namespace
  ProbeRegularizer call (crashed --probe-eval-every WITHOUT --probe-reg,
  e.g. gru). VALIDATED end-to-end on the live r16 relaunch: preflight
  passed (real peak 19.9 GB), epoch-10 probe-eval boundary cleared.
- Uncommitted follow-ups (2026-07-22, NOT yet committed): GOTCHAS #71-#77
  (probe empty-split, liveness+heartbeat monitoring, scenario finalize,
  mainline-home, pgrep self-match, Axon system_time seed, microbench vs
  full-graph); scripts/train_watchdog.sh (pid + GPU/log liveness monitor,
  desktop notify on death, tested against live r16); scenario_suite.exs
  --finalize (#38 seed .slp finalization, UNTESTED — see #38 STATUS).

edifice (through fb47bde): remediation phase 2 (SAETrainer warm-start +
finetune + minibatch + deterministic-fit; Diffing; quarantine markers);
multi-site Capture (probe_tap/build_probe/Capture); h_t site (scan/output
split) + SSMAnalysis (hidden-attention maps, timescale map); scalar-gain
Steering.gain. Full stale gate green (5379 tests).

## OPEN TASKS (CLI task list GONE — MCP dropped; rebuild from here)

| # | status | task |
|---|--------|------|
| r16 | **RUNNING** (relaunched, ep30+/100, healthy; watchdog live) | BC+recipe mamba_2 — the initiation test |
| #38 | finalize fix WRITTEN, untested | scenario_suite --finalize; test .slp parses (GOTCHA #73) |
| stateful-mamba2 | **NEXT BUILD** | O(1) step for mamba-2; ladder + 60fps path |
| PPO | repaired, UNRUN | smoke demo queued (r15 policy, cpu3, 20k steps) |
| interp | push | stage-diff -> crosscoder -> attribution graphs |
| #9 | SHELVED | netplay: home-fix done, handoff + Null-check open |
| #13 | Elixir done | Nx.block native FFI half gated on need (profiler: none) |
| #15 | mostly done | remaining: CLT redesign, JumpReLU decision |
| #19 | operational | full 4-way ladder (r13/r14/screen/r15) not yet run |
| #20 | in progress | epoch-budget curves land per run; recipe-cost known |
| #23 | pending | G&W track (greg corpus now available) |
| #30 | seeded+archive | human_blewf (own) + archive/mewtwo (pulled) + greg |
| #33 | corpus ready | five-char program: greg has all chars |
| bake-off | later | broaden arch screen after mewtwo solid |

Parking lot: upstream drafts await Bradley (DO NOT POST); ladder full
round; gate-10/StyleCard calibration vs corpus.

## GOTCHA CANDIDATES — WRITTEN 2026-07-22 as GOTCHAS.md #71-#77

(#71 probe empty-split, #72 liveness+heartbeat+exit-status, #73 scenario
finalize, #74 mainline-home, #75 pgrep self-match, #76 Axon system_time
seed, #77 microbench vs full-graph. Originals kept below for context.)

- **Training-run monitoring must be liveness+heartbeat, not error-grep.**
  Check: pid alive AND log mtime advancing within N min. r16 died at
  epoch 10, undetected 7h, because the Monitor grepped for FATAL/
  diverged/NaN — and the actual death line, `** (ArgumentError)`,
  matched none of them. Grep-based monitors miss BOTH silent kills
  (SIGKILL writes nothing) AND crashes whose message you didn't
  predict; liveness+heartbeat catches every death class. Corollary:
  before theorizing about a death, READ THE LOG TAIL — the r16 stack
  trace sat in the drill log for 7h while an OOM theory was built on
  "the log just stops."
- **Launcher must log the trainer's exit status** (now does): 137 =
  SIGKILL/OOM-killer, 139 = SIGSEGV/NIF-relink, else = crash with a
  stack trace in the log. r16's "no checkpoint" message hid exit 1 —
  one echo would have prevented the misdiagnosis. A real OOM kill DOES
  log to `journalctl -k` on this box (proof: gjs killed at 54 GB RSS,
  Jul 21 13:52).
- **scenario_suite .slp don't finalize** — it stops games mid-play, so
  Slippi never writes the raw-data length; the .slp fail Peppi parse
  even on clean completion. #38 seeds need play-to-game-end.
- **Mainline Slippi needs a MAINLINE-format home** (netplay-beta layout,
  no `ishiiruka` marker). Feeding the Ishiiruka home crashes at
  online-session start (CoreTiming ScheduleEvent nullptr).
- **pgrep -f "<pat>" self-matches the running shell** when the pattern is
  in your own argv — killed 2 launches; also makes "drill running"
  false-positive. Verify with `ps -eo pid,comm`; never combine a
  pgrep-kill with the thing you're launching.
- **Axon.build seeds param init from system_time()** — pass :seed for
  reproducible fits (bit edifice SAETrainer).
- **Microbench fusion != full-graph fusion** — two block benches
  misattributed the r15 slowdown; only the real-graph profiler ranked it.
