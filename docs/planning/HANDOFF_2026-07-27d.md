# HANDOFF 2026-07-27d — the seed farm corrects the record: escape is a coin flip, not a guarantee. Supersedes HANDOFF_2026-07-27c.md.

Evening session, autonomous while user at dinner. Everything on `main`.
Evidence: [EXPOSURE_BIAS.md](EXPOSURE_BIAS.md) items 8 (seed-farm
correction + queue-1 answer) and 9 (rescue n=2), all new tonight. Raw
logs: `eval_runs/evening_farm_20260727.log`, `eval_runs/0727_crouch_*`.

## READ FIRST — rules (updated tonight)

1. **≥3 seeds is NOT enough to call a seed-variant property replicated.**
   Item 8's "escape replicates 3/3" died at n=8 (now 4/8): p(3/3) under a
   fair coin is 1/8. For any effect that varies by init, report the RATE
   and check the failure MODE with forensics before claiming a method
   property. (The mode check is what saved the claim's core tonight: all
   4 failures are crouch-absorbed, so coverage still ~doubles escape
   odds vs 0/3 without — the correction is quantitative, not a reversal.)
2. All of c's rules stand: ≥3 runs/policy, staleness is per-run health
   (reboot before blocks; tonight's farm ran 0.3-1.4% throughout),
   `mix` hazard during loops, Output→STDERR, opponent is part of the
   input distribution, ~20% SD flake (cost r1 of the 180s block).

## What tonight established

1. **Escape rate of the crouch recipe: 4/8 seeds** (a,b,c,d yes;
   e,f,g,h no — all four failures crouch-absorbed 57-78% occupancy
   despite the crouch data; g absorbs at f104 and never shines once).
   Coverage biases the init lottery toward the escape solution; it does
   not delete the basin. ms_crouch_a remains champion (no new seed
   above chains 3).
2. **The sustain ceiling is intrinsic, not window clipping** (queue #1
   closed): 3x180s on seed a → 117/101 self/min, max chain 12 across
   ~180 chains/run, breaks 100% unforced. The 60s 19-22s were tail
   draws. Also first nonzero LEDGE occupancy (5.4% in one 180s run) —
   the ledge valley is real but rare; ledge-drill direction stands.
3. **Sampling-rescue replicates, pre-registered** (item 9 n=2): dead
   seed g at T=0.5 → 61.5-78.4/min, chains 3-4. Membrane leak is a
   class property of absorbed policies.

## Farm round 2 (completed same evening)

Seeds i-l: i and k escape (66-81/min, chains 3-4), j and l fail (both
crouch-absorbed: 73.8% / 62.9%). **Escape rate at n=12: 6/12 — exactly
50%.** No new sustainer; a's chains 19-22 are ~1/12 rare. Seed l is a
new failure variant: repeated ~300f absorbed spells with escapes
between, same RLE entry route every time (see item 8). Log:
`eval_runs/evening_farm2_20260727.log`.

## Non-research assets from today

- Video: `~/Videos/ms_crouch_a_vs_lvl1cpu_final.mp4` (full match vs
  lvl-1 CPU) + 23 MB share cut. Recording recipe (Hyprland/wf-recorder/
  window-class gotchas) in agent memory + committed nowhere — machine
  local.
- Research note: `docs/research/COACHABLE_AGENTS_STYLE_REWARDS_2026-07-27.md`
  (Sony style-conditioned UVFA; descriptor-conditioned BC is the
  laptop-feasible experiment; Cat-RAC for the PPO phase).

## Queue, by leverage (rewritten)

1. **Contrastive init forensics** — we now have clean pairs on identical
   data: d (escapes, chains 2) vs e/h (absorbed) vs a (escapes, chains
   22). Open problem: what does a winning init learn? Needs tooling
   beyond replay forensics (weight/representation probes on the .bin —
   attribution over the crouch-window inputs is the cheap first probe).
2. **Escape-rate estimate** — DONE: 6/12 at n=12. Operational cost:
   train 2-3 seeds, keep any escapee; farm ~12+ to hunt a sustainer.
3. **SS x crouch composition** — still untested; NOTE the 4/8 base rate
   means 3 seeds cannot distinguish composition from luck. Needs ~6+
   seeds per arm now; consider only if a cheap hypothesis emerges.
4. **T sweep / adaptive T on g** (0.3 / 1.0; sample-only-when-pooled) —
   cheap, extends item 9; adaptive T is an inference-time absorber
   detector that would compose with ANY policy.
5. **Mewtwo transfer of coverage synthesis** (unchanged).
6. **Ledge drill** (unchanged; +180s occupancy motivation).
7. **DAgger→convergence / PPO on GPU** (unchanged; read Cat-RAC first).

## Open problems (updated)

1. What breaks chains at ~12-22? Still 100% unforced, still resolving
   through air-reflector states. 180s ruled out clipping; remaining
   candidates: window capacity (16f), architecture, label noise at the
   JC boundary. Contrastive probe (queue #1) is the live approach.
2. Why do ~half of inits lose to the basin despite coverage? (Refines
   c's open problem 2.) The absorbed solutions are not all identical —
   g never shines, e/h shine a handful of times first, f survives 17s.
3. c's open problems 3-5 (90/min ghost, delay mismatch, SD flake)
   unchanged.
