# HANDOFF 2026-08-03 — the checklist marathon (resume point)

Supersedes HANDOFF_2026-08-02's next-moves. Everything through `4ddceac`
is pushed. Task-list-driven session; statuses live in the session task
tracker, details in LATENCY_ARCHITECTURE / INTERP_ROADMAP /
INIT_FORENSICS (all updated in place).

## Closed today (with verdicts)

1. **Delay-id patch probe**: jq's d3 collapse = channel-triggered
   (id=4 at real d3 → 431/min c430); mdq_ss d2 weakness = dynamics-side.
2. **Early-reject probes DEFAULT-ON** (+ CycleMargins lib, reject-at/-on,
   margin-select export, auto-detected dead-seed entries) — the
   anti-seed-farm kit, all landed and smoke-verified.
3. **P4 milestone: NOBODY READS TECHS** (584 episodes, 4 policies, flat
   offset curves) — conversion is coverage; curve = acceptance test for
   reaction training.
4. **Windowed-vs-headless gap: NOT REAL on current code** (10 identical
   380.5 c367 runs across windowed/lock/DPMS/load) — 08-01's 105 c19 was
   the pre-2bd9577 re-inference bug. Fast headless recipe validated:
   `--emulation-speed 0 --blocking-input` = record at half wall time.
5. **d2 inversion saga**: jitter refuted (grind-3 flattens everything);
   source-distribution refuted (grind-4; side-prize ms_g4_d2mix = NEW d3
   RECORD 424.4 c423); then grind-5 ({2,4,6,8}) SHATTERED the d2 pin —
   205.8 c73 — implicating RUNG SPACING (adjacent shifts interfere).
   Decisive A/B queued (task #18): multi-delay "2,4" vs "2,3".
6. **Delay ladder verdict**: SS-on-queue does NOT ladder past d4
   (d6/d8 metronomes). Netplay plan = d4 (task #12: 18 frames rejected;
   measure real friend ping via qtrace when a remote session happens).
7. **#36 probe-as-regularizer**: stale entry closed (ran in r15).

## Shipped, pending verdicts

- **CycleSim** (offline full-cycle simulator): built + trace/decode_lag
  instruments; gate (z-vs-zz) OPEN — next lever = replicate agent-path
  hysteresis decode (task #5 has the exact resume).
- **--stateful-resync N** (hybrid stateful deploy): shipped, 114 tests
  green; A/B blocked — ms_open_z's era numbers don't reproduce
  post-2bd9577; rerun on the current champion (task #11).
- **SAE v1**: GRU r13 has crisp shield/offstage features, knockdown
  DEFICIT (P4 at feature level); Mamba fit collapsed under shared
  hyperparams (task #14 v2 list).
- **All-timesteps trunk capture** (`load_trunk(all_timesteps: true)`) —
  merged, tested; serves the logit-lens tier.

## Ops learned tonight (all in GOTCHAS/rules)

- #82 SIGTERM mid-test strands rustler; `EXPHIL_SKIP_NIF_COMPILE=1`.
- #83 midnight nix-gc ate the un-rooted appimage-run path (evals died
  127); wrapper now GC-rooted via `nix build -o`.
- `&&/and` precedence bomb in probe gating (fixed both scripts).
- Collection runs MUST pass `--temperature` (deterministic harness =
  N identical replays).
- hyprlock wedged after lock-arm experiments (Bradley ran `lockfix`);
  no windowed evals under lock without a reachable tty.
- Couch session vs Bradley captured (qtrace + replays) → #8 targets;
  his observation: policy has two modes vs humans (sparse shines vs
  entering the multishine loop) — basin-entry gating is the question.

## Checkpoint inventory delta (all on B2)

ms_g3_jq_ss (jitter refutation), **ms_g4_d2mix (d3 RECORD 424.4
c423)**, ms_g5_ladder8 (d2 unpin evidence, 205.8 c73).

## Next moves (in order)

1. Task #18 spacing A/B ("2,4" vs "2,3") — one causal arm from a
   champion-recipe change; the d2 prize is real.
2. Task #5 hysteresis decode in CycleSim — one named lever from a
   trusted offline evaluator.
3. Task #11 A/B on ms_g2_mdq_ss (verify stateful+queue compat first).
4. Task #8 fight-state cycle (targets collected, deficit instrument
   exists via SAE/probes).
5. Bradley-gated: remote-friend qtrace ping measurement (#12), ACAB#182
   rematch (recipe unchanged, regime fears retired).
