# Session Handoff — 2026-07-15 (melee bot + interp)

Successor to `HANDOFF_2026-07-08.md` (kept for archaeology; its addenda tell
the 07-12→07-15 story blow by blow). The experiment ledger of record is
`INTERP_ROADMAP.md`'s experiment log.

## TASK LIST SNAPSHOT (rebuilt 07-15 ~14:30 after the old list was wiped)

The old list (#20, 21, 24, 27, 28, 29, 31) did NOT survive /clear + CLI
exit. Rebuilt from docs; old #20 and #29 were unrecoverable — ask the user.
Rule going forward: every handoff embeds this snapshot (memory:
handoff-includes-tasklist).

1. **Round 8 completion protocol** — probe scores → `interp_p3_case3.exs`
   → verdict in INTERP_ROADMAP → user eye-test (absorbs old #21 / case #3)
2. **Nx upstream PR prep** (old #31) — clip-grad g² fix; worktree
   `~/git/nx-clip-fix` (branch `fix/clip-grad-double-g` off origin/main)
   verified 07-15: fix applies clean, pin test 5/6 fail without → 6/6 pass
   with, grad suite 274/274; user writes PR description
3. **Commit the 07-12→07-15 arc** — exphil + edifice; nx per fuzz-branch
   conventions
4. **Report card** (old #27) — per-checkpoint offline behavior profile
   (copycat index, shield occupancy, aggression) + profile probe slowness
5. **Parallel probes** (old #28) — 3× throughput, kills ±15% error bars
6. **Edifice interp modules audit** (old #24) — SAE prerequisite
7. **LEACE live** — iterative fitting (fit→play→refit) or α-scaled edits
8. **Script hygiene after loop ends** — archive dated scripts, quarantine
   decision, r4-r6 supersede
9. **Yeti debut (August)** — netplay path + bot Slippi account (user)
10. **Recover scope of lost tasks #20 and #29** — no doc trace; ask user
11. **Update local nx** (blocked on 1) — ff main to origin/main (fcfc1e3e),
    rebase feat/nx-fuzz-errors (base 2026-04-20 → misses user's own merged
    #1731, v0.12.x, hooks rework); re-run fuzz pins after, esp. BUG-1740-B
12. **Bump nx pins to ~> 0.12** (blocked on 1) — edifice/mix.exs:47 +
    exphil/mix.exs:201 still "~> 0.11", freezing cloud builds pre-0.12

## RUNNING RIGHT NOW (as of ~13:45)

**Round 8** (`scripts/overnight_newera5_20260715.sh`, task-managed): the most
complete config yet — 24-game clean EXPLICIT pool (no globs), sighted expert
(facing-relative keys) + the new `mewtwo_turnaround_fair.slp` fixture
(+62% opponent-behind table coverage), dropout 0.4, transition weight 2.0.
Finishes ~15:45 (train ~2h + 3 probes). Background tasks survive /clear but
DIE if the CLI process exits — keep the terminal open until "[newera] done".

**On completion:**
1. Read probe scores: `grep -a 'knockdowns=' logs/overnight_newera5_20260715.log`
2. Run the pre-registered measurement:
   `devenv shell mix run scripts/interp_p3_case3.exs` (already pointed at
   r1/r7/r8). Success bar: r8's opp_position saliency share materially above
   8% (r4 blind = 4.6%, r7 sighted-but-sparse = 7.5% on behind-frames).
   Also watch trunk opp_behind decodability (r4 0.72 → r7 0.78 → ?).
3. Record verdict in INTERP_ROADMAP experiment log; user eye-test: does
   Mewtwo turn around when Fox is behind it?

## COMPLETED THIS ARC (07-12 → 07-15)

- **NaN saga CLOSED**: cause = logit clamp missing at the exp-overflow cliff
  (|x|≥88.7) with stability dependent on XLA fusion choices; fix =
  `Nx.min(Nx.max(logits, -60), 60)` at the loss entry
  (`Policy.Loss.imitation_loss`); validated past 556k steps; 8 straight
  divergence-free runs since. LR 2e-4 is safe again.
- **Upstream Nx bug found & fixed locally**: `Nx.clip` gradients are g²
  (double-multiply in `nx/lib/nx/defn/grad.ex`; invisible when g=1). Pinned
  by `nx/test/nx/fuzz_clip_grad_test.exs` (fails on nx main), fixed in
  `~/git/nx` checkout (6/6 pin, 233/233 regression). UNCOMMITTED — see loose
  ends.
- **The dummy was never the dummy**: `--dummy-cpu-level 3` handed port 2 to
  the game AI for the entire old era (GOTCHAS #57). Fixed (cpu 0 + bridge
  guard); TechRandom now walks toward the bot, shields occasionally, and
  actually techs. Old-era scores are historical only; new-era baseline
  started 07-14.
- **Causal-confusion (copycat) diagnosis**: prev-action channel sustains
  holds (shield-lock: 10%→0.1% shield under live ablation) and suppresses
  tap repeats (fair follow-ups). Shield was never a concept — it's the
  user's ~7f L-cancel taps metastasized (0 sustained shields in fixtures).
  Objectives shipped: prev-action dropout 0.4 + transition-frame
  upweighting (`--transition-weight`, data.ex).
- **LEACE**: closed-form Cholesky-whitened eraser (exphil `Interp.Erase`,
  upstreamed shape into edifice with 12/12 functional tests). Copy-signal
  erasure: offline surgical success (guarantee exact, hold-persistence
  0.64→0.16 with rates preserved); LIVE failed via closed-loop distribution
  shift — single-shot erasers need iterative fitting (P6 follow-up).
- **Case #3 (opponent blindness)**: attribution → trunk-knows/heads-don't
  (probe 0.72 vs 5-8% saliency) → ROOT CAUSE: MewtwoFairExpert key used
  abs(dx) — a direction-blind teacher. Fixed (facing-relative side_bucket);
  r7 moved opp_position saliency 4.6→7.5% (behind) and trunk feature
  0.72→0.78; user recorded a turnaround-fair fixture (50% behind-frames,
  639→1,034 behind-keys) now feeding r8.
- **Interp toolkit** (`lib/exphil/interp/`): Activations (trunk/heads/floor
  capture + prev-action ablation), GroundTruth (20 features incl.
  opp_behind, lead-time buckets), Probe (class-weighted, by-replay splits,
  single-class guard), Erase, Attribution (empirical dim discovery via
  perturb-and-diff), ReplayStats. Embedding cache in the capture path.
- **Time-saver infra**: crime-scene capture (fatal batch+params+optimizer →
  `interp/crime_scenes/`), cliff checkpoints, `replay_crime_scene.exs`
  autopsy (this is what cracked the NaN in 3 two-minute replays after six
  blind hour-runs). Doctrine in CLAUDE.md "Build the time-savers first".
- **Live-inference experiment flags**: `--ablate-prev-action`,
  `--leace-eraser PATH`, `--window`, `--transition-weight`,
  `--debug-grads-after`, `--nan-forensics`, `--mixed-precision` (tested:
  not the NaN fix; MixedPrecision u32-key crash fixed).
- **New-era scoreboard** (vs real tech dummy; ±15% error bars at 3 probes):
  r1 6.3% → r2 21.4% → r3 30.8% → r4 0% → r5 30% → r6 8.3% → r7 0%
  (r7's metric was attribution, which improved). Pool: 24 clean games.

## LOOSE ENDS

- **Uncommitted changes everywhere**: exphil (the whole arc), edifice
  (LEACE rebuild + CLAUDE.md precision policy), nx (grad fix + pin test on
  `feat/nx-fuzz-errors`, plus a pre-existing modified exla/mix.exs).
  Suggest: commit exphil + edifice; nx per its own fuzz-branch conventions.
- **Nx upstream PR** (#31): complete as-is (minimal fix + pin suite +
  real-world story). User action.
- **Probe-verify slowness mystery**: Erase.verify's probe path burned 20+
  CPU-min while zoo-style probes are fast — unprofiled; folded into #27.
- **LEACE live**: iterative fitting (fit→play→refit) or edit-scaling α<1.
- **Quarantine**: `~/Slippi/quarantine/` holds 6 orphan-recorded stub
  replays (GOTCHAS #58). r7 trained with 5 of them (~2% frames, mild).
- **Dated overnight scripts** still say `--dummy-cpu-level 3` — harmless
  (bridge guard zeroes it) but confusing; delete or fix if reused.
- **r4-r6 checkpoints** trained pre-sighted-expert; supersede as r8+ lands.

## FUTURE DIRECTIONS

- **P4 reaction analysis**: the lead-time decodability curve
  (frames_until_kd buckets) is built and now has VALID data (real random
  techs). Does any policy read techs, and when?
- **Targeted fixture recipe is validated**: identify a pathology →
  attribution/probes localize it → user records a dense fixture → measure.
  Candidates: getup-option coverage, edgeguard/offstage (FoxRecovery
  pattern), platform techs (needs non-FD, unlock per-stage geometry first).
- **#27 report card**: per-checkpoint offline behavior profile (copycat
  index, shield occupancy, aggression) — replaces many probe games; also
  the venue to profile the probe slowness.
- **#28 parallel probes**: 3× probe throughput; kills the ±15% error bars
  (headless pipe games don't need the GC adapter).
- **P5/P6**: probe-driven data curation; SAEs with ground-truth-scored
  features (edifice modules need #24's audit first — assume scaffold-grade).
- **PPO/self-play** eventually (f32 loss casts already in the ratio path).
- **Yeti debut (August)**: netplay path — see YETI_DEBUT.md; bot Slippi
  account still owed by user.

## FILE MAP (what's live vs historical)

**Live modules** (`lib/exphil/interp/`): `activations.ex` (trunk/heads/floor
capture, ablation toggle), `ground_truth.ex` (20 features), `probe.ex`,
`erase.ex` (closed-form LEACE), `attribution.ex` (saliency + empirical dim
discovery), `replay_stats.ex`.

**Live scripts** (`scripts/`):
- `overnight_newera5_20260715.sh` — CURRENT loop template (clean pool,
  final-Dolphin kill; derive future rounds from THIS one)
- `interp_p3_case3.exs` — attribution/compression measurement (r1/r7/r8)
- `interp_capture.exs`, `interp_probe_zoo*.exs`, `interp_leace_fit.exs`,
  `interp_p3_followups.exs`, `interp_shield_jump_offline.exs` — reusable
  studies
- `replay_crime_scene.exs`, `nx_clip_grad_repro.exs` — forensics
- `dagger_drill.exs`, `play_dolphin_async.exs`, `trace_tech_chase.exs` —
  core (drill flags documented in header + handoff)

**Historical scripts** (superseded; keep for archaeology, do not run):
every other dated `overnight_*`/`diagnose_nan_*` — several still carry
`--dummy-cpu-level 3`. Candidates for `scripts/archive/` once no loop runs.

**Artifacts**: `checkpoints/mewtwo_combo_newera_r*_policy.bin` (new era;
older names = pre-fix era, historical), `checkpoints/cliffs/`,
`interp/crime_scenes/`, `interp/erasers/`, `interp/zoo/` (cached captures),
`cache/embeddings/`, `~/Slippi` flat = training games (explicit pool list
in the newera5 script), `~/Slippi/quarantine/` = never ingest,
`~/Slippi/2026-07/` = user's online games, off-limits to pools.

**Fixtures** (`test/fixtures/replays/`): `mewtwo_fair_chains`,
`mewtwo_shfair_only`, `mewtwo_approach_fair`, `mewtwo_turnaround_fair`
(new, 07-15) — the fair/combo experts consume all four.

## OPERATIONAL RULES (hard-won this arc; details in CLAUDE.md/GOTCHAS)

- NO-MIX while a beam is live + **loop corollary**: loops recompile per
  stage — no lib edits while any loop runs.
- Kill guards by process name (`pgrep -x beam.smp`), never command-line
  text; kill the FINAL probe's Dolphin too.
- Pools are explicit lists; content-check unknown replays before ingesting.
- Loss/objective math in f32 at the entry point (both repos' CLAUDE.md).
- Long scripts tee to logs; buffering pipes hide 20-minute hangs.
- Nx.LinAlg eigh/svd on EXLA = XLA-compile trap; BinaryBackend +
  default-backend scoping for small decompositions.
- Online games (launcher stack, `~/Slippi/2026-07/`) are structurally
  separate from training games (flat `~/Slippi/`); every pool sweep is
  maxdepth 1; provenance audit 2026-07-15 confirmed zero cross-contamination.
