# HANDOFF 2026-07-21 (afternoon)

Supersedes HANDOFF_2026-07-20.md. Companion reading:
INTERP_NEXT_RESEARCH_2026-07-20.md (adopted research queue),
docs/research/AGENT_SURVEYS_2026-07-20.md (verbatim lit surveys),
edifice/INTERP_AUDIT_2026-07-15.md (remediation ledger).

## RUNNING STATE

- **r15 IN FLIGHT** (final resume ~14:35, launcher 546675, watcher
  armed): mamba_2 backbone, SEQUENTIAL scan (both scan env flags
  dropped — profiler verdict below), resumed from the epoch-50
  snapshot. Full recipe: conversion-weight 3.0, probe-reg 0.1/every 5,
  probe-eval every 10, 62-game pool, mainline probes, x/10 cards.
  ~9.4 min/epoch WITH recipe -> ends ~22:30. Post-run: probes fan out
  automatically; the queue below runs after.
- A stray Phoenix BEAM (mix phx.server, different erlang) is Bradley's —
  leave it.

## THE DAY'S VERDICTS

- **Backbone (WS4 closed)**: Mamba-2 is the line. Screen 8.6/10 + 4
  conv (mainline) > mamba-1 8.1 + 5 (ExiAI) > gru2x/min_gru. mamba-1
  @100ep held its card (8.8) but not conversions (2) — failed the
  adoption rule. En route: :mamba_2 dispatch was never wired
  (fixed eb60d56); engine A/B says ExiAI/mainline cards translate 1:1.
- **Training speed (profiled, closed)**: all scan variants within 10%
  in the full graph (seq 8.9ms/step FASTEST, even vs mamba-1 10.8);
  the NIF/custom-kernel route is NOT justified. r15's ~2.2x epoch cost
  vs the screen is the RECIPE: probe-reg's second trunk fwd+bwd in the
  loss (XLA does not CSE it) + conversion-weighting's extra steps.
  r16 optimization target: expose the trunk in the policy output
  container so the penalty reuses the BC pass (halves recipe cost).
  Two block-level microbenches misled before the full-graph profiler
  settled it — profile the real graph first next time
  (scripts/profile_train_step.exs stays as the instrument).
- **Cross-arch interp (deficit report, first run)**: mamba-2@30ep beats
  r14-GRU@100ep on EVERY non-saturated ground-truth feature (e.g.
  opp_knockdown .768->.951, time_since_kd .497->.648). Tech-reaction
  curves: GRU flat noise (never reads techs); mamba-2 textbook reacting
  signature (0.94 decodability @ +30f). Mamba-2's own deficits =
  frames_until_kd + tech_choice (below input floor) — named farming
  targets.

## SHIPPED TODAY (all compiled, tested, pushed)

exphil (through 52967e3 + later): DeficitReport + script (P5 harness,
controls baked in); tech_reaction_curve.exs (P4 artifact);
TrainingProbes + --probe-eval-every (P2 close-out; r15 logs
.probe_curves.jsonl); bench_ssd_scan + profile_train_step (+ divisor
variants); scan-config plumbing (--mamba-chunk-size/--mamba-matmul-scan
+ NEWERA8_* envs, lineage default untouched); Activations delegates its
capture loop to edifice Capture (equivalence-checked vs morning JSON:
identical); XLASelectiveScan deleted (dead, zero consumers).

edifice (through the seed fix): **Nx.block migration** — all 51
Nx.Shared.optional callsites (dead upstream) -> Edifice.Block.run;
native tier = one future defimpl (task #13 remainder, gated on need,
which the profiler says does not exist). **Interp remediation phase 2**:
SAETrainer warm-start (:init_params, shape-validated) + finetune/3 +
:batch_size minibatch + deterministic fit (Axon.build seed —
was system_time(); fit was NEVER reproducible before); Diffing
(decoder_shift/moved_features/firing_shift) = stage-wise diffing
primitive; quarantine STATUS warnings on GatedSAE/MatryoshkaSAE/CLT/
DASProbe. **Multi-site capture (audit gap 3)**: Common.probe_tap/
with_probe_taps + build_probe on both mambas (shared build_block =
shared site vocabulary: pre/post_conv, ssm_out, gate_z, post_gate,
B/C/dt; {batch,seq,...} shapes = per-timestep P0 tier);
Interpretability.Capture (model OR prebuilt predict fn; per-batch
backend_transfer). Full stale gate: 5379 tests green.

Checkpoint ladder: OPERATIONAL (PolicyOpponent ControllerState fix +
JIT warmup at construction — unwarmed P2 starved the loop to 0.1fps;
stray-Dolphin sweeps in the script). Warmup-test match: r14 beat r13.

## POST-r15 QUEUE (tonight, in order)

1. Score r15: gate 10 (armed approaches/min — was 0.0; THE recipe
   verdict) + openings/min + StyleCard columns; compare vs mamba_full
   8.8-equiv and the screens. Read .probe_curves.jsonl — first
   feature-formation curves (does representation growth precede card
   growth? where do curves plateau -> epoch budget).
2. Full 6-game ladder: r13, r14, mamba2_screen_best, r15 (GAMES_PER_PAIR=2).
3. Stage-wise diffing smoke: SAETrainer.fit on r13-GRU trunk capture ->
   finetune on r15's -> Diffing.decoder_shift (first real diff).
4. Gate-10/StyleCard calibration vs corpus/human_blewf/ (zelda file =
   Sheik early/Zelda late — segment by character) + r14 probes.
5. r16 decision: recipe verdict + trunk-reuse optimization + #38
   scenario-seeded farming if gate 10 stayed 0.
6. PPO smoke (paradigm-ladder gate now UNBLOCKED — audit jit fixes
   authored 2026-07-21, in the compile gate): mix run
   scripts/train_ppo.exs --pretrained <r15 policy> --opponent cpu3
   --timesteps 20000 --headless-ish (see script flags). Goal:
   rollouts collect, updates step at GPU speed, reward moves — NOT a
   better bot. First on-policy PPO over a Mamba-2 policy (unpublished
   territory per the survey). Expect bit-rot beyond the audited lines.

## OPEN TASKS (CLI task list is GONE — MCP dropped; rebuild from here)

| # | status | task |
|---|--------|------|
| #7 | pending | LEACE iterative-fit variant only |
| #9 | pending | Yeti netplay path (Direct smoke needs Bradley; windowed = rollback-free per research; EXPH#288 final) |
| #11 | pending | nx fuzz-branch remainder (push/PR = Bradley's calls) |
| #13 | ~DONE | Nx.block migration Elixir half SHIPPED; native defimpl gated on need (profiler: none) |
| #15 | mostly DONE | remediation ph2 shipped; remaining: CLT redesign (attribution-graphs prereq), JumpReLU decision, h_t site (scan-layer split, WriteSAE prereq) |
| #19 | ~DONE | ladder operational; first full round in post-r15 queue |
| #20 | in_progress | round velocity — epoch-budget curves land with r15; recipe-cost finding above |
| #23 | pending | G&W track (fixtures + corpus ready) |
| #27 | pending | per-checkout exla NIF isolation |
| #29-#33 | pending | five-char program (priors, corpus, catalog, taxonomies, program) |
| #30 | seeded | calibration corpus (queue item 4) |
| #35/#36 | VERIFIED, live in r15 | conversion sampling + probe-as-regularizer |
| #37 | DONE | research surveys collected + synthesized |
| #38 | pending | scenario-seeded combo farming (r16 candidate, gate-10-dependent) |

Parking lot: upstream drafts await Bradley (DO NOT POST);
idle_deadlock prefix re-curation; tech-chase/punish shared reach model;
openings/min stat SHIPPED.

## GOTCHA CANDIDATES (write into GOTCHAS.md when touched next)

- Microbench fusion != full-graph fusion: two block-level benches
  misattributed the r15 slowdown; only profile_train_step.exs (real
  drill graph) gave the true per-step ranking.
- Axon.build seeds param init from system_time() by default — pass
  :seed for reproducible fits (bit edifice SAETrainer).
- mix compile beside a live trainer only kills it if libexla RELINKS;
  pure-Elixir compiles squeak through — do not rely on this (got lucky
  14:1x today; the pkill that "stopped" the run had silently failed —
  VERIFY process death by PID, not by pkill exit).
