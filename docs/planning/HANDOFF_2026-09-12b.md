# HANDOFF 2026-09-12b (evening) — for Astra: the multishine methodology test, one day of instruments, and the root cause of the DAgger floor decline

**Supersedes HANDOFF_2026-09-12.md** (same day, morning; its §2a-2j are the
hour-by-hour log — read this file first, dip into that one for detail).
Written 09-12 ~23:00 by Claude Fable 5.1 while g26a trains. Bradley
directed every turn; his calls are marked **[B]**.

## 0. Read this first

- **Goal (Bradley's restatement, 09-11):** a Fox that multishines "no
  matter where Fox is, what Fox is doing, what the opponent is doing" —
  a METHODOLOGY test of the whole pipeline (drill -> gates -> live), not
  a product. v3 (the generalist) stays parked behind it.
- **Today's result, in one line:** the reason every DAgger round since
  g19 traded the technique floor away (stand dummy 296 -> 111 -> 93
  shines/min) is a LABEL CONFLICT invisible at shift 0 and present at
  every trained reaction delay; it is now structurally unrepresentable
  (INVARIANTS item 14), and g26a is the first checkpoint trained without
  it. **Its readout is the next thing to read** (§6).
- **What is running:** `systemctl --user list-units 'exphil*'` —
  `exphil-g26a2` (training, ~2 min/epoch, 60 epochs, snapshot-all) and
  `exphil-g26-readout2` (queued: waits for training, then runs the
  coverage map, stand floor, CPU gate). Logs under
  `eval_runs/0912_g26_phase/`. **Do not run `mix` while a beam is live**
  (SIGBUS kills the trainer). Check `pgrep beam.smp`.
- **Repo:** exphil `main` @ 65b99a0e+ (all of today committed; latest
  commits are the handoff-worthy summaries). Memory index:
  `~/.claude/projects/-home-blewf-git-exphil/memory/MEMORY.md`.

## 1. Candidates and how to play them

| checkpoint | what it is | stand floor (shines/min, v3 chain) | level-1 CPU |
|---|---|---|---|
| `checkpoints/ms_g23a_ep57.bin` | technique (fixture-exact) | 296 / c277 async; 427 / c427 sync | 54-62 / c2-3 |
| `checkpoints/ms_g24a_ep55.bin` | coverage (DAgger round 1) | 111 / c27 | 69-71 / c8-13 |
| `checkpoints/ms_g25a*.bin` | mirror + opp-context redress (FAILED live) | 93 / c2 | 82, 68 / c6-8 |
| `checkpoints/ms_g26a_ep33.bin` | item-14 labels, g24a pool (FAILED live; see §6c) | 67 / c4 | 49-51 / c2-5 |

Play any of them with **ONE knob**: `--reaction-delay 4` (the local
rung). Everything about delay is resolved from that number
(`ExPhil.Eval.HarnessRung`), the checkpoint's delay-id is derived from
it, and the runner MEASURES the latency at every game start
(`ExPhil.Bridge.LatencyProbe`) and says so in the log. Windowed local
play (Bradley's look):

```bash
bash scripts/x11_socket_fix.sh   # GOTCHA #114: Xwayland socket gets unlinked
mix run scripts/play_dolphin_async.exs --policy checkpoints/ms_g26a.bin \
  --dolphin "$HOME/.config/Slippi Launcher/netplay-beta-nixos" --iso ~/isos/melee.iso \
  --character fox --stage final_destination --reaction-delay 4 --temperature 1.0
```

Old cards still work: `--frame-delay N` is a deprecated alias for
`--reaction-delay N+1`; `--delay-id-override` still wins but now WARNS
when it disagrees with the rung.

## 2. What was established today (each measured; RESULTS.md has tables)

`eval_runs/0910_g20_causal/RESULTS.md` §6-9 is the ledger. In order:

1. **Closed-loop correction table, policy column real (§6).**
   `Agent.observe/4` (observe-only history warm-up) lets the scenario
   suite hand a stateful policy a MID-GAME state with its true history.
   With that plus the aligned rung, ep57 chains 14/14 on the mid-chain
   control (== teacher). On the 12 mined break moments: teacher chains
   10-14 on all; ep57/ep55 chain 1-3 (jump -> mid-air shine -> float,
   never a JC out of landing). The teacher's corrections are executable
   and restore the loop -> promotable.
2. **GOTCHA #115: harness latency.** The suite's native latency (1 frame)
   was a rung no policy trained at; symptom = the `366x18` float, twin of
   #81. Calibration grid on ep57's own game gave the suite law.
3. **[B] "we're building it wrong if we track latencies" -> ONE knob.**
   `--reaction-delay k` (physical reaction delay, the same number training
   counts) on both runners and the suite; drill delay-ids are PHYSICAL
   (`--pipeline-offset` retired; old recipe `"0,1,2,3"+2` == `"2,3,4,5"`);
   `LatencyProbe` measures at game start. Verified against Slippi's own
   recording: a synchronous send at `--frame-delay 3` is recorded 4
   frames later; the async runner's POLICY path adds one frame-tick
   (decision on f sent while handling f+1) -> sync fd N = latency N+1,
   async N+2, suite rd N = N+1. (An afternoon "sync == async" claim was
   WRONG and is retracted in GOTCHA #115 addendum 3; the chain evidence
   behind it was ep57 playing one frame faster than its labels on the
   sync runner — an id-calibration fact, not a latency one.)
4. **Instrument 2, the counterfactual coverage map (§7).**
   `scripts/probe_ms_coverage_map.exs`: teacher-forced stream through the
   agent (`Agent.observe(probe: true)`), p(correct) per loop state under
   43 counterfactual perturbations, ~5 min, no Dolphin. ep57: FLAT on
   position/percent/opp character/most opp actions; COLLAPSES on opp
   shield/grab/shine (JC out of shine f1 p 0.00-0.03), opp behind/close
   (aerial phase 0.01-0.3), MIRROR (not learned). Scans label offsets
   0..6 (physical id k tracks offset k).
5. **g25a (§8): the map's levers as augmentation FAILED live.**
   `--mirror-frames` + `--opp-context-frames` (per-list redresses) fixed
   the map's first-decision cells (0.00 -> 0.94+) but the aerial press
   (365/3a) collapsed to 0.31 even on the fixture; stand floor 93/min c2.
   **[B]** "shouldn't we start real games with different characters
   instead of augmenting?" — agreed and recorded: synthetic context !=
   real dynamics; the next COVERAGE round is real varied-start rollouts
   (opponent character, CPU level, SIDE). Not started; see §5.
6. **Instrument 1 at the TRAINING SHIFT (§9) — the root cause.**
   `scripts/audit_ms_pool_labels.exs --shifts "3,4,5"`: the pool agrees
   with itself on all 11 loop states at shift 0 and conflicts on 10 of
   11 at shift 4 (365/3a: fixture B+X 1.00 vs 3,185 rollout frames
   0.10/0.06). Mechanism: the drill made a reaction-delay-k label by
   taking the label of the RECORDED frame t+k — the STUDENT's future,
   which in rollouts is a break, not the loop. Every DAgger round since
   g19 trained against this and it scales with rollout volume.
7. **[B] "phase-indexed k-ahead expert, and make the error impossible"
   -> INVARIANTS item 14 (structural).** Frames carry `:label_source`
   (`:recorded` | `{:expert, mod}`); `ExPhil.Training.Labels.at_delay/3`
   is the one producer of delayed labels (recorded -> shift along the
   recording; expert -> `mod.label_ahead/4`); `Data.shift_actions/2`
   RAISES on an expert list. `MultishineExpert.label_ahead/4` is
   phase-indexed on the canonical cycle learned from the fixture
   (off-loop: hold the recovery, or drop). The auditor audits through the
   same function. **Proof:** the same pool, new labels: 0 conflicts / 0
   ambiguities at shifts 3,4,5. 16 tests, the fixture as oracle.

Also today: INVARIANTS 12 (`HarnessRung`, one latency table + the probe)
and 13 (mid-game handoff warms history; `Agent.observe`), GOTCHA #115 +
3 addenda, `Agent.observe(probe: true)`, `ControllerState.from_input/1`,
break miner `--tail-margin`, AR-head confidence probe history fix.

## 3. Standing rules (new ones first)

- **Controls first, always.** Before reading a policy number out of any
  harness, run a control the policy is KNOWN to pass (its own replay)
  and sweep the latency knob. A memoryless teacher passing proves
  nothing about the harness rung. A chain that re-enters then floats is
  a timing-convention bug until a control excludes it.
- **A chain optimum is not a latency measurement.** Only a recorded
  marker is (`LatencyProbe` vs the .slp).
- **Delayed labels come from the label source's own future** (item 14).
  Never `shift_actions` an expert-labeled list; the code refuses.
- **The pool auditor runs AT the training shifts and gates every
  prereg** (`--shifts` = the drill's `--multi-delay`).
- **Verify offline before Dolphin:** the coverage map is 5 minutes.
  Then BOTH live numbers must hold (stand floor = technique, CPU gate =
  coverage) — g24a/g25a showed one rises while the other falls.
- Older: never crown on stand-dummy numbers (g6); sampled gates (T=1.0),
  never argmax; `mix` never while a beam is live; long runs via
  `systemd-run --user`; no bespoke decode rules.

## 4. Open decisions for Bradley

1. **v3 deploy card (INVARIANTS item 12 consequence).** Both runners'
   floor is reaction 1 on the async runner (decision hop); the sync
   runner plays reaction 0 exactly at `--reaction-delay 0`. A reaction-0
   checkpoint (v16e, v3's default `--label-delay 0`) was deployed TWO
   frames slow by the 09-09 `--frame-delay 1` card (safe direction).
   Options: train v3 at `--label-delay 1` and play async at
   `--reaction-delay 1`, or play v3 on the sync runner at 0.
2. **Coverage round design** after g26 reads out: real varied-start
   rollouts (opponent character/CPU level/side, relabeled with item-14
   labels) vs another redress pass. Bradley leans real; so does the g25
   evidence.
3. **ep57's id-1 weakness** (chains c3-5 at every sync knob while its id
   2 chains 427): ids are not equally trained; if g26 shows the same,
   consider a single-rung drill at k=4.

## 5. NEXT (in order)

1. **Read g26a** (§6). If the floor is back near ep57's while the CPU
   gate holds g24a's gain, item 14 was the cause; promote and do
   Bradley's live look at `--reaction-delay 4`.
2. **Real varied-start DAgger round** (Bradley's design): rollouts vs
   level-1 CPU with `--dummy-character` over a roster, both SIDES
   (swap ports / mirror start), relabel with item-14 labels, retrain,
   map, gates. The loop exists (`eval_runs/0911_g24_dagger/gate_cpu.sh`
   collects; the drill relabels).
3. Interp list leftovers (HANDOFF_2026-09-12 §3): 3 chain-break
   attribution, 4 AR-head conditional entropy (the AR head is out for the
   ms line until this is understood), 5 re-entry profile.
4. Item 13 structural form (a handoff API that REQUIRES the history).
5. v3 only after 1-2 and decision 4.1.

## 6. How to read g26a's readout (when `exphil-g26-readout2` is done)

```bash
cat eval_runs/0912_g26_phase/readout.log                 # map rows + stand + CPU
sed 's/\x1b\[[0-9;]*m//g' eval_runs/0912_g26_phase/map_final.log | grep -E "baseline p|^\[.*\] (baseline|opp shield|opp grab|opp shine|stage mirrored|opp at p1.x-40)"
cat eval_runs/0912_g26_phase/stand_k4/sweep_table.txt   # per-epoch shines/min + chain
cat eval_runs/0912_g26_phase/cpu_k4/cpu_table.txt
```

Compare with: ep57 map `eval_runs/0912_coverage_map/ep57_id0.log`
(baseline 0.985 at offset 2), g25a map `eval_runs/0912_g25_context/
map_final.log` (0.883 at offset 4, 365/3a 0.31). Success = g26's baseline
back near 0.98 with 365/3a restored, stand floor >> 111, CPU >= g24a.
Expect g26's map to still be RED on opp shield/grab/shine/behind/mirror
(no redress in its pool) — that is the coverage round's job, not this
one's.


## 6b. Open question raised by Astra (23:00): the low-loss guard vs item-14 labels

g26a's log: `COLLAPSE-SUSPECT` restores at epochs 34/36/38/43 on losses
4e-6 / 8e-6 / 2e-6 / 8e-6 (guard: `loss < 1e-5` or a >100x one-epoch
drop == GOTCHA #99 collapse; restores best params, max 5). That rule was
calibrated when no genuine drill loss could reach 1e-5 — a pool with
label conflicts has a floor. Item-14 labels remove the conflicts, so the
pool may be fully fittable and 1e-6 may be real convergence. The
rejected snapshots are the suspect epochs' OWN weights (`--snapshot-all`
exports before the guard). Queued (`exphil-g26-guard`, after the
readout): `eval_runs/0912_g26_phase/guard_forensics.sh` — params/logits
finiteness + an independently recomputed BCE and p(correct) on the
fixture at offset 4 (`scripts/probe_snapshot_health.exs`), coverage maps
ep33 vs ep34/ep38, stand floor + CPU gate on ep33/34/36/38. If the
rejected snapshots are finite AND behave at least as well live, the
guard should be replaced by evidence (non-finite params/logits, or a
behavioral drop on a fixed probe set) instead of an absolute threshold.
Read `eval_runs/0912_g26_phase/guard_forensics.log`.

## 6c. g26a READ OUT (23:30) — item 14 did not restore the floor; guard verdict

Stand k=4: ep33 66.8/min c4, ep38 56.8 c1 (ep57 296; g24a 111; g25a 93).
CPU k=4: 49-51/min c2-8 (== ep57, below g24a). All snapshots finite,
independent fixture BCE 0.027-0.041, p(correct) 0.994-0.998 — the
"collapse-suspect" epochs are healthy, the 1e-5 guard is wrong under
conflict-free labels (replace with evidence; not done). Leading suspect
for the floor: the off-loop HOLD rule in `label_ahead/4` (recovery input
repeated at every k, no press edge) — k-ahead should follow the recovery
INTO the loop (phase k-1 after a shine press). RESULTS.md §10. Bradley
to call: fix-and-retrain, or confirm first with the closed-loop suite on
ep33. Training was stopped at epoch 48 by hand; no final export.
## 6d. Codex confirmation (09-13): recovery HOLD is observably wrong

See [RECOVERY_LABEL_CONFIRMATION.md](RECOVERY_LABEL_CONFIRMATION.md) for the
experiment, raw artifacts, and limitations. No production expert/label/guard
changes or training were launched in this confirmation.

- Native teacher mid-chain control: chain 14, zero delayed-label mismatches.
- Three clean grounded recoveries from G26 ep33's own replay: teacher chain 13
  each. At shift 4, **18/21 off-loop predictions disagree with the teacher's
  actual future commands; 0/327 on-loop predictions disagree**.
- Recovery does **not** universally enter the canonical loop one frame later:
  landing/startup and held-reflector traces take seven frames in these cases.
  Do not implement a blanket phase `k - 1` substitute.
- Broader 36-case pilot: 28 drifted and 3 errored; excluded, not a valid full
  benchmark. Cause of the prefix drift is not established here.
- Policy control at reaction 4: ep57 chain 1 twice despite zero prefix drift.
  Strict control gate stopped before G26 cases. The label defect is confirmed;
  its share of G26's regression is not. Restore policy control/calibration before
  interpreting a retraining comparison.

### 6e. Codex: timing-verified policy control and transport investigation (09-13)

- Fresh ordered tracker: `docs/planning/MULTISHINE_PIPELINE_PROOF.md`.
- ep57 reaction 2 control restored: six chain-14 runs, zero drift/errors, all
  commands verified against the run's own recording. Reaction 4 remains a
  failed control; changing warm-up history alone does not fix it.
- Measured intermittent send latency of 1 versus 2 frames with identical
  requested settings. Scenario policy runs now verify timing by default,
  withhold scores and exit 2 for invalid delivery. Do not conflate these with
  learning failures. See `docs/guides/SCENARIO_INPUT_TIMING.md`.
- User authorized dependency fixes: sibling `libmelee_ex` now drains completed
  frames before flushing controllers. Regression covers pipes/direct batches;
  12 patched reaction-2/3 controls pass timing and chain 14. Source is uncommitted;
  evaluation loads it in memory, no shared native rebuild or training launched.
- This fixes an independently reproduced extra-flush bug, not yet proven to
  explain all delivery variability. Additional-rung checks and exact evidence
  belong in the new tracker. Gates 2–4 (trusted teacher futures, tiny overfit,
  coverage expansion) remain queued behind reliable calibration.

### 6f. Codex: timing stress and recovery-target regression (09-13)

- `scripts/stress_input_timing.sh` tests four fresh launches each at blocking,
  100 ms, and 1 ms polling. Initial aggressive-polling batch failed 4/4 input
  timing checks; blocking/normal passed 8/8. Failures retained under
  `eval_runs/0913_timing_stress/`.
- Fixed bridge internal retries: repeated nil results previously flushed
  controllers again without a new decision. Sibling `Console.step/3` now
  supports `flush: false`; local bridge uses it on internal retries.
  `eval_runs/0913_timing_stress_retry_fix/` passes 12/12, all chain 14, zero
  timing/drift/errors. External `poll: true` loops are not qualified by this.
- Fixed verifier acceptance of missing/reordered trace frames. Added transport
  tests for late frames, repeated waits, and game restarts; library suite passes
  (119 doctests, 3 properties, 568 tests; 71 live-Dolphin tests excluded).
- `scripts/check_recovery_targets.exs` verifies teacher delivery and actual
  `Labels.at_delay` targets against issued teacher futures. Three grounded
  recordings pass 360/360 commands; all targets at shifts 2/3/4/5 match
  (354/351/348/345 targets, 21 off-loop each). Curated regression fixture and
  test preserve this evidence independently of external experiment files.
- Tiny training not launched: need recorded-teacher window ingestion (the
  snippet path intentionally retags expert labels), neutral-start coverage,
  and correction of the still-active low-loss guard. Do not bypass these.
  Full next steps: `docs/planning/MULTISHINE_PIPELINE_PROOF.md`.

### 6g. Codex: training preflight corrections landed in working tree (09-13)

- Low-loss correction: `EpochHealth` validates numerical loss/parameters;
  removed automatic rollback for <1e-5 or 100x loss drops. Behavioral gates
  remain necessary for finite-but-bad models. Resume fingerprints version this.
- Two verified neutral standing starts: `scenarios/ms_neutral_start_confirmation.json`,
  teacher chain 14 each, no drift, native timing/targets verified.
- `--recorded-frames` ingests causal unshifted explicitly recorded lists, without
  expert relabeling or previous-action overrides. Streaming explicitly rejected.
- Six exported teacher windows (720 frames) are in
  `eval_runs/0913_teacher_ingestion/validated/`; do not use prototype `.frames`
  in the parent folder. Final GPU preflight passes ingestion, gradient update,
  numerical guard and checkpoint serialization; no long training launched.
- Next: tiny overfit and start/sustain/recover gates on the same checkpoint.
  See `docs/guides/RECORDED_TEACHER_TRAINING.md` and the pipeline tracker.

### 6h. Codex: tiny overfit exposes temporal batching defect (09-13)

- Completed 40 epochs with validated recorded teachers, delay 2, GRU64/window16.
  Normal best-loss export reaches approximately 0.0001 but fails closed-loop
  start/sustain/recovery gate: 4/11 timing-valid passes; one withheld timing run.
  Sustain chains 14 twice; starts and two recovery cases fail. No promotion.
- Actual lazy sampler audit: 90 windows cross clip boundaries; all 29 teacher
  off-loop targets identified in the audit receive unrelated prior-clip context.
  Label shifting is correct; temporal histories are not. Not a claim that this
  is the sole cause of behavioral failure.
- Prepared projected-label comparator deferred because it shares the defect.
  The untrained control completed but all 12 runs fail input verification, so
  its scores are withheld. Offline offset scans do not resolve the mismatches;
  do not interpret them as proven extra-frame delays or a valid baseline.
  Next fix must retain early targets using valid input histories, not simply
  drop crossing windows. Add sampler regression, then repeat a separate bounded
  experiment. Keep recorded-input timing fail-closed.
- Protocol/results: `docs/planning/TINY_OVERFIT_2026-09-13.md`.
  Artifacts: `eval_runs/0913_tiny_overfit/`. No shared native rebuild.

### 6i. Codex: clip-boundary histories corrected (09-13)

- Eager drill now uses `Data.from_frame_lists/2`; lazy windows and action queues
  respect list boundaries and frame gaps. Early targets receive repeat-first
  cold-start padding (same convention as Agent), not unrelated clip history.
- Sampling/loss weighting, distillation masks, and probe labels map to the
  actual supervised targets. Changed resume fingerprint prevents old resumes.
- Real-data audit: 7,785 sequences, zero cross-clip windows, all 29 classified
  off-loop targets retained with no wrong-clip context. Original failed run and
  audit are preserved. No new 40-epoch run or promoted model.
- Cold-start padding is not replay-prefix reconstruction. Input-verification
  mismatches and a new behavioral proof remain open; streaming not certified.

### 6j. Codex: input verification and history diagnosed (09-13)

- Offline component audit: all 2,880 sent digital-button patterns match replay
  at +1 frame. Initial outputs have 1,402 analog-mismatch frames; corrected
  candidate has one, fully localized to digital R producing right trigger 1.
  Summed-trigger/raw-stick identity checks are not the correct general analog
  contract. No tolerance relaxation, re-scoring, or extra-delay claim.
- History audit confirms cold training vs warmed committed-prefix evaluation:
  15 padded windows per teacher clip; 18 contain not-yet-filled queue embeddings.
  The committed decision clock itself is consistent with delayed labels.
- Next: deterministic analog/readback contract + regressions; explicit matched
  cold-at-handoff history mode; then a fresh bounded proof. Continuous recovery
  must ultimately be tested/trained with populated history.
- Full evidence: `docs/planning/INPUT_AND_HISTORY_DIAGNOSIS_2026-09-13.md`.

### 6k. Codex: conversion, verifier, and cold-history fixes validated (09-13)

- Live sweep found real pipe-trigger compression: bipolar Axis + was fed a raw
  pad-byte fraction. Fixed sibling Controller pipe encoding; raw Slippi pad
  serialization is unchanged. Reload/compile that Elixir module when evaluating.
- Verifier v2 handles measured stick quantization/circular clamp/deadzones and
  independent trigger clicks. Explicit pipe_v1 for historical recordings,
  pipe_v2 for corrected transport; tighter 1e-5 tolerance and component errors.
- Both 549-command sweeps pass. Saved initial/corrected runs revalidate 12/12
  each under legacy profile, without modifying original scoreboards.
- Added --prefix-history cold: skip policy prefix observations and reset actual
  Agent history/queue at handoff; preserve physical prefix and delayed sends.
  Live untrained smoke passes timing/drift/full-response checks at two handoffs.
- No retraining. Next bounded proof should use fixed batching + cold history;
  continuously warmed recovery remains a separate requirement.
- Usage: `docs/guides/INPUT_READBACK_CONTRACT.md`.
- Fresh warmed ep57 positive control still chains 14 with valid timing, zero
  drift/errors, and 120 response frames. 18 Exphil tests pass; sibling coverage
  passes 9 doctests, 2 properties, 18 tests. No native rebuild.

### 6l. Codex: boundary-safe cold overfit run (09-13)

- Completed a fresh 40-epoch run with fixed batching, the same untrained initial
  parameter tree, and matched cold-history/pipe_v2 evaluation. Artifacts under
  `eval_runs/0913_tiny_overfit_cold/`; previous experiment untouched.
- Candidate passes 7/12, not the required 12/12. All trained runs have valid
  timing, zero drift/errors, and complete responses. Starts and two recovery
  cases remain unreliable; no promotion or extra epochs.
- Matched untrained control passes 0/12; all its responses also pass timing,
  drift, and completeness checks. Both evaluations finished; no job left running.
- Newly confirmed metric defect: the drill's "epoch loss" is the final
  minibatch loss, also used for best-checkpoint selection and target stopping.
  Its minimum 3.48e-6 is not proof of full-pool fitting. Both normal and
  forensics reductions have this behavior. No mid-experiment metric change.
- Next: correct epoch aggregation/selection metrics, add fixed full-pool and
  per-handoff teacher-forced loss/action accuracy, then repeat the bounded proof.
- Protocol, control results, and handoff table:
  `docs/planning/TINY_OVERFIT_COLD_2026-09-13.md`.

### 6m. Codex: epoch-loss aggregation corrected (09-13)

- Normal and forensics paths now use denominator-weighted batch aggregation;
  the aggregate drives logs, checkpoint selection, target stopping, and plateau
  tracking. Invalid batches remain invalid; resume fingerprint versions this.
- Eleven metric/health regressions pass. Bounded GPU smoke evidence is tracked
  separately; no new 40-epoch experiment or promotion.
- Normal and nan-forensics one-epoch checks both pass/export: 122 batches,
  mass 7,785, peak RSS 2.1 GB. Main processes exit 0; no native rebuild.
- An existing CLI edge remains: omitting --snapshot-all raises on nil `and`.
  The proof recipe already supplies it; not part of this metric correction.
- Next: fixed-checkpoint per-case teacher-forced fit, then first-divergence
  diagnosis / 12-of-12 cold proof, followed by warmed and held-out recovery.
- Details: `docs/planning/EPOCH_METRIC_CORRECTION_2026-09-13.md`.

### 6n. Codex: snapshot fix, frozen learning, and live failures (09-13)

- Omitted `--snapshot-all` no longer crashes; one-epoch export passes without
  an epoch snapshot. Fourteen guard/metric tests pass.
- Same candidate fits 696/708 teacher targets (conditional argmax), but only
  21/29 off-loop targets. All twelve errors are within the first six frames.
  Initial control has zero exact matches; no checkpoint reselection.
- Neutral starts have different first-two pending sends from the teacher and
  diverge at +1 despite perfect teacher-frame argmax fit.
- Runtime uses parsed AF on live states: 1,059/1,440 mismatches. Normalization
  leaves 39, all missing action 363 with delta +1. First-frame probabilities
  barely change with normalization; it is not proven to be the sole cause.
- Traced five failed responses; the two weak recoveries cycle late after
  wasting much of the response on aerial detours.
- Next: regression-backed AF contract/363 coverage, then same-candidate matched
  teacher pending-prefix proof. Keep original-source recovery separately;
  rebalance early targets/retrain only after that diagnosis.
- No new Dolphin run, training experiment, or promotion. Details:
  `docs/planning/PER_CASE_LEARNING_2026-09-13.md`.

### 6o. Codex: producer AF contract and matched-teacher revalidation (09-13)

- Corrected the earlier proposal: do NOT extend the disproven action-only
  table. New `libmelee_to_parsed/3` inverts the actual current Melee.Events
  character/action indexing adjustment, including Fox 363. Fractional age
  lost by producer truncation is outside this indexing contract.
- Suite selects `:libmelee` explicitly by default; `--no-live-af` preserves
  unconverted policy comparisons. Generic Agent/CLI defaults remain parsed.
  Teacher and scalar/batched embeddings use the producer rule. 12 tests pass.
- Original 1,440 and new 1,440 AF observations all match parsed replay under
  this rule. All new runs have matching pending sends and initial states.
- Same candidate, matched teacher sources, cold delay-2, T=1, two runs/case:
  neutral2228 14/14; neutral2566 14/14; sustain900 8/6; recovery4 13/13;
  recovery75 1/7; recovery146 13/1. **7/12**, no promotion or retraining.
- Every response passes timing/completeness/drift checks. The final shell
  gate exits 1 for behavioral failures, not an execution/transport error.
- Next: rebalance early cold sustain/landing targets, measure frozen per-case
  fit, then repeat the fixed matched-teacher gate; keep original-source
  recovery separately. Do not attribute combined changes to AF alone.
- Details: `docs/planning/MATCHED_HANDOFFS_2026-09-13.md`.

### 6p. Codex: targeted early fitting preparation (09-13)

- New opt-in `--recorded-prefix-weight 64 --recorded-prefix-frames 18` repeats
  actual cold teacher windows without changing labels or histories. All six
  teacher cases included; canonical targets remain once per epoch.
- Audit: all 7,785 unique targets retained, 108 early targets repeated x64,
  14,589 total draws / 228 batches, zero crossed clips. Early draws rise from
  1.39% to 47.38%. Scheduler scaling and resume fingerprint cover the recipe.
- Frozen readiness gate requires all six cases, complete rows, 18/18 early
  conditional argmax matches and per-target joint probability >=0.95. Checks
  checkpoint SHA; existing candidate fails, as expected.
- 18 focused tests pass. GPU preflight passes one disposable train step and
  export checks, ~2.0 GB peak. No multi-epoch experiment or improved-fit claim.
- Next bounded run: same initial tree/fresh optimizer, consider 21 weighted
  epochs (4,788 steps) to stay below prior 4,880-update budget. Select once by
  corrected aggregate, frozen fit gate, then unchanged matched-teacher live gate.
- Details: `docs/planning/EARLY_PREFIX_FITTING_2026-09-13.md`.

### 6q. Codex: 21-epoch prefix-weighted round completed (09-13)

- Fixed 21 x 228 = 4,788 updates, exact original initialization/fresh optimizer,
  no extra epochs or numerical rollback. Best aggregate epoch20 loss0.003503
  selects the ordinary export; no snapshot fishing. Peak RSS ~2.1 GB.
- Frozen teacher matches improve 696/708 -> 705/708; canonical remains
  7,077/7,077. Early matches 96/108 ->105/108. Neutral and landing recovery
  cases pass the >=0.95 minimum joint-probability gate.
- Sustain900 still misses X decisions at relative1/3; recovery4 misses X at
  relative0. Those cases fail readiness. No live evaluation, promotion, or
  budget extension; launcher stops with exit1 at the frozen gate.
- Next: exact input-window/target collision and train/eval consistency checks
  on those three decisions before further training. Behavioral gate unchanged.
- Artifacts: `eval_runs/0913_early_prefix_fit/round21/`.
- Details: `docs/planning/EARLY_PREFIX_ROUND21_2026-09-13.md`.

### 6r. Codex: three-window diagnosis finds recurrent-state and precision seams (09-13)

- Full-pool vs isolated input windows are bit-identical. Each failed window
  has only its own exact match in 7,785 targets; no exact conflicting labels.
- Confirmed defect: implicit Axon GRU initial state is random Glorot, shaped
  by batch size/row. Same recovery input repeated64 has P(X)=0.191–0.542 and
  five rows press X. Zeroing only initial-state graph nodes removes all
  within-batch variation, but does NOT fix the three current decisions.
- Additional mismatch: BF16 training inputs/model vs F32 export reconstruction;
  precision metadata is absent. Reconstructed BF16 still misses all three.
- No model/code deployment changes, training, or live run. Gate remains closed.
- Next: versioned explicit zero-state contract + batch/reload regression tests;
  explicit precision parity including sequential AR head (or F32 training).
  Then reassess these targets before spending another training budget.
- Evidence: `docs/planning/EARLY_WINDOW_DIAGNOSIS_2026-09-13.md` and
  `eval_runs/0913_early_window_diagnosis/`.

### 6s. Codex: explicit zero-state/F32 contract before further training (09-13)

- New drill GRUs default to `windowed_gru_f32_v1`: zero hidden state per window,
  F32 training/eval/sequential AR sampling, no stateful-step/BPTT.
- Implemented in the actual shared `../edifice` recurrent builder. Both repos
  must be built/source-loaded; the older local recurrent module is not the policy path.
- Export/snapshot metadata and resume/warm-start checks prevent silent contract
  changes. Unstamped checkpoints retain legacy behavior, never silently zeroed.
- Six CPU tests pass. Zero-update CUDA smoke: exact export/reload logits,
  sequential logits differ by 1.64e-7 with all decisions matching. GPU cross-batch
  difference 0.001493 remains; see documented numerical tolerance.
- No training or live run. Round21 candidate and readiness gate remain unchanged.
- Next: fresh matching initialization, bounded fitting, unchanged per-case gate,
  then teacher-matched live validation. Do not reuse the old random-state init.
- Tracking: `docs/planning/EXECUTION_CONTRACT_2026-09-13.md`.

### 6t. Codex: fresh zero-state/F32 fit completes; five of six cases pass (09-13)

- User clarified the objective: reliable multishining/recovery as a local pipeline
  proof. No teacher-model distillation; direct recorded expert supervision suffices.
- Fresh GRU64 initialization, same21-epoch/4,788-update budget and prefix weighting.
  Source-loaded actual Edifice zero-state builder; initialization metadata and graph checked.
- Best aggregate export epoch20, loss0.0000657157. All three previous mistakes fixed.
  Teacher707/708, early107/108, canonical7076/7077. Five of six cases pass confidence gate.
- Remaining recovery4 index6/frame10 (action363/af17): should release X, P(X)=0.609787.
  A different failure from the old checkpoint. One canonical early miss also remains.
- Gate fails: no live run, promotion, or training extension. Candidate hash
  `c250c1b4e1f572364fbfe1d84ca488d52f8eea8484eebd8cef733d7d88cac5ef`.
- Next: audit the remaining windows against training-time inputs. Inherited
  previous-action dropout0.1 is applied during precomputation; eval uses0.0.
  This augmentation mismatch is a hypothesis, not a confirmed cause. Prefer a
  controlled no-augmentation proof if warranted, not distillation/threshold changes.
- Details: `docs/planning/ZERO_F32_FIT_2026-09-13.md`;
  artifacts: `eval_runs/0913_zero_f32_fit/round21/`.

### 6u. Codex: dropout audit separates remaining canonical/recovery misses (09-13)

- Same checkpoint, no training/live run. Canonical miss is index0/frame0/action361af2;
  recovery miss remains index6/frame10/action363af17. Both incorrectly press X.
- Canonical cold window has no previous actions: identical under all masks,
  P(X)=0.927712. Dropout cannot directly explain that input's miss.
- Recovery window is mask-sensitive: seeded10% masks give4/32 correct, target
  probability0.000122–0.999823. Unmasked P(X)=0.610294. All-slots-absent is worse.
- Historical mask was not retained; do not claim reconstructed training inputs
  or proven causation. Dropout is cached once, so64x repeats use the same mask.
- Added optional canonical rows and a seeded audit script; two focused regressions
  pass. Broad data suite retains an unrelated cache assertion failure.
- Next: controlled same-initialization/21-epoch **zero augmentation** fit with
  `--prev-action-dropout 0.0`, preserving previous-action inputs and frozen gates.
  Not started. Canonical cold-target fitting may need separate attention.
- Details: `docs/planning/PREVIOUS_ACTION_DROPOUT_AUDIT_2026-09-13.md`.

### 6v. Codex: same-init no-dropout fit passes frozen local proof (09-13)

- Authorized same21-epoch/4,788-update fit, changing only previous-action dropout
  to0.0. Kept queue inputs; initial parameter trees exactly match prior fresh init.
  Shuffle RNG is not a paired replay. Best aggregate export epoch20, loss0.0000798043.
- Offline:708/708 recorded teacher targets,108/108 early targets; all six confidence
  gates pass, worst early target probability0.997332. Canonical remains7076/7077
  with known cold frame0 X-release miss; that is outside the frozen six-case gate.
- Same checkpoint passes **12/12 local closed-loop responses**, chains13–14,
  all120frames, valid timing, no truncation/errors/divergence. Cold history,
  delay2, temperature1.0, libmelee AF. No gate relaxation or training extension.
- Candidate: `eval_runs/0913_no_dropout_fit/round21/candidate.bin`, SHA256
  `404a1f543889ddd4193380745fbc603c6c94bb4a200a3caa7cbca6ddf823182a`.
- The finite trained-handoff pipeline proof now passes. Do not equate this with
  arbitrary gameplay or general offstage recovery. No default deployment changed.
- Next: longer local episodes and held-out starts/perturbations on this frozen
  checkpoint, not distillation or another training sweep.
- Details: `docs/planning/NO_DROPOUT_FIT_2026-09-13.md`.

### 6w. Codex: human replay inspected; interruption benchmark baseline collected (09-13)

- Local session15:41:59:137s, max chain27,78 strict cycles,4 stocks lost.
 46 hitstun episodes:9 complete cycles,33 re-hit censors,4 deaths.15 ever reach
  grounded zero-hitstun readiness; successful ready-to-cycle usually8–9f, one289f.
- Frozen development manifest chooses hit502 (no return),1389 (fast),3152 (slow),
  two runs each,360f, same checkpoint, delay2/T1/libmelee AF, committed history.
-6/6 valid executions,0/6 strict full-cycle returns. Two never reach readiness;
  one has only34f follow-up; three have >=60f observation after readiness. No
  timing failures/drift/errors. Single shines/loose cycle counts are not recovery.
- New unfamiliar hit trajectories AND warm history differ from cold trained proof;
  do not claim clean causal attribution. Human opponent inputs replay non-reactively.
- Next: known-good warm-history controls and isolated-hit/then-stop cases, then
  validate teacher recovery corrections before adding training data. No training done.
-10 benchmark tests pass. Details: `docs/planning/INTERRUPTION_RECOVERY_2026-09-13.md`;
  artifacts: `eval_runs/0913_interruption_recovery/`.

### 6x. Codex: controlled recovery tests expose history sensitivity and restart failure (09-13)

- Warm known-good controls:6/12 pass vs12/12 cold. Neutral/sustain all pass;
  recovery4/75/146 all fail (chains1/0/0). Same checkpoint, valid drift/timing.
- Neutralize P2 only after the same human-hit handoffs:0/6 strict cycle returns.
  All357 checked P2 frames/run neutral, no later damage increments or stock loss.
  Four runs have220–327f follow-up after readiness, still no cycle.
- Remaining two fail the conservative readiness proxy but spend long spans
  CROUCHING(action40), excluded by that proxy; never call this continuous hitstun.
  Retain metric version/results; refine readiness coverage separately.
- Added --response-opponent replay|neutral (default replay), response-only helper,
  recorded-opponent audit and raw Peppi controller regression.12 focused tests pass.
- No training. Next: real input-only pre-handoff history on recovery clips,
  preserving early targets; validate teacher restart trajectories, then bounded
  fitting with both cold and warm regression gates. Do not reset history on hits.
- Details: `docs/planning/RECOVERY_CONTROLS_2026-09-13.md`.

### 6y. Codex: real context exported; teacher restarts and window parity validated (09-13)

- Teacher isolated-hit responses pass 3/3, strict cycles39/36/37, chains40/37/38.
  All1080 issued inputs and1074 delay2 targets validate. Case502 is on-loop;
  final report explicitly allows on-loop coverage, without relaxing input checks.
- Ready exports: `eval_runs/0913_context_recovery/clips_v6/*.frames` only.
  Nine cold/warm pairs,3564 targets,162 input-only prefix frames. Early targets
  retained;324 actual Agent/training window comparisons pass at1e-6 tolerance.
- Fixed negative countdown and player-counter lower-bound mismatches in live
  embeddings;16 focused tests pass. Local play wrapper source-loads both fixes.
- Same checkpoint warm recheck remains6/12: neutral/sustain pass, recovery
  4/75/146 fail both repetitions. All timing/drift/execution gates valid.
- No training. Next: context-aware per-case reporting, frozen update budget,
  bounded fitting, then cold/warm and both interruption gates. Do not reuse
  21 epochs blindly on the larger pool or train from partial earlier exports.
- Details: `docs/planning/RECORDED_CONTEXT_2026-09-13.md`.

## 7. Resume

```bash
cd ~/git/exphil && git log --oneline -5
systemctl --user list-units 'exphil*' --no-pager; pgrep beam.smp
tail -c 400 eval_runs/0912_g26_phase/train_a.log | tr '\r' '\n' | grep epoch | tail -1
# audit any pool at its training shifts (gates):
mix run scripts/audit_ms_pool_labels.exs --rollouts "<globs>" --openers "<globs>" --snippets <file> --shifts "3,4,5"
# coverage map (offline, 5 min): mix run scripts/probe_ms_coverage_map.exs --policy <ckpt> --delay-id 4 --out <json>
# closed-loop table policy column: scripts/scenario_suite.exs --driver policy --policy <ckpt> --reaction-delay 2 --character fox --manifest scenarios/ms_breaks_manifest.json --only "0,...,11" --runs 2 --temperature 1.0
```

Key files: `lib/exphil/training/labels.ex`, `lib/exphil/agents/multishine_expert.ex`
(`label_ahead/4`), `lib/exphil/eval/harness_rung.ex`, `lib/exphil/bridge/latency_probe.ex`,
`lib/exphil/agents/agent.ex` (`observe/4`), `scripts/audit_ms_pool_labels.exs`,
`scripts/probe_ms_coverage_map.exs`, `scripts/scenario_suite.exs`,
`docs/planning/INVARIANTS.md` (items 12-14), `docs/reference/GOTCHAS.md` (#115).
