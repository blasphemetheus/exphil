# HANDOFF 2026-07-23 (~00:10 orig; ⬆ UPDATED 02:50 — r16 DONE)

Supersedes HANDOFF_2026-07-22.md (which has the r16 root-cause writeup +
GOTCHA originals — read it for detail, this doc for the live state).
Companion reading unchanged: INTERP_NEXT_RESEARCH_2026-07-20.md,
docs/research/AGENT_SURVEYS_2026-07-20.md.

## ⬆ UPDATE 02:50 — r16 FINISHED, verdict in, flywheel P1+P2 verified

The "RUNNING STATE" section below is STALE (pre-completion). Current truth:

- **r16 converged ep89 @ 02:25, then the fan-out ran to `[newera8] done`
  @ 02:43. Launcher exited; mix-lock LIFTED (GPU idle, no training/relaunch
  active — three stale prior-session watcher tasks reported completion
  around 02:44, all no-ops; the lone `mix phx.server` beam has no EXLA so
  mix is safe).**
- **GATE-10 VERDICT: BC did NOT move initiation.** armed approaches/min
  mean 0.17 (per-game 0.13/0.25/0.38/0.0/0.25/0.0, target ≥1.0), report-
  card 6.8/10, conversions 6/119 (5%). → **r17 = ESCALATE branch** (heavier
  BC, then PPO approach-shaping). See the r16/r17 rows in OPEN TASKS.
- **Flywheel Priority 1+2 now `mix test`-GREEN (30/30) and smoke-validated
  on real r16 probes** — all "verify later" risks resolved (live frames
  carry stock/percent/facing; coach_report armed/min cross-validates the
  report_card). Fixed one test-only assertion bug. Details: flywheel-verify
  / flywheel-smoke rows + DATA_FLYWHEEL_DESIGN implementation log.
- **Direct tie-in:** the 125 passivity_window drill candidates auto-found
  on r16's probes ARE the "going-in" moments r17's escalation must target;
  human_drill (initiation demos) + seeded self-play (P4) are the levers.
- **Next after this:** flywheel Priority 3 (style-tag audit → NeutralScan
  opener gates → sparring ingest) now builds on a verified base. r17 launch
  is the parallel training track. `git push` the still-unpushed commits.

## RUNNING STATE — r16 TRAINING, epoch 75/100 (~00:05), ETA ~04:15

r16 relaunched successfully 2026-07-22 10:47 after the epoch-10 crash was
root-caused + fixed (probe empty-split, NOT OOM — see 07-22 handoff /
GOTCHA #71). It CLEARED the epoch-10 boundary that killed it and every
probe-eval since (10/20/…/70) has run clean. ~10 min/epoch (mamba_2 +
probe-reg double-forward).

- **Log:** `logs/overnight_newera8_r16_20260722.log` (note: dated 07-22,
  the launch day). Dead-run evidence preserved at `*_20260722_DIED.log`.
- **Output:** `checkpoints/mewtwo_combo_newera_r16_r13_policy.bin` (final,
  not present until done) + `_latest.bin` (playable snapshot, every 10ep)
  + `.bin.trainer.ckpt` (resume state) + `.bin.probe_curves.jsonl`.
- **Best epoch so far: epoch 70, loss 0.0200** — below r15's export band;
  the run exports the BEST epoch, so intermediate cosine-LR oscillation
  (e.g. 0.43 @ ep75) is not what ships.
- **Live pids (only valid until the run ends):** launcher
  `overnight_newera8.sh` pid 3098500; a watchdog from THIS Claude session
  (task b4vpo9gqz) — **it dies when context clears**, but its `notify-send`
  still pops a desktop toast on death while its bash process lives. There
  is NO other automated monitor for THIS run (r16 was launched before the
  watchdog-in-launcher integration; future runs auto-monitor).

### Check progress after context clear (no pid needed)
```
tail -5 logs/overnight_newera8_r16_20260722.log   # or grep "epoch .*loss"
ls -la checkpoints/mewtwo_combo_newera_r16_r13_policy.bin  # exists => done
nvidia-smi --query-gpu=utilization.gpu --format=csv,noheader  # ~88% => alive
```

## WHEN r16 FINISHES — READ THE GATE-10 VERDICT

THE experiment: does archive-human BC move **gate-10 (armed
approaches/min = initiation)?** r15 raised conversions but left gate-10
~0 (converts better, still won't go in). r16 adds direct human BC to
target exactly that.

After the checkpoint appears the launcher runs its probe fan-out (headless
ExiAI games) + report_card automatically; look for `SCORE:` and
`conversions=` lines in the log, and the StyleCard gate-10 number.
- **gate-10 > 0** → BC unlocked initiation → r17 = BC + #38 seeds combined.
- **gate-10 ≈ 0** → imitation isn't enough → escalate (PPO approach-
  shaping / heavier BC). See r17 PLAN in the 07-22 handoff.

## IF r16 DIED — EXACT RELAUNCH (reconstructed this session)

No stale checkpoint = fresh start. Run from `devenv shell` in ~/git/exphil.
Pool = 62 replays (48 base + 6 r13 probes + 8 extra) + 20 BC archive =
2,036,709 frames. The 8 extra rollouts (2 human demos + 6 r14 probes):
```
NEWERA8_TAG=r16 NEWERA8_BACKBONE=mamba_2 NEWERA8_MAX_EPOCHS=100 \
NEWERA8_ROUNDS=1 NEWERA8_DROPOUT=0.6 PROBE_STATEFUL=0 \
NEWERA8_CONVERSION_WEIGHT=3.0 NEWERA8_PROBE_REG=0.1 NEWERA8_PROBE_REG_EVERY=5 \
NEWERA8_PROBE_EVAL=10 NEWERA8_BC_REPLAYS='corpus/archive/mewtwo/*.slp' \
NEWERA8_BC_SAMPLE=20 NEWERA8_PREFLIGHT=1 \
NEWERA8_EXTRA_ROLLOUTS='/home/blewf/Slippi/Game_20260719T213810.slp,/home/blewf/Slippi/Game_20260719T213957.slp,probes/newera8/r14/plain/p1/Game_20260718T073713.slp,probes/newera8/r14/plain/p2/Game_20260718T073726.slp,probes/newera8/r14/plain/p3/Game_20260718T073738.slp,probes/newera8/r14/debounce/p1/Game_20260718T074551.slp,probes/newera8/r14/debounce/p2/Game_20260718T074603.slp,probes/newera8/r14/debounce/p3/Game_20260718T074615.slp' \
scripts/overnight_newera8.sh > logs/r16_launcher.log 2>&1 &
```
`NEWERA8_PREFLIGHT=1` dress-rehearses the full pool (~15 min, incl.
probe-eval) before committing epochs; the launcher now auto-starts a
liveness watchdog. Preserve any existing `*_20260722.log` first (the new
run reuses that path since the tag+date match) — see the DIED-log rename
in this session's history if relaunching on 07-22's date.

## THIS SESSION SHIPPED — 3 commits on main, NOT PUSHED

`3fb521f` r16 post-mortem: probe empty-split fix + OOM-diagnosis infra
(Probe empty-split guard + regression tests; Training.MemoryLedger — VmHWM
bytes/frame model, `logs/memory_ledger.jsonl`, 3 points now; dagger_drill
`--preflight` + `--memory-check` + per-epoch heartbeat+RSS + snapshot
reordered BEFORE probe-eval; overnight_newera8.sh describe_rc exit-status
+ NEWERA8_PREFLIGHT gate; fixed wrong-namespace ProbeRegularizer crash on
`--probe-eval-every` without `--probe-reg`). VALIDATED live on r16.

`baeb591` watchdog + GOTCHAS #71-77 + scenario --finalize + r17 plan
(scripts/train_watchdog.sh; GOTCHAS.md #71-#77; scenario_suite.exs
`--finalize` — UNTESTED; 07-22 handoff r17 plan).

`55e6aac` watchdog auto-integration + 2 design docs
(overnight_newera8.sh auto-starts train_watchdog.sh, `NEWERA8_WATCHDOG=0`
to disable; preflight fails loud on sub-window pool; DATA_SCALING and
STATEFUL_MAMBA2 design docs).

**TODO: `git push` these 3** when ready (deliberately unpushed this
session).

## OPEN TASKS (full snapshot — rebuild the CLI list from here)

| # | status | task |
|---|--------|------|
| r16 | **DONE 02:25** (converged ep89, loss 7.7e-4). VERDICT: **BC did NOT move gate-10** — armed approaches/min 0.13/0.25/0.38/0.0/0.25/0.0 (mean 0.17, target ≥1.0); report-card 6.8/10; conv 6/119 (5%). Best→`mewtwo_combo_newera_r16_best_policy.bin` | BC+recipe mamba_2 — initiation test FAILED |
| r17 | **ESCALATE branch SELECTED** (BC didn't unlock initiation) | per 07-22 r17 PLAN: (a) heavier BC weight, then (b) PPO approach-shaping. The flywheel passivity_window candidates (125 found on r16 probes) + human_drill initiation demos + seeded self-play (P4) are the concrete levers |
| flywheel-verify | **DONE 02:47** — 30/30 green (fixed 1 test-only assertion bug in flip test) | mix test gap_ledger/failure_scan/coverage all pass |
| flywheel-smoke | **DONE 02:48** — all 4 scripts validated on real r16 probes; all "verify later" risks resolved | auto_bookmarks (passivity 125/dropped 42/deaths 3 → stock/percent/facing confirmed live); coach_report (armed/min 0.17 CROSS-VALIDATES report_card exactly); coverage_ledger (76 vs 464 buckets — note: best on sparring, probe dummy skews bot percent low); find_situations (15 spaced retrievals). Graceful skip on 1 truncated .slp |
| flywheel-verify | **P1+P2 BUILT, PENDING** (gate for all flywheel work) | `mix test` gap_ledger/failure_scan/coverage test files — pre-verified standalone (36 assertions), expect green first try |
| flywheel-smoke | pending, after flywheel-verify | 5 real-replay smokes (auto_bookmarks, coach_report, coverage_ledger, find_situations); CONFIRM live Peppi frames carry stock/percent/facing; tune dropped-punish 15% / passivity 300f / min_corpus_frac to real dists — see VERIFICATION PENDING block in DATA_FLYWHEEL_DESIGN |
| flywheel-p3a | **DONE ~04:00** — inference was the only missing link; Agent style_id/style_tag + CLI flags + registry persisted at export; embed-parity green | style-tag wiring end-to-end |
| flywheel-p3b | **DONE ~04:05** — NeutralScan (+11 tests) + 2 StyleCard gates; REAL r16 probe: 35 openers, top_share 0.914, entropy 0.501 bits → both FAIL (the metric works) | opener taxonomy + diversity gates |
| flywheel-p3c | **DONE ~04:08** — ingest_sparring.exs smoked end-to-end (copies, merged sidecar, nested coach_report) | sparring ingest |
| flywheel-p4 | **DONE ~04:15** — selfplay_rollouts.exs LIVE-VERIFIED (6484f parseable rollout, both ports live: 27 distinct actions each); human_drill keeps discards as preference negatives; uncertainty logging (Agent + --uncertainty-log + uncertainty_gaps.exs consumer) live-verified. PPO *consumption* of selfplay seeds still gated on the PPO smoke | seeded self-play + pref pairs + uncertainty |
| flywheel-p5 | **DONE (morning 07-23)** — `ExPhil.Data.SituationIndex` (+7 tests): streaming per-file f16 shard build (127 archive files / 1.77M frames / 142s), brute-force cosine top-k + temporal spacing (added after the first live query showed one held moment flooding top-k). Live: r16 passivity gap g_54affc1c → 40 diverse human instances (0.999 cos), gap marked mined. `scripts/situation_index.exs` build/query; index at `cache/situation_index/mewtwo` (gitignored — rebuild in ~2.5 min) | retrieval v2 — the flywheel is now COMPLETE |
| parser-netplay-code | deferred, small | native Peppi: extract the netplay connect code (EXPH#288) — parser only exposes `name_tag` now; unblocks reliable coach_report/sparring bot-port autodetect |
| #38 | **DONE 03:55** — finalized + Peppi parses (2158f) + Mewtwo SDs (stocks 3→2→1). CAVEAT: one entry hit finalize_timeout (position-dependent float/DJ save, as the #38 note predicted) — @suicide may want a down-hold for full coverage; unfinalized runs drop cleanly | scenario_suite `--finalize` live test |
| stateful-mamba2 | **design ready** (STATEFUL_MAMBA2_DESIGN.md) | O(1) step for 60fps/rollback; implement init_state+step+parity test in edifice |
| data-scaling | **embed-pass DONE + flatness PROVEN 07-23** (SituationIndex shards; VmHWM 1.48/1.55/1.77 GB at 65k/415k/1.77M frames vs 19.9 GB all-in-RAM — see DATA_SCALING doc update); drill wiring still open: compact targets in shards + relabel first-pass + shuffle buffer, land WITH a full training smoke | wire shard-streaming into dagger_drill |
| PPO | repaired, UNRUN | smoke demo (r15 policy, cpu3, 20k steps); prereq for r17 escalation branch |
| interp | push | stage-diff → crosscoder → attribution graphs |
| #9 | SHELVED | netplay: home-fix done, handoff + Null-check open |
| #19 | operational | full 4-way ladder (r13/r14/screen/r15) not yet run |
| #23 | pending | G&W track (greg corpus on B2, not pulled) |
| #30 | seeded+archive | human_blewf + archive/mewtwo + greg |
| #33 | corpus ready, **blocked on data-scaling** | five-char program — needs streaming embed first |
| bake-off | later | broaden arch screen after mewtwo solid |

Parking lot: upstream drafts await Bradley (DO NOT POST); gate-10/StyleCard
calibration vs corpus.

## REFERENCE — new this session

- **GOTCHAS #71-#77:** probe empty-split, liveness+heartbeat monitoring
  (+ exit-status + read-the-log-tail), scenario finalize, mainline-home,
  pgrep self-match, Axon system_time seed, microbench≠full-graph.
- **Operational infra now in place for any run:** `--preflight` gate,
  memory ledger (real 2M-frame mamba_2 point), `train_watchdog.sh`
  (auto-started by the launcher), exit-code logging, per-epoch heartbeat.
- **Mix rule reminder:** r16 is training → do NOT run `mix` (relinks EXLA
  NIF → SIGSEGVs the run). Edit files freely; commit is safe; no
  compile/test/run until r16 is done.

## DRILLS BUILD (this session, ~01:00, while r16 trained — ALL UNTESTED)

Bradley's UnclePunch-style drill loop, built for the bot. Design +
rationale + ordered test plan: **docs/planning/DRILLS_DESIGN_2026-07-23.md**
(read that first). New/changed, uncommitted:
- `scripts/scan_bookmarks.exs` (NEW) — d_down-in-.slp bookmark scanner
- `scripts/build_seed_dir.exs` (NEW) — scoreboard → seed dir + seed_meta.json
- `scripts/human_drill.exs` (NEW) — savestate drill sessions on the GUI
  netplay build; taunt=save / L+R+taunt=discard; emits --mix-frames files
- `scripts/dagger_drill.exs` (EDIT) — slices seed rollouts to
  [handoff, handoff+window] when seed_meta.json sits beside them
All parse-checked only (no mix while r16 runs; scenario_suite.exs was
deliberately NOT touched — the r16 fan-out still invokes it). Feeds the
r17 #38 branch directly (build_seed_dir + slicing are the missing pool
hygiene for scenario seeds).

**Follow-on: docs/planning/DATA_FLYWHEEL_DESIGN_2026-07-23.md** — full
implementation spec for the data-accumulation pipeline. Build-order
table at the bottom; Priorities 2-5 still design-only.

**PRIORITY 1 IMPLEMENTED (2026-07-23 ~01:30, Opus, still under r16
mix-lock so parse-checked + standalone-logic-verified only):**
- `lib/exphil/eval/gap_ledger.ex` (+ test) — shared `gaps.json` currency
- `lib/exphil/eval/failure_scan.ex` (+ test) — 4 outcome detectors
  (neutral_loss, dropped_punish, death_sequence, passivity_window) over
  ScenarioScan slim frames; P1=bot, `flip/1` for P2 bots
- `scripts/auto_bookmarks.exs` — FailureScan → gap ledger
- `scripts/coach_report.exs` — post-set report (ReplayStats combat stats +
  FailureScan candidates) → `logs/coach/<ts>/` + gap ledger
- 20 logic assertions PASS standalone (detectors are pure; ran outside the
  project via a scratchpad munge harness — no mix touched).
- Two spec corrections: (1) bot-port detection can't use the EXPH#288
  connect code — native parser only exposes `name_tag`; coach_report uses
  `--bot-port` (default 1) / `--bot-tag`. (2) passivity_window uses a
  consecutive ≥300f run, not 300-of-600.

**PRIORITY 2 ALSO IMPLEMENTED (2026-07-23 ~01:45, Opus, parse-checked +
standalone-verified only):**
- `lib/exphil/eval/coverage.ex` (+ test) — 5-feature situation buckets,
  occupancy ledger, Laplace-smoothed bot-vs-corpus diff
- `scripts/coverage_ledger.exs` — occupancy diff → coverage gaps (cached
  corpus ledger via `--corpus-ledger`/`--save-corpus`)
- `scripts/find_situations.exs` — bucket-grep retrieval (`--like`/`--bucket`/
  `--gap`) → corpus matches, marks gap "mined", `--char-filter`
- 16 more logic assertions PASS standalone (36 total across P1+P2).

**⚠️ VERIFICATION PENDING — all of P1+P2 is parse-checked + logic-verified
but NOT `mix test`-run (r16 mix-lock). Turnkey checklist (unit tests +
5 real-replay smokes, in order) is in the "VERIFICATION PENDING" block of
DATA_FLYWHEEL_DESIGN_2026-07-23.md.** DO the gate-10 verdict FIRST, then
run that checklist. Known real-run risks flagged there: slim-frame field
drift (stock/percent/facing on live Peppi frames), coach_report bot-port-2
arg orientation, `--char-filter` metadata, and tunable thresholds
(dropped-punish 15% / passivity 300f / coverage min_corpus_frac) that are
guesses until fit to real distributions.

Priority 3 (style-tag audit + NeutralScan opener gates + sparring ingest)
is the next row — but should wait until P1+P2 are actually test-green so
it builds on a verified base.

## LOOSE ENDS

1. `git push` the 3 commits.
2. r16 result → gate-10 verdict → pick the r17 branch.
3. #38 `--finalize` needs its first live test (untested SD input).
4. Streaming-embed wiring is the gate for the five-char program (#33).
5. My-session watchdog dies on context clear; r16 has no persistent
   monitor after that (desktop toast still fires if the bash process is
   alive). Re-check the log manually, or relaunch monitoring:
   `scripts/train_watchdog.sh --pid <launcher> --log <log> --interval 120 &`
