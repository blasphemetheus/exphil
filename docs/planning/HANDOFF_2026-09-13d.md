# HANDOFF 2026-09-13d — warm recovery closed; off-loop labels abstain

Supersedes `HANDOFF_2026-09-13c.md` as the resume point (its ledger and the
09-12b addendum stay valid history). Session: Claude Fable, 09-13 evening.

## State at write time

- Nothing running. GPU free. Everything committed (exphil + siblings
  edifice `c3f46c6`, libmelee_ex `a44e57e`).
- **Best multishine-proof candidate:**
  `eval_runs/0913_context_fit/round21/candidate.bin`
  SHA256 `8bd1b009a5504cdb7aa42c12c65e6f4d2de90aa294e096cac0ec319d2296426f`
  (contract unchanged: windowed GRU F32, zero state, GRU64x2, AR head,
  window 16, queue 3, prev action, reaction delay 2).
- **Playable fallback (still what `scripts/play_multishine_proof.sh` loads):**
  `eval_runs/0913_no_dropout_fit/round21/candidate.bin` (404a1f54...).
- Full write-up: `docs/planning/CONTEXT_FIT_2026-09-13.md`.

## What changed this session

1. **The 09-13 sessions' work compiles natively and is committed.** The
   authoring sessions source-loaded modules with `EXPHIL_SKIP_NIF_COMPILE=1`;
   on slanka `mix compile` is clean and 165 focused + 39 label + gate tests,
   edifice recurrent (207), libmelee_ex (569) are green. New launchers use
   plain `mix run` — no `-r` source-loading needed here.
2. **Warm-history recovery gap CLOSED** (the 09-13c goal). Trained the same
   recipe on the canonical fixture + 18 cold/warm clips (10,641 targets,
   21 epochs = 10,206 updates). Warm controls 12/12 (was 6/12); isolated
   interruptions 6/6 strict recoveries with teacher-level chains 35-39
   (was 0/6); replay-opponent interruptions recover in 6/6 (was 0/6).
   **One regression:** cold sustain-900 chain 1 (4/4 reps; prior candidate
   4/4 pass) — the live cold seam, not the fit. Cold is the earlier proof's
   artifact contract; warm is the deploy contract.
3. **Off-loop delayed labels ABSTAIN** (the g-line's leading open defect,
   12b §6c/6d): `MultishineExpert.label_ahead/4` returns `:skip` off the
   loop at k > 0; `Labels.at_delay` default `off_loop: :drop`; `:hold` is
   the explicit legacy mode (`dagger_drill --off-loop-labels hold`,
   `audit_ms_pool_labels --off-loop hold`, fingerprinted). Experts without
   `label_ahead/4` raise unless `:hold` is asked for. INVARIANTS 14 amended.
   Consequence: at a delayed rung the expert supervises only the loop;
   recovery supervision = recorded teacher futures. NOT yet run at the
   g-line rungs 3/4/5.
4. **Fit report + gate cover cold AND warm clips**, indexed by supervised
   target (`measure_teacher_fit.exs --recorded-frames GLOB
   --expected-targets N`; `EarlyTeacherGate` reads the report's own
   `gate_cases`/`targets`). Baseline of the prior candidate on the 18 clips
   is in `eval_runs/0913_context_fit/baseline_round21_fit.json` (gitignored,
   >5MB; regenerable).
5. Budget lesson: the handoff's 9-epoch budget under-fit (gate 13/18, loss
   still falling 25%/epoch); 21 epochs (the prior proof's) converged.
   `round9/VERDICT.md` records the failed round; nothing was extended
   within a run.
6. `scripts/compare_checkpoint_params.exs A B` — recursive parameter-tree
   equality (used to prove the initialization is the prior proof's).

## Open decisions for Bradley

1. **Install round21 as the play-script default?** Warm + interruption
   gates pass; the cold-900 regression is in a contract nobody deploys. If
   yes: point `scripts/play_multishine_proof.sh --policy` at it and do the
   live look. If the cold gate must also hold, the next round needs a
   cold-seam-faithful clip (the live cold seam = pending prefix commands +
   cold padding; the training "cold" is repeat-first-frame only).
2. **g-line (ms_g2x, rungs 3/4/5) under abstain labels:** the equal-budget
   hold-vs-drop comparison RECOVERY_LABEL_CONFIRMATION asked for is now one
   flag away (`--off-loop-labels`), but recovery supervision at those rungs
   must then come from recorded teacher clips — the g-line has none at
   k=3/4/5 yet (clips_v6 are validated at delay 2 only).
3. 12b §4 decisions (v3 deploy card, coverage-round design, ep57 id-1)
   unchanged.

## NEXT (in order)

1. Bradley's live look at round21 (`play_multishine_proof.sh`, pass its
   path) — the human session is the broader test.
2. Held-out starts: new handoffs the model was NOT trained on (positions,
   facing, opponent characters), exported cold+warm via
   `eval_runs/0913_context_recovery/prepare.sh` + teacher validation, gated
   by the same chain. Coverage one dimension at a time.
3. Cold-seam question (decision 1) only if Bradley wants the cold gate.
4. `dolphin_disconnected` in cold-history batches (3/24 vs 0/36 warm).
5. g-line: record teacher recoveries at rungs 3/4/5, then hold-vs-drop.

## Resume commands

```bash
cd /home/blewf/git/exphil
pgrep beam.smp || true
git status --short
cat eval_runs/0913_context_fit/round21/progress.log
jq -r '.cases[] | [.case,.history,.early_correct,.minimum_target_probability] | @tsv' eval_runs/0913_context_fit/round21/fit_gate.json
# repeat the whole chain into a new directory (train -> fit gate -> live):
#   systemd-run --user --unit=exphil-ctx --working-directory=$PWD --collect \
#     -p StandardOutput=append:$PWD/eval_runs/0913_context_fit/run.log -p StandardError=append:$PWD/eval_runs/0913_context_fit/run.log \
#     devenv shell -- bash eval_runs/0913_context_fit/run.sh eval_runs/0913_context_fit/NEW 21
```

## Addendum 22:50 — Bradley's live look at round21: PASSED

`bash scripts/play_multishine_proof.sh --policy eval_runs/0913_context_fit/round21/candidate.bin`,
Fox vs Bradley (Fox, GC adapter) on FD, reaction 2, T=1.0. Bradley: "easily the
best version of this that I've played." Strict benchmark of the session replay
(`eval_runs/local_multishine_20260913_224806/`, `observed.log`) vs the 15:41
session on the prior candidate (INTERRUPTION_RECOVERY_2026-09-13):

| | prior candidate (15:41) | round21 (22:48) |
|---|---:|---:|
| playable frames | 8,203 | 6,162 |
| max chain | 27 | 101 |
| completed cycles | 78 | 330 |
| ground shines / min | 54.4 | 209.2 |
| stocks lost | 4 | 1 |
| hit episodes: recovered / total | 9 / 46 | 17 / 31 (12 re-hit before ready, 1 death, 1 end) |
| ready-to-cycle frames (recovered) | 31,289,9,9,8,9,8,8,8 | 8-11 typical; 51/84/122/141/170 outliers |

Same-day, same opponent, n=1 session each; descriptive, not a decider. Decision 1
(install round21 as the play-script default) is effectively answered by use;
the script still hard-codes the prior path — pass `--policy` until it is repointed.

## Addendum 09-14 morning — reaction-4 control PASSED; g-line hold-vs-drop running

Bradley's four questions, answered:
- **AR head: keep it.** The proof line passes every gate with it (incl. the
  human session); the 09-12b bench pending an entropy study is RETIRED by
  evidence.
- **Deterministic: no.** Everything runs at T=1.0 sampling; argmax buttons
  collapse (08-28) and the no-bespoke-decode rule (08-29) stands.
- **Reaction-4 control:** DONE, passes — `docs/planning/DELAY4_PROOF_2026-09-14.md`.
  The proof recipe trained at delay 4 (queue 5, fresh init: the delay-2 init
  is 328 wide vs 352) chains 13-14 on cold 12/12 AND warm 12/12 at
  `--reaction-delay 4`, recovers 5/6 isolated + 6/6 replay-opponent
  interruptions. Harness rung 4 is fine; ep57's chain-1 there was its
  training. PIPELINE_PROOF gate 1 closed. Candidate
  `eval_runs/0914_delay4_proof/round21/candidate.bin` (424ae2f1...).
  Residual: 1/12 interruption runs = ready then empty hop + 5 s crouch
  stall (the prior candidate's stall too).
- **Equal-budget hold vs drop on the g26 pool:** RUNNING as
  `exphil-0914-g26ab` (`eval_runs/0914_g26_hold_vs_drop/run.sh`): g26a
  recipe x `--off-loop-labels hold|drop`, 40 epochs each, then map at id 4,
  stand floor + CPU gate at reaction 4 (ep 30/40). ~3.5 h from 11:27.
  Read `eval_runs/0914_g26_hold_vs_drop/progress.log`. NOTE the drop arm
  has NO recorded recoveries at rungs 3/4/5 (none exist yet) — it tests
  "wrong recovery labels" vs "no recovery labels".

**What a larger version is** (Bradley's question): same contract/recipe,
scaled in this order — delay (train+gate at 4: now proven), coverage
(held-out starts/positions/facing/opponents, each exported cold+warm via
`prepare_recorded_context.exs --delay k --policy none` then parity with the
trained candidate), then capacity last. GRU64 has not been the bottleneck
anywhere. Tooling landed: `prepare_recorded_context.exs` is delay-generic
(`--delay/--queue-depth/--context/--reports/--policy`), `measure_teacher_fit.exs`
takes `--delay/--queue-depth`, `compare_recorded_clips.exs` (semantic clip
equality; term_to_binary bytes are not stable across exports).

## Addendum 09-14 14:30 — hold vs drop READ OUT: drop wins every readout

`docs/planning/G26_HOLD_VS_DROP_2026-09-14.md`. Same g26a recipe x one flag,
40 epochs each, reaction 4: stand floor hold 371 / drop 391 per min (g26a
67, ep57 296); CPU gate chain hold **1** / drop **14-52**; coverage map drop
flat 0.999 on every axis incl. mirror (hold 0.874, opp_action 0.804). Under
drop ~85% of the g-line's off-loop frames vanish at the trained shifts. The
held labels were the g-line's defect; g26a's floor collapse also involved
the old guard (this rerun of its recipe does not reproduce 67/min). Not
separated further; not gating. Checkpoints `checkpoints/ms_g27{hold,drop}*.bin`.

Scaled-bot recipe is now settled: proof contract + AR head + T=1.0, drop
labels, recorded recoveries per rung as the only delayed recovery
supervision, delay proven at 4, coverage next, capacity last. Nothing
running at write time.

## Addendum 09-14 evening — coverage round at rung 4: held-out handoffs PASS

`docs/planning/COVERAGE_ROUND_2026-09-14.md`. 13 input-driven source games
(5 opponent bodies + Bradley ghost) -> 51 validated delay-4 handoffs (102
clips) -> train 39 / hold out 12 -> full-pool candidate
`eval_runs/0914_coverage_round/round21/candidate.bin` (90d63f35...).
At reaction 4: original 9 cold 12/12, warm 12/12; coverage train 75/78;
**held-out 24/24 neutral opp, 22/24 under the replay opponent**, strict
recoveries 18/18 at 9-10 f. Held-out teacher-forced early fit 94.7%.

Two things fixed/found on the way: the suite hard-coded port 2 = Fox
(every non-Fox prefix drifted at frame -38; now read from the replay,
`--opponent-character` overrides), and CPU-AI ports are UNREPLAYABLE
(stick floats no controller byte reproduces) -> source games must be
input-driven (`gen2.sh`: policy vs ghost inputs on the right body).
Disk: root was 100% (GOTCHA #116); cache/embeddings now on /data.

Residuals: slow re-entry from far-from-loop neutral starts (100-200 f);
Fox-ghost hit 299 weak under pressure. Nothing installed; the play script
still loads the delay-2 round21 (`--policy` to override; the coverage
candidate is a delay-4 checkpoint — play it at `--reaction-delay 4`).
