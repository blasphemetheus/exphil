# HANDOFF 2026-09-13d — warm recovery closed; off-loop labels abstain

Supersedes `HANDOFF_2026-09-13c.md` as the resume point (its ledger and the
09-12b addendum stay valid history). Session: Claude Fable, 09-13 evening.

## State at write time

- Nothing running. GPU free. Everything committed (exphil + siblings
  edifice `c3f46c6`, libmelee_ex `a44e57e`).
- **Best multishine-proof candidate:**
  `eval_runs/0913_context_fit/round21/candidate.bin`
  SHA256 `8bd1b009a5504cdb7aa42c12c65e6f4d2de90aa294e096cac0ec319d2296426f`
  (contract unchanged: windowed GRU F32, zero state, GRU64x2, AR head,
  window 16, queue 3, prev action, reaction delay 2).
- **Playable fallback (still what `scripts/play_multishine_proof.sh` loads):**
  `eval_runs/0913_no_dropout_fit/round21/candidate.bin` (404a1f54...).
- Full write-up: `docs/planning/CONTEXT_FIT_2026-09-13.md`.

## What changed this session

1. **The 09-13 sessions' work compiles natively and is committed.** The
   authoring sessions source-loaded modules with `EXPHIL_SKIP_NIF_COMPILE=1`;
   on slanka `mix compile` is clean and 165 focused + 39 label + gate tests,
   edifice recurrent (207), libmelee_ex (569) are green. New launchers use
   plain `mix run` — no `-r` source-loading needed here.
2. **Warm-history recovery gap CLOSED** (the 09-13c goal). Trained the same
   recipe on the canonical fixture + 18 cold/warm clips (10,641 targets,
   21 epochs = 10,206 updates). Warm controls 12/12 (was 6/12); isolated
   interruptions 6/6 strict recoveries with teacher-level chains 35-39
   (was 0/6); replay-opponent interruptions recover in 6/6 (was 0/6).
   **One regression:** cold sustain-900 chain 1 (4/4 reps; prior candidate
   4/4 pass) — the live cold seam, not the fit. Cold is the earlier proof's
   artifact contract; warm is the deploy contract.
3. **Off-loop delayed labels ABSTAIN** (the g-line's leading open defect,
   12b §6c/6d): `MultishineExpert.label_ahead/4` returns `:skip` off the
   loop at k > 0; `Labels.at_delay` default `off_loop: :drop`; `:hold` is
   the explicit legacy mode (`dagger_drill --off-loop-labels hold`,
   `audit_ms_pool_labels --off-loop hold`, fingerprinted). Experts without
   `label_ahead/4` raise unless `:hold` is asked for. INVARIANTS 14 amended.
   Consequence: at a delayed rung the expert supervises only the loop;
   recovery supervision = recorded teacher futures. NOT yet run at the
   g-line rungs 3/4/5.
4. **Fit report + gate cover cold AND warm clips**, indexed by supervised
   target (`measure_teacher_fit.exs --recorded-frames GLOB
   --expected-targets N`; `EarlyTeacherGate` reads the report's own
   `gate_cases`/`targets`). Baseline of the prior candidate on the 18 clips
   is in `eval_runs/0913_context_fit/baseline_round21_fit.json` (gitignored,
   >5MB; regenerable).
5. Budget lesson: the handoff's 9-epoch budget under-fit (gate 13/18, loss
   still falling 25%/epoch); 21 epochs (the prior proof's) converged.
   `round9/VERDICT.md` records the failed round; nothing was extended
   within a run.
6. `scripts/compare_checkpoint_params.exs A B` — recursive parameter-tree
   equality (used to prove the initialization is the prior proof's).

## Open decisions for Bradley

1. **Install round21 as the play-script default?** Warm + interruption
   gates pass; the cold-900 regression is in a contract nobody deploys. If
   yes: point `scripts/play_multishine_proof.sh --policy` at it and do the
   live look. If the cold gate must also hold, the next round needs a
   cold-seam-faithful clip (the live cold seam = pending prefix commands +
   cold padding; the training "cold" is repeat-first-frame only).
2. **g-line (ms_g2x, rungs 3/4/5) under abstain labels:** the equal-budget
   hold-vs-drop comparison RECOVERY_LABEL_CONFIRMATION asked for is now one
   flag away (`--off-loop-labels`), but recovery supervision at those rungs
   must then come from recorded teacher clips — the g-line has none at
   k=3/4/5 yet (clips_v6 are validated at delay 2 only).
3. 12b §4 decisions (v3 deploy card, coverage-round design, ep57 id-1)
   unchanged.

## NEXT (in order)

1. Bradley's live look at round21 (`play_multishine_proof.sh`, pass its
   path) — the human session is the broader test.
2. Held-out starts: new handoffs the model was NOT trained on (positions,
   facing, opponent characters), exported cold+warm via
   `eval_runs/0913_context_recovery/prepare.sh` + teacher validation, gated
   by the same chain. Coverage one dimension at a time.
3. Cold-seam question (decision 1) only if Bradley wants the cold gate.
4. `dolphin_disconnected` in cold-history batches (3/24 vs 0/36 warm).
5. g-line: record teacher recoveries at rungs 3/4/5, then hold-vs-drop.

## Resume commands

```bash
cd /home/blewf/git/exphil
pgrep beam.smp || true
git status --short
cat eval_runs/0913_context_fit/round21/progress.log
jq -r '.cases[] | [.case,.history,.early_correct,.minimum_target_probability] | @tsv' eval_runs/0913_context_fit/round21/fit_gate.json
# repeat the whole chain into a new directory (train -> fit gate -> live):
#   systemd-run --user --unit=exphil-ctx --working-directory=$PWD --collect \
#     -p StandardOutput=append:$PWD/eval_runs/0913_context_fit/run.log -p StandardError=append:$PWD/eval_runs/0913_context_fit/run.log \
#     devenv shell -- bash eval_runs/0913_context_fit/run.sh eval_runs/0913_context_fit/NEW 21
```
