# HANDOFF 2026-07-31 — resume point: queue-as-input campaign, grind arm in flight

Supersedes HANDOFF_2026-07-30.md's "next" sections (that file remains the
record of the 07-30 breakthroughs: sync-d2 harness, farm-11 perfect bots,
Direct loopback, delay-smear measurement). Everything through `b15cc23`+
is on origin/main. Bradley done playing online for now — full capacity OK.

## IN FLIGHT at handoff time

**Grind session 1, arm 3 (`jq`) training** — detached nohup (survives
session restarts), pinned cores 8-31 nice 15, VRAM 0.25:
- Script: `eval_runs/d3_grind1.sh`; log: `eval_runs/d3_grind1_20260731.log`
- Arm jq = multi-delay {2,3,4} + pipeline-offset 2 + shift-jitter 1 +
  queue-depth 4 + delay-id. Started 16:11; ~30 min train + 3 realtime
  evals (~4 min). RESUME ACTION: read the log tail, collect the jq
  d2/d3/d4 numbers, consolidate the grind table (below), update this doc.
- Future runs may unpin (drop `nice/taskset`, restore
  `--emulation-speed 0` in evals) — Bradley is off the GPU/CPU.

## Grind-1 results so far (1-run screening, sync headless, stand)

| arm | recipe | d2 | d3 | d4 |
|---|---|---|---|---|
| jitter5 | ad3+po2, jitter ±1, delay-id | 82 c2 | 78 c2 | 105 c2 |
| mdq | multi-delay d+2 shifts, queue4, delay-id | 77 c2 | 73 c1 | **111 c9** |
| jq | both | PENDING | PENDING | PENDING |

mdq's d4 c9 = first eval where the queue channel HELPED anywhere.
Neither prereg passed; d3 rung still unbroken by 5090-data training
(only laptop-lineage R3 chains there: 133-194/min c2-84).

## Queue-as-input state (the day's build)

- **Architecture COMPLETE and verified end-to-end**: embedding channel
  (`queue_depth` K slots + `with_delay_id` one-hot 9; K=1 ==
  classic prev-action, pinned byte-identical), data pipeline (per-slot
  boundary/dropout/DAgger conventions; DAgger slots read
  `frames[i-k+1].prev_controller` so expert corrections never leak),
  live agent ring buffer + `delay_id` (from --frame-delay), export/load
  carry the layout (flat config keys; the embed_config map is a legacy
  flattened thing that drops unknown keys — export whitelist is in
  `imitation/checkpoint.ex`). Tests: `test/exphil/training/queue_input_test.exs`.
- **Two config structs exist** (%Embeddings.Game{} AND %Game.Config{});
  datasets carry the former. New embed fields go in BOTH (learned hard).
- **Live-queue forensics instrument**: `EXPHIL_QUEUE_TRACE=1` on any
  play script logs per-frame ring contents + GAME-APPLIED inputs
  (`applied=` from libmelee controller_state — replay-free ground
  truth). Verdict: decode->send->apply chain is PERFECT (100.0%
  agreement); live decision->application latency at --frame-delay N is
  **N+2** (the intrinsic +2, now measured input-side). Under the correct
  frame correspondence the queue indexing was right; the LABEL shift is
  what should be N+2 (`--pipeline-offset 2`).
- **queue1 lesson**: teacher-forced queue channels amplify exposure
  bias (58-73/min vs control's 239/99/95 despite 3x lower loss).
  Un-run mitigation: scheduled sampling ON QUEUE SLOTS (needs SS module
  extension; module also has a pre-existing broken backend-mixing test).

## Drill flags added today (dagger_drill.exs + _freeze variant)

`--opening-replays GLOB` (farm-11 coverage in drills), `--x-hold-extend N`,
`--queue-depth K`, `--with-delay-id`, `--multi-delay "2,3,4"` (per-frame
delay tags), `--pipeline-offset P` (shift = d+P), `--shift-jitter J`
(deliberate R3-style label smear, per source list, draws logged).
`dagger_drill_freeze.exs` = heads-only fine-tune variant (FREEZE_DONOR
env; grad-mask optimizer wrapper + donor trunk splice at export).

## Machine/ops facts (today's hard-won)

- **Freeze root cause**: EXLA `memory_fraction: 0.75` = 24.6GB VRAM
  upfront starved the desktop (nvidia NVKMS failures) -> pressure spiral
  -> hard reboot. Fix: `EXPHIL_GPU_MEMORY_FRACTION` env (farm scripts
  use 0.25); earlyoom added to dotfiles (needs `nos`).
- **Eval cleanup kills BOT Dolphins only** (grep appimage-run path) —
  the unscoped kill took out Bradley's live game once.
- **Ports**: bot runs ALWAYS `--slippi-port 51442` (launcher owns
  51441); netplay port 2626=bot default, Bradley's forced to 2660.
- **Wifi saga** (if netplay drops recur): Spectrum router band-steering
  (WNM Disassociation Imminent) + client band-roaming; BOTH NM profiles
  ("Dr. Felix Heathcliff Cilantro" and "... 1" — the ACTIVE one is the
  " 1" duplicate) now pinned to BSSID A0:8A:06:FF:8A:11 band a. Router
  fix = disable Smart Connect / split SSIDs (Bradley's court). Black-box
  recorder pattern in eval_runs/wifi_blackbox.log (stopped).
- **Launcher/NixOS**: nix-ld libraries (40 libs) + 
  APPIMAGE_EXTRACT_AND_RUN=1 in dotfiles = update-proof; launcher's
  eternal-redownload loop was its version probe failing the same way.
- Mainline replay writer DIES mid-game randomly (2 of 3 today) —
  `SlpRepair.repair_gaps` handles zero-gap corruption;
  `parse_lenient` on gap files can produce frame-MISALIGNED parses
  (anchor-check before trusting; the state trace is the safer ground
  truth). Bot-side replays remain unreliable; fix someday.
- Busy-guard self-match: wrapper cmdlines containing "dagger_drill.exs"
  make eval_live_protocol REFUSE (pgrep matches the wrapper). Run
  trains and evals via script FILES, or as separate commands.

## Next moves (in order)

1. Collect jq results; consolidate grind-1. If any arm shows d3/d4
   chains >= 20, iterate that arm (more rollouts, 3-run confirm,
   record-grade eval).
2. **SS-on-queue** — the principled exposure-bias fix for the queue
   channel (extend ScheduledSampling to swap queue slots with decoded
   outputs; fix its broken test while in there).
3. Consider DAgger rounds ON TOP of the best multi-delay base (collect
   at d3 with temperature from the mdq/jq policies, aggregate, retrain).
4. The Direct rematch once any policy chains >= 20 at two rungs:
   loopback recipe in HANDOFF_2026-07-30 (bot home
   ~/.config/slippi-dolphin-bot, EXPH#288; measure cal on the netplay
   replay via repair_gaps).
5. Parked: Mewtwo transfer (lock-in warning), PPO under delay (Bradley
   wants eventually), forward-model compensation (#6, most innovative).

## Checkpoint inventory (today's, all on B2 too)

ms_farm11_{a1,a2,b1,b2} (perfect d2 bots, a2=294 record before freeze2),
ms_d3_freeze2 + ms_d3_diverse1 (387/min WHOLE-RUN chains at d2 — the
frame-perfect regime), ms_q_ctrl1 (multi-delay generalist), ms_q_queue1
(teacher-forced queue, config-repaired), ms_d3_ad5, ms_g1_{jitter5,mdq}
(+ jq when it lands).
