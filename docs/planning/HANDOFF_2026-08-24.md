# HANDOFF 2026-08-24 — online menus fully closed-loop; JIT is the last wait

**Resume protocol: read this + `MEMORY_WATCH_PROGRAM.md` (the
"Late-night arc 2026-08-24" section) + `JIT_WARMUP.md`. Supersedes
HANDOFF_2026-08-23. Repos: exphil `a090c6a`, libmelee_ex `79e8938`,
all suites green (libmelee 559/0, bridge 66/0, agents 121/0).**

## State: the online flow, end to end (all live-validated tonight)

Post-JIT: **menus 0.5s → pick lands DURING warmup → keyboard
confirmed 61ms after the first START pulse → code typed+verified
1.7s ≈ 2.5s total** (was ~25s two nights ago). Every step is
evidence-driven:

- `online_menu_selection` 0x804D7788 (Ranked0/Unranked1/Direct2/
  Teams3/Party4) merged onto main-menu gamestates — kills the
  "Unranked hover".
- PROBE-STEERING at the online CSS: the A-press probe is the position
  sensor (selection word names the picked portrait; B reclaims REAL
  mis-picks, bounded 120f). The hover byte is STALE there (local-CSS
  residue; its earlier "online validation" was luck) and helper
  steering was frozen-snapshot luck.
- Selection words read FRESH each frame via /proc pread (MEM1 located
  once by a background task; watcher on-change semantics are hostile
  to these words and every seed/staleness heuristic was
  timing-fragile). Online unselected sentinel = 26 (no-roster ids
  classify :none via normalize_selection/2).
- `online_menu_depth` 0x804060E0: CSS=2, keyboard=3, SEARCHING=2 (the
  searching screen IS the CSS with a status checklist — screenshot).
  Consumer shipped: depth==3 → instant handback (pulse wait 2s→61ms)
  + keyboard-strand protection. **Deliberately NO 3→2 re-arm** (that
  edge fires at every search start). Z (not B) cancels a search.
- BlindCss = event-driven phase machine (steer/press/confirm/pulse),
  budgets = legacy fallbacks, silent-watcher trace pinned bit-exact.
  Warmup FREEZES the steer budget (probes own the JIT window; a
  mid-warmup pick parks at :pulse — no-START interlock kept).
- ONE bounded (500ms) watcher snapshot per menu frame; nothing slow
  inline (two spectator-starvation dolphin_disconnected incidents:
  multi-call frames, inline /proc scan retry). Unknown submenu =
  WAIT, never B.

## JIT (the last wait): docs/planning/JIT_WARMUP.md

- EXLA exec cache: wired, measured (26.5 cold / 13.4 warm), then
  **CONVICTED live** — cached executables hang inference mid-game
  (counter frozen, inputs latched down-B) — **default-off**
  (EXPHIL_XLA_EXEC_CACHE). Bisect owed.
- The whole ~20s lives in the temporal `warmup_sample` branch; stage
  instrumentation (sample1/sample2/confidence) is IN — any next boot
  logs the breakdown. Next: attribute the residual, then autotune
  cache flags, then the resident policy server (structural answer).

## Status-checklist hunt: HARVESTED, verify owed

The hunt ran (libmelee_ex tmp/mw_checklist_hunt.exs, log
tmp/mw_visual/chk.log): 4 SEARCHING-flag candidates with the clean
on-at-search/revert-on-Z-cancel signature. Best: **0x804EE960
(1 idle / 0 searching — inverted), STATIC region** like every other
winner; the other three (0x80638FDC / 0x80639F3C / 0x80649990,
0→1) are heap-region. OWED: a second-boot verify run (same harness)
before MemoryMap promotion as :online_searching. Consumer ideas:
search-state readback for unattended session management (app #8) and
the MENU STUCK report. The picked/locked transitions had 463/3678
candidates — too noisy this round; re-hunt with tighter windows if
those bits are ever needed.
- Odd observation not chased: the searching screen displayed
  "Searching for EXPH#288" (the bot's OWN code) — display semantics
  unclear.

## Next (order)

1. Harvest the checklist hunt (chk.log) → MemoryMap + tests.
2. JIT residual attribution (boot once, read the new warmup stage
   lines) → update JIT_WARMUP.md → pick the next lever.
3. FoD/PS stage-internal address hunt (#6 refinement).
4. Stage-pinned crown decider (ep4 vs g15; FD + one agreed stage) —
   the menu stack makes deciders cheap now.
5. Cache-hang bisect (per-function enable) when JIT work resumes.

## Ops laws (unchanged + tonight's additions)

--verbose for observability sessions; teardown pkill set includes
`[s]lippi-dolphin-bot`; pkill NEVER in compounds (self-match killed
launch chains twice tonight); NO mix while any exphil beam lives
(violated once tonight — killed g7's beam via NIF swap); wall-clock
presses vs free-running dolphin; measure before wiring (the depth
probe killed a harmful feature before it shipped).
