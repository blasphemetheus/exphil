# HANDOFF 2026-08-12 — orientation + Bradley's tackle order

Supersedes HANDOFF_2026-08-10b.md (whose addenda are the raw log of
08-10 → 08-12). Everything committed through `1cf6a44`.

## STATE OF THE LAB

**Two crowns:**
- Specialist: `ms_g15_oppmask_full.bin` (multishine; d4 netplay / d3
  local; netplay chain record 46).
- Generalist: `fox_il_v2_convC2_20260811_222129_best_policy.bin`
  (crowned by 6-game blind human decider 08-12: only human stock taken,
  Bradley's blind best/worst both matched the seal). **Delay-0 — cannot
  netplay** until delay-trained. edgeB fallback.

**The validated training recipe** (three arcs of evidence):
corpus + stacked mixes via `--mix-corpus`, where each mix is a SPECIFIC
failure→outcome link mined at scale (edge SDs: 18.4→0.6; conversion:
stocks 0.5→0.9 async). Stacking doctrine, both clauses: every retrain
carries ALL validated mixes (convC1 lesson) AT their proven absolute
shares (convC3 dose lesson). Blanket situation-window oversampling is
EXHAUSTED (round-2: convC3/C4 vs convC2 — nothing at any dose;
val tax at 14.9% total share; dose-response data in eval_runs/0812_*).

**Evaluation doctrine:** sync pools (13 min, n=8) measure SURVIVAL
only — structurally blind to offense at any CPU level. Async realtime
(`eval_live_protocol.sh`) is the offense/promote rung. Human sessions
decide crowns (sealed-order blind protocol, eval_runs/0812_crown_decider
is the template).

**Shipped this arc (all tested):** coach roadmap #1–4 (ExPhil.Situations
47 labels / Inspect API / rewind viewer / Options + 1.5M-event knowledge
model); precise stage geometry for all 6 stages + PS's five layouts +
FoD live platforms + Randall + Whispy (players-as-probes method);
frame data for all 26 fighters (ExPhil.FrameData, hitbox-gated viewer);
the bot-Improoover loop (viewer bookmarks (M) → scenario manifest →
scenario_suite practice → scenario_dagger_mine success harvest);
suspend inhibitors in all long-running scripts.

## BRADLEY'S TACKLE ORDER

### 0. Dose-response taste test (FIRST, post-clear)
Play convC2 / convC3 / convC4 blind, one game each —
`bash eval_runs/0813_dose_session/run_dose_session.sh` (staged, sealed
order). Value: async ranks them C2 > C4 > C3; if blind feel reproduces
that ranking, the async rung is validated as a human-experience proxy.
Score + unseal after (analyze_behavior per game dir).

### 1. Debts & polish (category 7)
- **Task #5 windmill exact rate**: best path = dedicated recording (bot
  parked at fixed blade radius through full revolutions) or the decomp
  constant; hub (−36.64, 38.8) + CW already fitted.
- **Trajectory death-classifier backport** to Melee.GameEvents +
  analyze_behavior (percent<20 heuristic wrong both ways — GOTCHA #94;
  the correct pattern lives in edge_snippet_mine).
- Viewer wishlist as it arises (Bradley QA'd all six stages; current
  known-good).

### 2. RL phase — START WITH MULTISHINE FOX (Bradley's call, endorsed)
Why it's the right first RL target:
- DENSE reward already instrumented (shines/min, ShineChain strict
  chains, the stand-dummy rung).
- NARROW domain: drill setting constrains exploration; cheap rollouts
  (headless sync recipe).
- ANCHOR exists: KL-anchor to ms_g15 (the slippi-ai lesson — unanchored
  RL collapses; reference_slippi_ai_clone.md); promote ladder catches
  regressions (and the g6 lesson: never crown on stand-dummy numbers
  alone).
- Spec seed: OFFLINE_RL_SPEC (F5) exists; adapt to the specialist
  first, generalist second. The coach's value model (roadmap #5) shares
  this build — one outcome/value head serves both.
Design sketch to start from: offline RL (advantage-weighted /
reward-filtered) on the existing drill + eval replay pool with shine-
chain reward, KL(π‖ms_g15) anchor, evaluate on the standing ladder
(stand rung → local human chain → netplay).

### 3. Then (order TBD with results)
- **Delay-train convC2** — unlocks netplay for the generalist = human
  games at volume (feeds everything).
- Bookmark-driven scenario practice on the decider moments (loop is
  built; round-2 verdict says precision beats volume for the flaw
  list).
- Coach roadmap #5–7 (value model overlaps RL; Improoover .gci spike
  needs a live session; curriculum layer last).
- Erickfm ranked corpus decision (850k rank-labeled games, pre-3.18,
  ~180GB for Fox — knowledge-model upgrade, not event source).
- v3-edge arm (per-stage ledge; corpus rebuild + train, mechanical).

## OPEN TASKS (harness)
- #5 windmill rate (in_progress — hub/direction done, rate bracketed).

## KEY GOTCHAS THIS ARC (numbered in GOTCHAS.md)
#92 corpus mode ignores --mix-frames (use --mix-corpus); #93 replay-mode
windows cross snippet boundaries; #94 percent<20 death classifier;
#95 Output.banner length crash; #96 external-vs-internal stage ids
(YS 8 = FoD 8 collision — convert via Melee.Enums.Stage.from_external).
Plus unnumbered: mix DOSE is the treatment; sync pools can't measure
offense; pgrep self-match (use "[p]attern" bracket idiom); Livebook
release packaging (`livebook start`, LIVEBOOK_HOME, Mix.install needs
top-level nx/exla overrides when EDIFICE_LOCAL_NX=1).
