# HANDOFF 2026-07-29 — records through the new harness; dummy artifact; headless ladder; next: DAgger through the delayed bridge. Supersedes HANDOFF_2026-07-28.md.

Everything on `main` through `d0b070b` (9 commits, 2026-07-28 both
sessions). Laptop (Manjaro, Hyprland). Full evidence:
[LATENCY_ARCHITECTURE.md](LATENCY_ARCHITECTURE.md) (directions #2/#4
validation + "dummy artifact + headless trust campaign" section).

## READ FIRST — rules that save sessions

1. All of 28's standing rules hold: >=3 runs; pre-register predictions
   (score this week: ~4 confirmed, 9 FAILED — failures taught more);
   Output writes to STDERR; the `mix` hazard; metronome phenotype
   warning (~45-50/min chains 1).
2. **The eval opponent is part of the distribution — NEVER compare
   across dummies.** The 07-28 "champion 121-147" numbers were
   STAND-dummy; cpu-1 blocks score ~70 with the same policy. This
   artifact burned three prereg failures before being caught (by a
   human watching the game, not by the numbers).
3. **Check `uptime` before eval blocks** — and know that the 07-28
   "degraded regime" theory was WRONG (fresh reboot changed nothing;
   it was the dummy). Load still matters at the margin.
4. **Harness quality metric: offset-calibration sharpness**
   (`probe_cycle_margins.exs` cal spread). Sharp single peak >=0.75 =
   trustworthy timing; flat/smeared = variable jitter, chains collapse.
   Offsets are harness-specific — do not compare peak POSITIONS across
   harnesses, only concentration.

## State of play

**Champion: `checkpoints/ms_open_z.bin` — 165-186 self/min, chains
15-30 (all all-time records), stand-dummy windowed async, THROUGH the
polling harness.** Windowed `--blocking-input` is free (170-182,
chain 30). cpu-1 lineage numbers: ~70/min chains 4-6.

**Harness (all landed + validated 2026-07-28):**
- Polling console default ON (`console_timeout` 0.1s; `--console-timeout
  0` = legacy blocking AND auto-gates LRAS off — a pausing chord
  deadlocks blocking dispatch, reproduced 2/2).
- LRAS 11/11 instant quits with polling. SD no longer costs data.
- MeleePort `poll: true` surfaces `:no_frame`; legacy callers get
  transparent re-poll (~88s cap).
- Stateful step path: fixture bit-parity gate green
  (stateful_fixture_parity_test.exs); live block: 1.38x below windowed
  (UNRESOLVED <2x, arms don't overlap — carried-state divergence
  suspected: trained as sliding h0+16 windows, deployed as whole-game
  state). O(1) win confirmed (staleness 0.5-1.0% vs 1.4-1.6%).
  --stateful-step stays opt-in.
- Headless on laptop: Slippi Launcher Beta channel installs
  `~/.config/Slippi Launcher/netplay-beta/Slippi_Netplay_Mainline-x86_64.AppImage`
  (do NOT AppImageLauncher-integrate). Point DOLPHIN_DIR at the FILE.
  Mainline headless replays need `--replay-dir` (monthly subfolder:
  glob recursively; ~/Slippi gets NOTHING — protocol's copy grabs
  stale files).
- Headless ladder (stand dummy): speed1.0/pace0 122-163 chains 10-16
  (RECOMMENDED recipe) > speed1.0/pace60 > speed0/* (offset SMEAR,
  chains 2-10, do not use for timing-critical). Drift-free pacer +
  pace-before-send landed (inert in recommended configs).
- `--emulation-speed 0`: menus at max fps — run overhead ~2min ->
  ~12s. Blocked for timing-critical use until a runtime menus-0/game-1
  speed switch exists.

## NEXT UP (user-directed, task list live in session)

1. **DAgger through the delayed bridge** (the campaign's most
   promising +1-adaptation path, untried):
   - Audit `scripts/dagger_drill.exs` (exists; eval protocol's busy
     check greps for it).
   - Teacher: the shifted closed-loop expert (`closed_loop_d1`,
     triggers one frame early — held a 787-chain through
     online_delay=1; fixture
     `test/fixtures/replays/fox_multishine_closed_d1.slp`).
   - Student drives THROUGH `--frame-delay 1`; teacher relabels
     visited states (farm-5 rule: recorded controllers are only valid
     labels when the recorder was the teacher — DAgger satisfies this
     by construction).
   - GATE: calibration-sharpness on delayed-bridge replays BEFORE
     training on them.
   - Prereg: chains at online_delay=1 climb across rounds; delay-0 may
     degrade (fine).
   - Collection: WINDOWED (headless not yet timing-equivalent).
2. Runtime menu/game speed switch (unlocks fast menus everywhere).
3. Headless full trust (close cal 0.67 -> >=0.75, chains gap).
4. Multi-game-per-boot (--games N) + mainline instant_match_restart.

## Open problems (carried)

1. Shift pathology (six seeds, mechanism open) — full-cycle offline
   rollout is the wanted instrument.
2. Stateful carried-state divergence (see above) — hybrid k-frame
   re-sync or train-unroll=deploy-step if it ever needs resolving.
3. SD residual flake telemetry still armed.
4. 27d leftovers: Mewtwo transfer of coverage synthesis; ledge drill;
   DAgger/PPO on GPU.

## Machine facts

- Laptop uptime resets don't fix eval numbers (see rule 3).
- Best block hygiene: stand dummy for capability, windowed async,
  `uptime` check, protocol script (kills dolphins, copies replays,
  scores).
- eval_runs/ untracked-by-design; last night's dirs: stand_windowed
  (records), stand_windowed_blocking (chain 30), stand_headless*,
  polling_ab, stateful_arm/windowed_arm, postreboot_baseline.
