# HANDOFF 2026-08-04b — resume point

> **EVENING ADDENDUM (17:30)** — the afternoon arc (full entries in
> INTERP_ROADMAP_V2's experiment log): (1) **#27 bake-off CLOSED, P3 —
> GRU stays** (mamba_2 on the champion recipe: stand 121.8 c1, rung-0
> 2.91; fit the data, failed closed-loop). (2) **y-augmentation (cycle
> 4a/4a2)**: overdose P3'd the core; safe dose produced `ms_g11b_yaug`
> with the all-time STAND record (428.4 c427) but n=10 YS replication
> shows ~80% collapse — altitude augmentation does NOT fix YS; parked.
> (3) **n=8 replications inverted every n<=3 YS reading of the day**:
> g10b is 11/11 no-collapse on YS (chains 43-208) — the P1 verdict's YS
> half is REINSTATED for the g10b artifact (though bakeoff_gru shows the
> recipe isn't seed-guaranteed). **Hierarchy: g10b > g4 > g11b.**
> EVAL_PROTOCOL's run-level-bucket rule is now written in blood: YS
> claims need n>=8. Ops: my own probe beside a live eval beam SIGBUS'd
> it (NO-MIX covers eval stages too); snippet delay-mismatch is now
> FATAL in the drill (GOTCHA #86); dagger_drill gained --y-augment
> (frame-budgeted). Next moves list below unchanged — the human session
> with g10b is still the gate that matters.

Supersedes HANDOFF_2026-08-04.md (morning). That doc's headline
correction (no checkpoint multishines vs a human; dummy rankings can
invert; production g4_d2mix) still stands EXCEPT as amended below.
Everything here happened in one 02:50-09:00 sprint; the experiment log
in `docs/planning/INTERP_ROADMAP_V2.md` holds the full entries.

## HEADLINE: cycle-3b = P1. The curation loop closed POSITIVE.

**`checkpoints/ms_g10b_human.bin`** — g4's recipe + 12,339 REAL-HUMAN
snippet frames re-mined at the correct `--action-delay 2`:

| gate | g10b | reference |
|---|---|---|
| stand FD d3 (chains) | **421.4/min c421** | g4: 423 (gate >=300) |
| YS collapse | **0 of 3** (c103/c139/c177) | g4: 2 of 3 (c1-c2) |
| YS platform landings | **0 in all runs** | absorbed g4 runs: entry each |
| opp-sensitivity rung 0 | **1.44** (robust band) | g4 1.34 / g6 3.84 |

- **Cycle 3a (morning, ms_g10_human, stand 104.9) was INVALID** — its
  snippets were mined at action-delay 5 vs the drill's 2; the aligned
  re-mine also removed ~80 phantom anchors (173→93). 3a vs 3b on the
  same source games attributes the ENTIRE 3a failure to misalignment.
- Caveat on the record: 3b converged at loss 1.0e-5 in 17 epochs
  (aligned prev-controller makes prediction far easier). Behavior evals
  are clean; the number is recorded in case it recurs somewhere it
  shouldn't.
- **NOT yet verified vs a human.** g10b's promotion case is offline
  only. The next human session should score it (chains, per
  EVAL_PROTOCOL stopping rules) before any production claim.

## Mechanisms NAMED today (all causal, all instrumented)

1. **The absorber is the platform, and the broken link is the JC.**
   Entry = mid-cycle shine-jump lands on YS's low platform (y=23.5);
   in that context the X head is hard-off (fire 0.0000 over ~5,400
   frames, max logit < 0) while down+B persists → shine-hold/squat.
   Patching own-y ALONE to 23.45 on healthy windows reproduces the
   silence (X mean −0.99 → −6.0): **the OOD channel is own-y, not
   stage flags** → #33 multi-stage fixtures target the right variable
   (grounded-at-height exemplars); DL likely safe offline only because
   its platforms are too high to land on mid-cycle.
2. **g6's static overfit = opponent-PERCENT as cycle clock.** The
   dummy's percent climbs with every shine — a metronome. Fine-grained
   perturbation: percent-only moves g6's logits more than teleporting
   the dummy 120 units; g4 instead reads opponent ACTION. Open
   falsifiable prediction: freeze dummy percent → g6 chains degrade,
   g4's don't.
3. **Human play ≈ absorber-level OOD** (trunk-Mahalanobis): g4 scores
   human games 7.6-10.9k vs self-floor 35 — same magnitude as absorbed
   YS runs. The human gap and the absorber are one off-manifold
   phenomenon. BUT cross-policy OOD comparison is compression-
   confounded (g6 maps human states CLOSER — blindness, not
   robustness); within-policy monitoring only.

## New instruments (all shipped + validated today)

- **336-dim interp extension**: `Activations.embed_frames/embed_config_for`
  — every interp instrument (BasinRollout, CycleSim, probe scripts) now
  handles queue/delay-id policies; `--delay-id` REQUIRED for
  with_delay_id checkpoints (raises otherwise).
- **`ExPhil.Interp.AbsorberEntry`** — platform-landing entry detector,
  floor-tested per GOTCHA #79 (3/3; healthy platform touches correctly
  rejected). Ready as the cycle-4+ snippet anchor. YS contrastive pair
  promoted to `test/fixtures/replays/ys_multishine_{good,absorbed}_2026-08-04.slp`.
- **`scripts/probe_opponent_dependence.exs`** — promote_check rung 0
  (offline, seconds): LOW = robust, HIGH = static-overfit red flag;
  inversely ranks human outcome perfectly at n=3 (+g10b consistent).
  `--fine` gives per-channel attribution.
- **`scripts/probe_platform_patch.exs`** — state-level patching on
  contrastive pairs (the fast-tracked attribution tier).
- **`scripts/probe_ood_score.exs`** — within-policy Mahalanobis drift
  monitor (f64 Cholesky — Nx.LinAlg.invert NaNs on these covariances).
- **`docs/guides/EVAL_PROTOCOL.md`** — CRN pairing (deterministic
  FD/BF = 1 run not 3), sequential stopping, successive halving,
  calibration ledger (seeded n=3: FD chain does NOT rank human outcome;
  opp-sens inversely does).

## Specs ready to implement (docs/planning/)

- **DISTILL_ANCHOR_SPEC.md (F3)**: KL anchor to frozen g4 via
  PRECOMPUTED teacher logits + clean-frame mask (Route A); frozen-base
  low-rank adapter = hot-swappable specialist delta (Route B). With P1
  landed these are cycle-5+ dose-raising levers, not rescue.
- **OFFLINE_RL_SPEC.md (F5)**: advantage-weighted BC from
  Rewards.Standard return-to-go; shuffled-weight control arm; rung-0
  guardrail. Gated on human corpus ~30 games + cycle-4 concluding.

## Ops lessons

- **The miner's action-delay warning printed and was ignored** — cycle
  3a launched anyway and burned 100 GPU-minutes. Contract mismatches
  that corrupt training should ABORT, not warn (queued below).
- Deterministic stages: the traditional 3-run stand eval duplicates
  bit-identical replays; 1 run per EVAL_PROTOCOL from now on.
- `MetricFloor` is test-env only — `mix run -e` one-offs must use Peppi
  directly.

## Next moves (in order)

1. **Human session** (Bradley-gated): score g10b vs a person — chains,
   pre-registered stopping question, new ledger row. ACAB#182 is still
   owed a match; the YETI August debut clock is running.
2. **Make the snippet action-delay mismatch FATAL** in dagger_drill
   (小 change; the 3a lesson) + one-line GOTCHA entry.
3. **Cycle 4 = #33 multi-stage fixtures** (now evidence-backed by the
   y-channel kill test): record grounded-at-height multishine fixtures
   (YS/BF platforms), retrain, gate on AbsorberEntry + YS chains. The
   YS residual (chains 100-177 vs FD 421, squat 3-11%) is the target.
   AbsorberEntry-anchored snippets are the fallback arm if fixtures
   alone stall.
4. **Percent-freeze falsification** (cheap, one eval config): freeze
   dummy percent, prediction g6 degrades / g4 holds — closes the W2
   story's loop.
5. **W3 relapse dashboard**: bad-habit + absorber-direction projections
   across g2→g10b; then snippet pre-filtering for cycle 5.
6. **#27 bake-off re-aim** with the new instruments as metrics
   (rung 0, AbsorberEntry, OOD drift — not headline dummy score).
7. Parked: F3/F5 implementation (triggers in specs), attribution-graph
   flagship (fast-track delivered; full graphs when a named question
   needs feature-level edges), auditing game, RL staging (#29 — F5 is
   its bridge), Mewtwo arm conversion eval (#from 08-02, still open).

## Checkpoint inventory (delta)

**`ms_g10b_human.bin` — best offline card in the lab** (stand 421 c421,
YS 0-collapse, rung-0 1.44) but human-unverified; `ms_g4_d2mix` remains
PRODUCTION until g10b passes a human session per the promotion rule.
`ms_g10_human.bin` = cycle-3a artifact, labeled example of channel-
misalignment damage — keep for forensics, never deploy. Rest unchanged
from HANDOFF_2026-08-04.
