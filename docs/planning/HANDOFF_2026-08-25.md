# HANDOFF 2026-08-25 — ep4 crowned; JIT closed; generalist line OPEN

**Resume protocol: read this + memory's [project_jit_warmup] (the
08-24/25 arc lives there in detail) + GUARDS_BACKLOG.md. Supersedes
HANDOFF_2026-08-24. All suites green; ~20 commits 0c18659..HEAD.**

## Production

**CHAMPION: `checkpoints/ms_g19_ep4.bin`** (crowned 08-24, blind
stage-pinned FD decider vs ms_g15: canonical chains {32,12} vs
{3,3,13}). Deploy netplay = `--frame-delay 4 --delay-id-override 3`
(id4 UNTRAINED — guard now enforces); local d3. Fallback ms_g15.
**Transfer-selected candidate: `ms_g19r3_ep4.bin`** (fox ceiling +
broad transfer 344/274/363 vs mewtwo/yoshi/ICs where the champion is
a popo-only specialist, mewtwo/yoshi CHAIN 1) — blind decider vs
champion SET UP and PAUSED: scratchpad decider2/ (arms green/orange
blinded, key.txt UNREAD, order: green orange orange green green
orange), eval_runs/0824_transfer_decider. Mewtwo games from Bradley
are the informative ones.

## The 08-24/25 arc in bullets (details in memory + eval_runs RESULTS)

1. **JIT WARMUP CLOSED** (JIT_WARMUP.md all-verdicts): probes =
   `--stateful-step` 1.5s; windowed deploys/fleets = **policy server
   0ms checkouts** (POLICY_SERVER_DESIGN.md; warm-agent POOLING is
   the amortizer, NOT JIT identity; boot-time distribution required;
   sessions release + run CPU-only; server beam = no-mix law;
   `eval_live_protocol.sh --server` adopted). Exec cache BISECTED
   dead (predict-cache load timing starves the frame loop).
   Stateful+resync60 restores chains locally; netplay tail sampling
   open (deploy default stays windowed).
2. **Stage program**: FoD heights (0x80C62F90/0x80C63CF0) + PS
   transform digit (0x8043205C, GrPs<d>.dat loader buffer,
   ps_transform/1) hunted, fresh-boot verified, RAM-merged into local
   gamestates (netplay = passive probe until addresses verify there).
   Searching-flag candidates FALSIFIED. Unthrottled probes =
   emulation_speed 0 + blocking_input. `--no-frozen-stadium` (exphil
   played frozen PS forever). W4 verdict: champion trunk STAGE-BLIND
   on both stages (discards even the input's leaked layout signal) →
   `--stage-internals` embedding feature (+7, default off) + **live
   id-space fix** (bridge fed INTERNAL stage ids to the
   external-keyed embedding — every live game ever embedded stage as
   "other").
3. **Recipe science**: champion recipe replicates n=5 — fox stand
   gate SATURATED at ~439/min ceiling (dead as discriminator); argmax
   epoch = lottery {4,16,45,4,59,2} → **gate-sweep mandatory**;
   mewtwo transfer = 10x lottery AND selectable → **joint protocol:
   fox-ceiling ∩ mewtwo gate** (mewtwo = hardest opponent test,
   predicts yoshi/ICs; the reverse doesn't hold). AWBC already in the
   champion (0813 arms + 0819 replicate history); RTG shelved by the
   philosophy verdict: true-state channels yes, training-artifact
   channels = specialist garnish; **the road = generalist restart +
   BC-then-RL** (surpass-Phillip-Mewtwo is Track A's decision
   problem).
4. **GENERALIST LINE OPEN**: erickfm master-Fox corpus fully
   extracted (7,911 games, replays/erickfm_ranked/FOX/extracted).
   Pilot trained: 300 games GRU-60, val 4.694 —
   `fox_gen_pilot1_fixed.bin` (metadata-corrected copy; see caveat).
   **Bradley's first live look (0825_gen_pilot_live): mostly idles;
   crouches when approached; occasional jab/dtilt/short-hop; sometimes
   runs offstage** — textbook small-corpus BC mode-averaging.
   Scale wall: 300 files fits, 1,000 dies at val device placement →
   full corpus needs --stream-chunk-size.
5. **Guards program** (GUARDS_BACKLOG.md): BUILT = untrained
   delay-id guard (+ train_delays metadata incl --multi-delay),
   embedding fingerprint canary, windowed-fps starvation alarm
   (EXPHIL_STARVATION_FATAL=1 in evals), gate-sweep 5-fail abort,
   drill unknown-flag abort (#9), replay-dir default, embed-size
   truth (export computes from embed config; agent trusts canary
   length). Remaining: #4 RAM ranges, #5 GPU lockfile, #6 checkpoint
   lint (**now with a concrete spec: verify the PARAMS' input width
   vs canary length at save** — the pilot's config lied 296 while
   params were 288), #7 convergence trust, #8 matchmaking-timeout
   error signature.

## Known-broken / owed next

- **train.exs data pipeline drops --stage-internals** (third
  standalone config-build; the pilot trained 288 despite the flag —
  metadata claimed 296). Fix = wire the flag through its pipeline
  embed-config + guard #6's params-width check so metadata can't lie.
- Streaming scale-up: generalist on all 7,911 via --stream-chunk-size.
- Transfer decider (paused mid-setup, game 1 green was searching).
- Passive: resync netplay tails; netplay stage-address probes
  (~10s log lines on netplay FoD/PS games); #8 error signature hunt.
- 5 decider-era flag/bug fixes are committed; the sync runner now
  honors --delay-id-override.

## Ops laws (new this arc; older ones stand)

Score chains from replays or the qtrace `act=` line — NEVER press
counts. Copy deploy knobs from the last record session
(DEPLOY_KNOBS.md). Policy-server/eval `mix compile` BEFORE the server
boots. Standalone OptionParser scripts must reject unknown flags.
The stand fox gate cannot discriminate arms anymore. Every arm gets a
gate-sweep. Single-run transfer claims are void (10x lottery).
